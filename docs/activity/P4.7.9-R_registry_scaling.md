# P4.7.9-R — Known-template registry scaling

Date: 2026-05-16  
Status: A3a / A3b / A3c verified, A3d documented and committed.

## Purpose

P4.7.9-R starts after P4.7.8-R was closed as DONE / VERIFIED / DOCUMENTED / REPRODUCIBLE.

P4.7.8-R proved that the known-template production chain works for two different templates:

- german-marketing-handwriting-practice
- knee-training-health-practice

The verified chain is:

Activity Event -> Object-Action Rubricator -> approved Entity Classification -> Value Object Bridge -> Value Object Instance / Link -> State Delta -> Aggregates / Snapshots -> Processing Logs -> Impact Events / Impact Aggregates.

P4.7.9-R changes the focus from proof to registry scaling.

## A3a result

A3a was read-only inspection.

Important schema corrections:

- real impact rules table: public.impact_rules
- not: public.activity_impact_rules
- activity_templates has default_activity_type_id
- not: activity_type_id

A3a confirmed:

- both target templates exist;
- both target value objects exist;
- German template has 5 impact rules;
- Knee template has 3 impact rules;
- rubricator references exist and are approved / active;
- German metadata was legacy-only;
- Knee metadata was partially normalized.

## A3b result

A3b updated only:

public.activity_templates.default_metadata_json

for:

- german-marketing-handwriting-practice
- knee-training-health-practice

No changes were made to:

- activity_events
- entity_classifications
- value_objects
- impact_rules

A3b added / normalized:

- knownTemplateRegistry
- rubricatorCandidate
- valueObjectTitle
- valueObjectMapping

Strict flags from live DB:

{
  "kneeRuleKeyOk": true,
  "germanRuleKeyOk": true,
  "templates_count": 2,
  "kneeValueObjectMappingOk": true,
  "kneeRubricatorCandidateOk": true,
  "germanValueObjectMappingOk": true,
  "germanRubricatorCandidateOk": true,
  "bothTemplatesHaveValueObjectTitle": true,
  "bothTemplatesHaveValueObjectMapping": true,
  "bothTemplatesHaveRubricatorCandidate": true,
  "bothTemplatesHaveKnownTemplateRegistry": true
}

## A3c result

A3c was read-only post-normalization audit.

Final flags from live DB:

{
  "kneeOk": true,
  "germanOk": true,
  "templatesCount": 2,
  "allRegistriesOk": true,
  "allRubricatorRefsOk": true,
  "allImpactRulesCountsOk": true,
  "allValueObjectMappingsOk": true,
  "a3cReadyToDocumentAndCommit": true
}

## German template after normalization

- slug: german-marketing-handwriting-practice
- template ID: 8eef44f7-9e29-461d-8af0-3db4af1d3c92
- rule key: german_marketing_handwriting_practice_to_business_german
- object type: German_language
- action type: practice
- context: learning
- contextual category: business-german
- value object: Business German writing practice
- value object type: skill
- unit: minutes
- impact rules count: 5

## Knee template after normalization

- slug: knee-training-health-practice
- template ID: c1698136-341e-4f61-b091-fab366a0942f
- rule key: knee_training_health_practice_to_knee_exercises
- object type: knee
- action type: train
- context: health
- contextual category: knee-exercises
- value object: Knee training practice
- value object type: health_activity
- unit: minutes
- impact rules count: 3

## Files

SQL file:

docs/sql/P4.7.9-R-A3_known_template_metadata_normalization.sql

Documentation file:

docs/activity/P4.7.9-R_registry_scaling.md

## Current decision

activity_templates.default_metadata_json is now the transitional DB-backed registry v0.1.

Do not remove hardcoded registry rules yet.

Safe next path:

1. create typed DB metadata read-helper;
2. validate metadata shape strictly;
3. compare DB metadata against hardcoded rules;
4. keep hardcoded fallback;
5. only then consider switching runtime read path.

## Next block

Next step:

P4.7.9-R-A4a — inspect current helper / mapper contracts and decide read-helper location.

Likely new file later:

lib/activity/knownTemplateRegistryMetadata.ts

Expected helper behavior:

- read activity_templates.default_metadata_json;
- return typed known-template registry metadata;
- validate knownTemplateRegistry, rubricatorCandidate and valueObjectMapping;
- never call AI;
- never mutate DB;
- stay diagnostic / read-only until A5 / A6.

## Anti-mistakes

Do not create a new registry table yet.

Do not remove hardcoded rules yet.

Do not switch runtime to DB metadata before comparison audit.

Do not use AI/text fallback in production known-template classification.

Do not repeat P4.7.8-R final route audit unless route/helper/bridge/mapper behavior changes.

---

## 12. A4a / A4b / A4c — registry metadata helper

A4a inspected helper and mapper contracts before adding a DB metadata read-helper.

A4b created:

lib/activity/knownTemplateRegistryMetadata.ts

Purpose:
- read activity_templates.default_metadata_json;
- parse normalized known-template registry metadata;
- validate knownTemplateRegistry, rubricatorCandidate and valueObjectMapping;
- return typed metadata for later comparison against hardcoded rules.

Exported functions:
- parseKnownTemplateRegistryMetadataRow
- readKnownTemplateRegistryMetadataBySlug
- readKnownTemplateRegistryMetadataById
- summarizeKnownTemplateRegistryMetadata

Important behavior:
- no AI calls;
- no DB mutation;
- no classification creation;
- no Value Object Bridge execution;
- no runtime replacement of hardcoded rules yet.

A4c documents and commits the helper.

Next block: P4.7.9-R-A5a — compare hardcoded registry with DB metadata before any runtime switch.

---

## 16. A5a / A5b / A5c / A5d / A5e — diagnostic comparison

A5 compared the current hardcoded registry / mapper with DB metadata v0.1.

Diagnostic script:

scripts/diagnostics/P4.7.9-R-A5b_compare_known_template_registry_metadata.ts

A5c first run showed:
- allDbOk = true;
- allSourceOk = false;
- reason: false-positive check rubricatorValueObjectMapperHasRuleKey.

Decision:
- classification ruleKey belongs to knownTemplateRubricatorRules.ts;
- rubricatorValueObjectMapper.ts maps approved classification to Value Object;
- mapper is not required to contain the same classification ruleKey.

A5d patched the diagnostic comparator by removing the false-positive mapper ruleKey check.

Final A5d result:
- templatesCount = 2;
- allSourceOk = true;
- allDbOk = true;
- allOk = true;
- errors = empty.

Verified templates:
- german-marketing-handwriting-practice;
- knee-training-health-practice.

Current decision after A5:
- DB metadata v0.1 matches hardcoded source of truth for both known templates;
- runtime still uses hardcoded registry / mapper;
- no runtime switch has been made yet;
- hardcoded fallback must remain for the next step.

Next block:
- P4.7.9-R-A6 planning / implementation decision;
- possible next technical action: introduce DB-backed metadata read path behind hardcoded fallback, without removing current rules.
