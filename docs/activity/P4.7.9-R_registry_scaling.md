# P4.7.9-R — Known-template registry scaling

Date: 2026-05-16  
Status: A3a / A3b / A3c verified, A3d documented and committed.

## Purpose

P4.7.9-R starts after P4.7.8-R was closed as DONE / VERIFIED / DOCUMENTED / REPRODUCIBLE.

P4.7.8-R proved that the known-template production chain works for two different templates:

- german-marketing-handwriting-practice
- knee-training-health-practice

The verified chain is:

Activity Event -> Object-Action Rubricator -> approved Entity Classification -> Value Object Bridge -> Value Object Instance / Link -> State Delta -> Aggregates / Snapshots -> Processing Logs -> Impact Events / Impact Aggregates.

P4.7.9-R changes the focus from proof to registry scaling.

## A3a result

A3a was read-only inspection.

Important schema corrections:

- real impact rules table: public.impact_rules
- not: public.activity_impact_rules
- activity_templates has default_activity_type_id
- not: activity_type_id

A3a confirmed:

- both target templates exist;
- both target value objects exist;
- German template has 5 impact rules;
- Knee template has 3 impact rules;
- rubricator references exist and are approved / active;
- German metadata was legacy-only;
- Knee metadata was partially normalized.

## A3b result

A3b updated only:

public.activity_templates.default_metadata_json

for:

- german-marketing-handwriting-practice
- knee-training-health-practice

No changes were made to:

- activity_events
- entity_classifications
- value_objects
- impact_rules

A3b added / normalized:

- knownTemplateRegistry
- rubricatorCandidate
- valueObjectTitle
- valueObjectMapping

Strict flags from live DB:

{
  "kneeRuleKeyOk": true,
  "germanRuleKeyOk": true,
  "templates_count": 2,
  "kneeValueObjectMappingOk": true,
  "kneeRubricatorCandidateOk": true,
  "germanValueObjectMappingOk": true,
  "germanRubricatorCandidateOk": true,
  "bothTemplatesHaveValueObjectTitle": true,
  "bothTemplatesHaveValueObjectMapping": true,
  "bothTemplatesHaveRubricatorCandidate": true,
  "bothTemplatesHaveKnownTemplateRegistry": true
}

## A3c result

A3c was read-only post-normalization audit.

Final flags from live DB:

{
  "kneeOk": true,
  "germanOk": true,
  "templatesCount": 2,
  "allRegistriesOk": true,
  "allRubricatorRefsOk": true,
  "allImpactRulesCountsOk": true,
  "allValueObjectMappingsOk": true,
  "a3cReadyToDocumentAndCommit": true
}

## German template after normalization

- slug: german-marketing-handwriting-practice
- template ID: 8eef44f7-9e29-461d-8af0-3db4af1d3c92
- rule key: german_marketing_handwriting_practice_to_business_german
- object type: German_language
- action type: practice
- context: learning
- contextual category: business-german
- value object: Business German writing practice
- value object type: skill
- unit: minutes
- impact rules count: 5

## Knee template after normalization

- slug: knee-training-health-practice
- template ID: c1698136-341e-4f61-b091-fab366a0942f
- rule key: knee_training_health_practice_to_knee_exercises
- object type: knee
- action type: train
- context: health
- contextual category: knee-exercises
- value object: Knee training practice
- value object type: health_activity
- unit: minutes
- impact rules count: 3

## Files

SQL file:

docs/sql/P4.7.9-R-A3_known_template_metadata_normalization.sql

Documentation file:

docs/activity/P4.7.9-R_registry_scaling.md

## Current decision

activity_templates.default_metadata_json is now the transitional DB-backed registry v0.1.

Do not remove hardcoded registry rules yet.

Safe next path:

1. create typed DB metadata read-helper;
2. validate metadata shape strictly;
3. compare DB metadata against hardcoded rules;
4. keep hardcoded fallback;
5. only then consider switching runtime read path.

## Next block

Next step:

P4.7.9-R-A4a — inspect current helper / mapper contracts and decide read-helper location.

Likely new file later:

lib/activity/knownTemplateRegistryMetadata.ts

Expected helper behavior:

- read activity_templates.default_metadata_json;
- return typed known-template registry metadata;
- validate knownTemplateRegistry, rubricatorCandidate and valueObjectMapping;
- never call AI;
- never mutate DB;
- stay diagnostic / read-only until A5 / A6.

## Anti-mistakes

Do not create a new registry table yet.

Do not remove hardcoded rules yet.

Do not switch runtime to DB metadata before comparison audit.

Do not use AI/text fallback in production known-template classification.

Do not repeat P4.7.8-R final route audit unless route/helper/bridge/mapper behavior changes.

---

## 12. A4a / A4b / A4c — registry metadata helper

A4a inspected helper and mapper contracts before adding a DB metadata read-helper.

A4b created:

lib/activity/knownTemplateRegistryMetadata.ts

Purpose:
- read activity_templates.default_metadata_json;
- parse normalized known-template registry metadata;
- validate knownTemplateRegistry, rubricatorCandidate and valueObjectMapping;
- return typed metadata for later comparison against hardcoded rules.

Exported functions:
- parseKnownTemplateRegistryMetadataRow
- readKnownTemplateRegistryMetadataBySlug
- readKnownTemplateRegistryMetadataById
- summarizeKnownTemplateRegistryMetadata

Important behavior:
- no AI calls;
- no DB mutation;
- no classification creation;
- no Value Object Bridge execution;
- no runtime replacement of hardcoded rules yet.

A4c documents and commits the helper.

Next block: P4.7.9-R-A5a — compare hardcoded registry with DB metadata before any runtime switch.

---

## 16. A5a / A5b / A5c / A5d / A5e — diagnostic comparison

A5 compared the current hardcoded registry / mapper with DB metadata v0.1.

Diagnostic script:

scripts/diagnostics/P4.7.9-R-A5b_compare_known_template_registry_metadata.ts

A5c first run showed:
- allDbOk = true;
- allSourceOk = false;
- reason: false-positive check rubricatorValueObjectMapperHasRuleKey.

Decision:
- classification ruleKey belongs to knownTemplateRubricatorRules.ts;
- rubricatorValueObjectMapper.ts maps approved classification to Value Object;
- mapper is not required to contain the same classification ruleKey.

A5d patched the diagnostic comparator by removing the false-positive mapper ruleKey check.

Final A5d result:
- templatesCount = 2;
- allSourceOk = true;
- allDbOk = true;
- allOk = true;
- errors = empty.

Verified templates:
- german-marketing-handwriting-practice;
- knee-training-health-practice.

Current decision after A5:
- DB metadata v0.1 matches hardcoded source of truth for both known templates;
- runtime still uses hardcoded registry / mapper;
- no runtime switch has been made yet;
- hardcoded fallback must remain for the next step.

Next block:
- P4.7.9-R-A6 planning / implementation decision;
- possible next technical action: introduce DB-backed metadata read path behind hardcoded fallback, without removing current rules.

---

## 17. A6a / A6b / A6c / A6d — DB-backed resolver with hardcoded fallback

A6 begins safe preparation for a DB-backed known-template registry read path.

A6a inspected the current classification lifecycle before any runtime patch.

Confirmed runtime classification calls still go through:
- src/app/api/activity/record/route.ts
- src/app/api/activity/complete/route.ts
- src/app/api/activity/intake/events/[id]/confirm/route.ts

A6b created:

lib/activity/knownTemplateRegistryRuleResolver.ts

Purpose:
- read DB metadata through knownTemplateRegistryMetadata.ts;
- compare DB metadata against hardcoded known-template rule;
- return DB-backed rule only if metadata matches hardcoded rule;
- fall back to hardcoded rule if metadata is missing, invalid or mismatched.

Exported functions:
- findHardcodedKnownTemplateRuleBySlug
- compareKnownTemplateDbMetadataWithHardcodedRule
- buildKnownTemplateRuleFromDbMetadataWithHardcodedDefaults
- resolveKnownTemplateRubricatorClassificationRule

A6c created diagnostic script:

scripts/diagnostics/P4.7.9-R-A6c_test_known_template_rule_resolver.ts

First A6c run failed only because local tsx execution did not automatically load .env.local:
- Missing SUPABASE_URL

A6c-fix loaded .env.local into the PowerShell process and reran the diagnostic successfully.

Final A6c result:
- templatesCount = 2;
- checksCount = 4;
- allOk = true;
- dbMetadataRowsOk = true;
- hardcodedFallbackRowsOk = true.

Verified behavior:
- prefer_db_metadata returns source = db_metadata for both templates;
- hardcoded_only returns source = hardcoded_fallback for both templates;
- dbMetadataMatchesHardcoded = true;
- mismatches = empty;
- warnings are empty in DB-backed mode;
- hardcoded_only mode intentionally reports that DB metadata was not read.

Important current decision:
- runtime routes are still not patched;
- hardcoded registry remains safe source of truth / fallback;
- resolver is ready for the next controlled integration step.

Next block:
- P4.7.9-R-A7a inspect exact lifecycle patch point;
- then optionally patch activityRubricatorClassificationLifecycle.ts to use resolver in prefer_db_metadata mode with hardcoded fallback.

---

## 18. A7a / A7b / A7c / A7d — lifecycle integration of DB-backed resolver

A7 safely integrates the DB-backed known-template rule resolver into the classification lifecycle.

A7a inspected the exact lifecycle patch point in:

lib/activity/activityRubricatorClassificationLifecycle.ts

The previous hardcoded-only selection point was:

const rule = findRuleForTemplateSlug(templateSlug);

A7b patched the lifecycle helper to call:

resolveKnownTemplateRubricatorClassificationRule({
  supabase: input.supabase,
  templateSlug,
  mode: "prefer_db_metadata",
});

Important safety behavior:
- hardcoded rules were not removed;
- resolver still falls back to hardcoded rule if DB metadata is missing, invalid or mismatched;
- lifecycle metadata now records ruleResolver.source, diagnostics and errors;
- production routes were not individually patched because they already call the lifecycle helper.

A7b-fix added a nullable templateSlug guard:

template_slug_missing_after_resolution

Reason:
- TypeScript correctly detected that templateSlug can be string | null after resolveTemplateSlug;
- resolver requires a non-empty string;
- the guard keeps the helper safe and explicit.

During A7b shell recovery, accidental untracked root files were created by pasted pager/continuation text.
They were removed during A7b-cleanup and A7b-cleanup-2.

A7c created diagnostic script:

scripts/diagnostics/P4.7.9-R-A7c_test_lifecycle_db_metadata_resolver.ts

A7c tested the patched lifecycle helper on already preclassified events, intentionally avoiding creation of new classifications.

Verified German event:
- event ID: 61cb7185-5c53-4e3c-9925-1d3bfada2b9b
- title: P4.7.8-R-L4.4 registry runtime verification
- beforeClassificationCount = 1
- afterClassificationCount = 1
- ruleResolver.source = db_metadata
- dbMetadataMatchesHardcoded = true
- mismatches = empty

Verified Knee event:
- event ID: c8cbb0fd-237d-44de-b13f-468102dfc4d2
- title: P4.7.8-R-L6.3 knee confirm route fixture
- beforeClassificationCount = 1
- afterClassificationCount = 1
- ruleResolver.source = db_metadata
- dbMetadataMatchesHardcoded = true
- mismatches = empty

Final A7c summary:
- templatesCount = 2;
- checksCount = 2;
- allOk = true;
- allUsedDbMetadata = true;
- noClassificationCountChanged = true.

Current decision after A7:
- lifecycle helper now prefers DB metadata;
- hardcoded fallback remains active inside resolver;
- known-template runtime path is now DB-backed but protected by hardcoded comparison;
- no new registry table has been introduced yet;
- no AI fallback was introduced.

Next block:
- P4.7.9-R-A8 — route-level runtime verification after lifecycle patch;
- verify record / complete / confirm paths still work and logs expose ruleResolver.source = db_metadata.

---

## 19. A8a / A8b — record route runtime verification after lifecycle DB-backed resolver patch

A8 verifies real route-level behavior after A7 integrated the DB-backed known-template resolver into the lifecycle helper.

A8a inspected route-level contracts for:

- src/app/api/activity/record/route.ts
- src/app/api/activity/complete/route.ts
- src/app/api/activity/intake/events/[id]/confirm/route.ts

Confirmed:
- all three routes call ensureActivityEventRubricatorClassificationForKnownTemplate(...);
- record route passes templateSlug directly from template.slug;
- complete and confirm routes can rely on lifecycle templateSlug resolution through activityTemplateId;
- processing logs currently store rubricatorClassificationResult summary fields;
- debug-trace can read activity_processing_logs and metadata, but may not expose nested resolver metadata.

A8b first browser record-route run was blocked by local auth:

POST /api/activity/record -> 401 Unauthorized

After local authentication was restored, A8b browser runtime verification created a real record-route event:

- event ID: 4cac4811-6b77-4fbb-a196-dba3200a979e
- title: P4.7.9-R-A8b-record-1778951444025
- route response: record status = 200
- debug-trace response: status = 200

The browser trace scanner showed:
- hasRuleKey = true;
- hasRubricatorText = true;
- hasRuleResolverText = false;
- hasDbMetadataSource = false.

Interpretation:
- record route worked;
- debug-trace/processing logs exposed rubricator and ruleKey;
- debug-trace/processing logs did not expose ruleResolver.source = db_metadata.

A8b-2 created server-side diagnostic script:

scripts/diagnostics/P4.7.9-R-A8b2_inspect_record_route_event.ts

A8b-2 directly inspected the record-created event and reran the lifecycle helper on the existing event without creating duplicate classifications.

A8b-2 first execution succeeded at runtime but TypeScript check found a nullable user_id issue in the diagnostic script.

A8b-3 fixed the diagnostic script by adding an explicit userId guard before calling the lifecycle helper.

Final A8b-3 result:
- recordRouteCreatedEvent = true;
- routeEvidenceOk = true;
- lifecycleResolverEvidenceOk = true;
- noDuplicateClassification = true;
- allOk = true.

Detailed confirmed values:
- classificationsBeforeCount = 1;
- classificationsAfterCount = 1;
- classificationCountUnchanged = true;
- lifecycleOk = true;
- lifecycleSkipped = false;
- lifecycleAlreadyExisted = true;
- lifecycleRuleKey = german_marketing_handwriting_practice_to_business_german;
- lifecycleHasDbMetadataSource = true;
- lifecycleHasRuleResolver = true;
- lifecycleHasDbMetadataMatchesHardcoded = true;
- lifecycleHasEmptyMismatches = true;
- eventLogsCount = 5;
- eventLogsHaveRubricator = true;
- eventLogsHaveRuleKey = true.

Important nuance / future task:
- processingLogsExposeResolverMetadata = false;
- eventLogsExposeRuleResolver = false;
- eventLogsExposeDbMetadata = false;
- activity_processing_logs currently expose rubricator/ruleKey evidence but not nested ruleResolver metadata;
- later logging/debug-trace should be extended to preserve or expose ruleResolver.source, dbMetadataMatchesHardcoded and mismatches.

Current decision after A8b:
- record route is verified as route-level runtime OK after DB-backed lifecycle resolver integration;
- no duplicate classification was created;
- lifecycle resolver evidence is confirmed server-side;
- missing resolver metadata in processing logs is not a runtime blocker;
- A8b can be closed and committed.

Next block:
- P4.7.9-R-A8c — complete route runtime verification;
- then P4.7.9-R-A8d — confirm route runtime verification.

---

## 20. A8c — complete route runtime verification after lifecycle DB-backed resolver patch

A8c verifies the real complete route after A7 integrated the DB-backed known-template resolver into the lifecycle helper.

A8c-1 inspected the complete route contract:

- src/app/api/activity/complete/route.ts

Confirmed:
- complete route accepts an eventId and completion timing fields;
- complete route updates the activity_event to completed;
- complete route calls ensureActivityEventRubricatorClassificationForKnownTemplate(...);
- complete route passes activityTemplateId from updatedEvent.activity_template_id;
- lifecycle helper can resolve templateSlug internally through activityTemplateId;
- complete route writes rubricatorClassificationResult summary into processing logs / response-like structures, but not nested ruleResolver metadata.

A8c browser runtime verification executed:

start -> complete -> debug-trace

Created/used event:
- event ID: e20680a6-585a-480b-a267-524aacdbedcd
- title: P4.7.9-R-A8c-complete-1778952195441
- start status = 200;
- complete status = 200;
- completedEventId = e20680a6-585a-480b-a267-524aacdbedcd;
- debug-trace summary/full = OK.

Browser route-level check showed:
- startOk = true;
- completeOk = true;
- summaryTraceOk = true;
- fullTraceOk = true;
- hasRuleKey = true;
- hasRubricatorText = true;
- hasDbMetadataSource = false;
- hasRuleResolverText = false;
- A8c allOk = true.

Interpretation:
- complete route works at runtime;
- rubricator/ruleKey evidence is visible through route/debug-trace;
- nested ruleResolver/db_metadata evidence is not visible through browser debug-trace/log scan.

A8c-3 created server-side diagnostic script:

scripts/diagnostics/P4.7.9-R-A8c3_inspect_complete_route_event.ts

A8c-3 directly inspected the completed event and reran the lifecycle helper on the existing completed event without creating duplicate classifications.

Final A8c-3 result:
- completeRouteCompletedEvent = true;
- routeEvidenceOk = true;
- lifecycleResolverEvidenceOk = true;
- noDuplicateClassification = true;
- processingLogsExposeResolverMetadata = false;
- allOk = true.

Detailed confirmed values:
- eventStatus = completed;
- eventProcessingStatus = processed;
- durationMinutes = 7;
- classificationsBeforeCount = 1;
- classificationsAfterCount = 1;
- classificationCountUnchanged = true;
- lifecycleOk = true;
- lifecycleSkipped = false;
- lifecycleAlreadyExisted = true;
- lifecycleRuleKey = german_marketing_handwriting_practice_to_business_german;
- lifecycleHasDbMetadataSource = true;
- lifecycleHasRuleResolver = true;
- lifecycleHasDbMetadataMatchesHardcoded = true;
- lifecycleHasEmptyMismatches = true;
- eventLogsCount = 7;
- eventLogsHaveRubricator = true;
- eventLogsHaveRuleKey = true.

Verified classification:
- classificationId = a62eacb9-b0e5-4032-8bca-f753dfad414e;
- classificationStatus = approved;
- created = false;
- alreadyExisted = true.

Important nuance / future task:
- processingLogsExposeResolverMetadata = false;
- eventLogsExposeRuleResolver = false;
- eventLogsExposeDbMetadata = false;
- activity_processing_logs currently expose rubricator/ruleKey evidence but not nested ruleResolver metadata;
- later logging/debug-trace should be extended to preserve or expose ruleResolver.source, dbMetadataMatchesHardcoded and mismatches for record/complete/confirm routes.

Current decision after A8c:
- complete route is verified as route-level runtime OK after DB-backed lifecycle resolver integration;
- no duplicate classification was created;
- lifecycle resolver evidence is confirmed server-side;
- missing resolver metadata in processing logs is not a runtime blocker;
- A8c can be closed and committed.

Next block:
- P4.7.9-R-A8d — confirm route runtime verification.

---

## 21. A8d — confirm route runtime verification after lifecycle DB-backed resolver patch

A8d verifies the real confirm route after A7 integrated the DB-backed known-template resolver into the lifecycle helper.

Route under verification:

- POST /api/activity/intake/events/[id]/confirm
- src/app/api/activity/intake/events/[id]/confirm/route.ts

A8d-1 inspected the confirm route contract.

Confirmed:
- confirm route works with imported_pending activity_events;
- confirm route changes status to completed;
- confirm route changes processing_status to processed;
- confirm route calls ensureActivityEventRubricatorClassificationForKnownTemplate(...);
- confirm route passes activityTemplateId from updatedEvent.activity_template_id;
- lifecycle helper can resolve templateSlug internally through activityTemplateId;
- confirm route writes rubricatorClassificationResult summary into processing logs, but not nested ruleResolver metadata.

A8d-2a created a server-side imported_pending fixture script:

scripts/diagnostics/P4.7.9-R-A8d2_create_confirm_fixture.ts

Initial fixture attempt failed because processing_status was set to processing.

Observed error:

new row for relation activity_events violates check constraint activity_events_processing_status_check

A8d-2a-fix1 inspected route constants:
- promote route uses PROMOTED_PROCESSING_STATUS = pending;
- confirm route uses CONFIRMED_PROCESSING_STATUS = processed;
- record/complete/start route logs may use processing as processing log status, but activity_events.processing_status does not accept processing.

Fixture was patched from:

processing_status: processing

to:

processing_status: processed

After fix, fixture creation succeeded.

Created fixture:
- event ID: 13502589-b478-4c49-93c4-e793058e4b79;
- title: P4.7.9-R-A8d-confirm-1778952662992;
- status: imported_pending;
- processingStatus: processed;
- activityTemplateId: 8eef44f7-9e29-461d-8af0-3db4af1d3c92;
- activityTypeId: 80e43db3-46c4-4fd2-8c2a-de050cbaca7f;
- durationMinutes at fixture creation: 8.

A8d-2b browser runtime verification executed the real confirm route:

POST /api/activity/intake/events/13502589-b478-4c49-93c4-e793058e4b79/confirm

Browser result:
- confirm status = 200;
- response status = confirmed_completed;
- confirmedEventId = 13502589-b478-4c49-93c4-e793058e4b79;
- summaryTraceOk = true;
- fullTraceOk = true;
- hasConfirmedCompleted = true;
- hasRuleKey = true;
- hasRubricatorText = true;
- hasDbMetadataSource = false;
- hasRuleResolverText = false;
- A8d allOk = true.

Interpretation:
- confirm route works at runtime;
- route/debug-trace exposes rubricator and ruleKey evidence;
- route/debug-trace does not expose nested ruleResolver/db_metadata evidence.

A8d-3 created server-side diagnostic script:

scripts/diagnostics/P4.7.9-R-A8d3_inspect_confirm_route_event.ts

A8d-3 directly inspected the confirmed event and reran the lifecycle helper on the existing confirmed event without creating duplicate classifications.

Final A8d-3 result:
- confirmRouteCompletedEvent = true;
- routeEvidenceOk = true;
- lifecycleResolverEvidenceOk = true;
- noDuplicateClassification = true;
- processingLogsExposeResolverMetadata = false;
- allOk = true.

Detailed confirmed values:
- eventStatus = completed;
- eventProcessingStatus = processed;
- durationMinutes = 10;
- classificationsBeforeCount = 1;
- classificationsAfterCount = 1;
- classificationCountUnchanged = true;
- lifecycleOk = true;
- lifecycleSkipped = false;
- lifecycleAlreadyExisted = true;
- lifecycleRuleKey = german_marketing_handwriting_practice_to_business_german;
- lifecycleHasDbMetadataSource = true;
- lifecycleHasRuleResolver = true;
- lifecycleHasDbMetadataMatchesHardcoded = true;
- lifecycleHasEmptyMismatches = true;
- eventLogsCount = 2;
- eventLogsHaveRubricator = true;
- eventLogsHaveRuleKey = true.

Verified classification:
- classificationId = f6bec193-2494-45a5-af43-96cf94afc340;
- classificationStatus = approved;
- created = false;
- alreadyExisted = true;
- processorName in existing classification evidence = activity_confirm_route_known_template_rubricator_classification.

Important nuance / future task:
- processingLogsExposeResolverMetadata = false;
- eventLogsExposeRuleResolver = false;
- eventLogsExposeDbMetadata = false;
- activity_processing_logs currently expose rubricator/ruleKey evidence but not nested ruleResolver metadata;
- later logging/debug-trace should be extended to preserve or expose ruleResolver.source, dbMetadataMatchesHardcoded and mismatches for record/complete/confirm routes.

Current decision after A8d:
- confirm route is verified as route-level runtime OK after DB-backed lifecycle resolver integration;
- no duplicate classification was created;
- lifecycle resolver evidence is confirmed server-side;
- missing resolver metadata in processing logs is not a runtime blocker;
- A8d can be closed and committed.

A8 route-level verification status:
- A8b record route = verified;
- A8c complete route = verified;
- A8d confirm route = verified;
- record / complete / confirm all work after lifecycle DB-backed resolver integration.

Next block:
- P4.7.9-R-A9 — close A8 route-level verification block and decide next scaling step;
- likely next: improve logging/debug-trace to expose ruleResolver metadata OR proceed to registry-table extraction planning.

---

## 22. A9 — closure of A8 route-level verification block

A9 closes the route-level verification block after the lifecycle helper was switched to the DB-backed known-template resolver.

Verified route paths:

- A8b — /api/activity/record;
- A8c — /api/activity/complete;
- A8d — /api/activity/intake/events/[id]/confirm.

Final route-level status:

- record route = verified;
- complete route = verified;
- confirm route = verified;
- all three routes call ensureActivityEventRubricatorClassificationForKnownTemplate(...);
- all three routes work after lifecycle DB-backed resolver integration;
- no duplicate entity_classifications were created during server-side diagnostics;
- lifecycle helper returns ruleResolver.source = db_metadata;
- DB metadata matches hardcoded fallback rules;
- mismatches are empty;
- hardcoded fallback remains available.

Commits closing A8:

- 97284c5 Verify record route DB-backed resolver behavior;
- 6fcf860 Verify complete route DB-backed resolver behavior;
- 493749d Verify confirm route DB-backed resolver behavior.

Diagnostic scripts added during A8:

- scripts/diagnostics/P4.7.9-R-A8b2_inspect_record_route_event.ts;
- scripts/diagnostics/P4.7.9-R-A8c3_inspect_complete_route_event.ts;
- scripts/diagnostics/P4.7.9-R-A8d2_create_confirm_fixture.ts;
- scripts/diagnostics/P4.7.9-R-A8d3_inspect_confirm_route_event.ts.

Repeated observation across A8b/A8c/A8d:

- route/debug-trace exposes rubricator evidence;
- route/debug-trace exposes ruleKey evidence;
- route/debug-trace does not expose nested ruleResolver metadata;
- processingLogsExposeResolverMetadata = false;
- eventLogsExposeRuleResolver = false;
- eventLogsExposeDbMetadata = false.

Interpretation:

- this is not a runtime blocker;
- DB-backed resolver behavior is proven server-side;
- however, observability is incomplete;
- future debugging would be easier if activity_processing_logs and/or debug-trace exposed ruleResolver.source, dbMetadataMatchesHardcoded and mismatches.

Decision after A9:

- P4.7.9-R-A8 route-level verification is closed;
- record / complete / confirm paths are safe after DB-backed lifecycle resolver integration;
- next recommended block is A10 — improve logging/debug-trace visibility for resolver metadata before extracting a dedicated registry table.

Recommended next block:

P4.7.9-R-A10 — logging/debug-trace resolver metadata exposure

Goal of A10:

- include ruleResolver.source in processing log output or metadata;
- include dbMetadataMatchesHardcoded;
- include mismatches;
- keep logs compact;
- avoid storing large duplicated DB metadata payloads unless needed;
- verify record / complete / confirm logs expose resolver metadata without changing runtime behavior.

Alternative later block:

P4.7.9-R-A11 — registry-table extraction planning

A11 should only start after A10 or after an explicit decision to postpone logging/debug-trace improvements.

---

## 23. A10 — logging/debug-trace resolver metadata exposure

A10 fixes the observability gap discovered during A8 route-level verification.

Problem before A10:

- record / complete / confirm routes worked correctly through the DB-backed lifecycle resolver;
- server-side diagnostics proved ruleResolver.source = db_metadata;
- dbMetadataMatchesHardcoded = true;
- mismatches = [];
- however, route-level activity_processing_logs and debug-trace summary did not expose nested ruleResolver metadata;
- processingLogsExposeResolverMetadata was false in A8b/A8c/A8d.

A10a / A10b findings:

- safeCreateActivityProcessingLog already accepts metadata and persists it to activity_processing_logs.metadata_json;
- debug-trace summary builds processingTimeline from compactProcessingLog(...);
- compactProcessingLog previously exposed metadataKeys only, not compact metadata values;
- route rubricator logs already wrote ruleKey/classificationId/created/alreadyExisted in output_json;
- route rubricator logs did not write compact ruleResolver data into metadata_json.

A10c implementation:

New helper:

- lib/activity/rubricatorResolverLogMetadata.ts

Purpose of helper:

- extract a compact resolver summary from lifecycleResult.metadata.ruleResolver;
- avoid storing the full dbMetadataSummary payload in processing logs;
- keep only operational observability fields.

Compact fields stored in route log metadata:

- source;
- selectedSource;
- mode;
- requestedTemplateSlug;
- hardcodedRuleFound;
- dbMetadataReadOk;
- dbMetadataFound;
- dbMetadataMatchesHardcoded;
- mismatches;
- warnings;
- errors.

Routes patched:

- src/app/api/activity/record/route.ts;
- src/app/api/activity/complete/route.ts;
- src/app/api/activity/intake/events/[id]/confirm/route.ts.

Each route now adds:

ruleResolver: buildRubricatorResolverLogMetadata(rubricatorClassificationResult)

inside the rubricator classification processing log metadata.

Debug-trace patched:

- src/app/api/activity/debug-trace/route.ts;
- added compactProcessingLogRuleResolver(...);
- added getRecordArrayField(...);
- compactProcessingLog now exposes ruleResolver in summary mode.

Runtime verification after A10:

A10d record route:
- event ID: 0f154c3e-2120-4a31-92ca-381ef2b2858f;
- record status = 200;
- A10d allOk = true;
- summary processingTimeline exposes metadataKeys including ruleResolver;
- ruleResolver.source = db_metadata;
- ruleResolver.selectedSource = db_metadata;
- ruleResolver.dbMetadataMatchesHardcoded = true;
- ruleResolver.mismatches = [];
- ruleResolver.warnings = [];
- ruleResolver.errors = [].

A10d-2 complete route:
- event ID: d23d6cce-2923-416d-88c9-fab7684acb49;
- start status = 200;
- complete status = 200;
- A10d-2 allOk = true;
- summary processingTimeline exposes ruleResolver metadata;
- ruleResolver.source = db_metadata;
- dbMetadataMatchesHardcoded = true;
- mismatches = [].

A10d-3 confirm route:
- fresh imported_pending fixture ID: 0f522cdd-9ce5-47fd-8594-5895a69e7818;
- confirm status = 200;
- response status = confirmed_completed;
- A10d-3b allOk = true;
- summary processingTimeline exposes ruleResolver metadata;
- ruleResolver.source = db_metadata;
- selectedSource = db_metadata;
- mode = prefer_db_metadata;
- requestedTemplateSlug = german-marketing-handwriting-practice;
- dbMetadataMatchesHardcoded = true;
- mismatches = [];
- warnings = [];
- errors = [].

Final A10 result:

- record route resolver metadata exposure = verified;
- complete route resolver metadata exposure = verified;
- confirm route resolver metadata exposure = verified;
- debug-trace summary now exposes compact resolver metadata;
- the A8 observability gap is closed.

Important design decision:

- A10 does not store the full DB metadata summary in activity_processing_logs;
- only compact resolver diagnostics are exposed;
- this keeps debug-trace useful without creating heavy duplicated log payloads.

Current status after A10:

- DB-backed resolver is proven operationally and observably;
- route-level logs now show whether DB metadata or fallback was selected;
- mismatches between DB metadata and fallback can be seen in debug-trace summary;
- next block can proceed to registry-table extraction planning or broader scaling.

---

## 24. A10f — final second-template resolver metadata smoke test

A10f verifies that the A10 debug-trace resolver metadata exposure also works for the second known-template, not only for the German template used in record / complete / confirm route verification.

Template under test:

- knee-training-health-practice

Runtime test:

- route: POST /api/activity/record;
- templateSlug = knee-training-health-practice;
- title prefix = P4.7.9-R-A10f-knee-record;
- debug-trace mode = summary.

Created event:

- event ID: 5cf48d15-597a-4cf4-b4e6-1185b976b5be;
- record status = 200;
- eventLinks count in route response = 1;
- impactEvents count in route response = 3.

A10f checks:

- recordOk = true;
- summaryTraceOk = true;
- hasKneeRuleKey = true;
- hasRubricatorText = true;
- hasRuleResolverText = true;
- hasDbMetadataSource = true;
- hasDbMetadataMatchesHardcoded = true;
- hasEmptyMismatches = true;
- A10f allOk = true.

Observed ruleResolver in summary processingTimeline:

- source = db_metadata;
- selectedSource = db_metadata;
- mode = prefer_db_metadata;
- requestedTemplateSlug = knee-training-health-practice;
- hardcodedRuleFound = true;
- dbMetadataReadOk = true;
- dbMetadataFound = true;
- dbMetadataMatchesHardcoded = true;
- mismatches = [];
- warnings = [];
- errors = [].

Final interpretation:

- A10 resolver metadata exposure works for the German known-template;
- A10 resolver metadata exposure works for the knee known-template;
- both known-template registry paths expose DB-backed resolver selection in debug-trace summary;
- P4.7.9-R can proceed to A11 registry-table extraction planning.
