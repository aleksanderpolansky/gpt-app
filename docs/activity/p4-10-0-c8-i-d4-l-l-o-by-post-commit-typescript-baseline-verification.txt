GPT-APP / AI-NAVIGATOR
P4.10.0-C8-I-D4-L-L-O-BY-POSTCHECK
Post-commit TypeScript and baseline verification after O-BY report

Date: 2026-05-26
Timezone: Europe/Warsaw
Repository: gpt-app
Branch: main

Baseline
- Expected HEAD before this postcheck: 8e96c4d
- Expected origin/main before this postcheck: 8e96c4d
- Baseline commit:
  8e96c4d docs: add C8-I O-BY real auth browser verification report

Purpose
This postcheck closes a tooling gap in the O-BY report block.

During the O-BY report block, the command:

npx tsc --noEmit

was attempted directly in PowerShell. On this Windows environment, PowerShell blocked npx.ps1 because script execution is disabled by policy. After that, the previous inline script incorrectly printed "OK: TypeScript passed" because the PowerShell script did not correctly stop on that SecurityError.

This postcheck verifies TypeScript correctly by using npx.cmd instead of npx.ps1.

Command used
npx.cmd tsc --noEmit

Result
PASS_TYPESCRIPT_CHECKED_THROUGH_NPX_CMD_AFTER_O_BY_REPORT

Scope
This block is verification/report-only.
No code files are changed.
No controlled-intake route integration is performed.
No Auth0/session runtime code is changed.
No DB read is executed.
No DB write is executed.
No SQL is executed.
No AI call is executed.
No activity_events insert is performed.
No semantic candidates, Value Objects, state facts, state deltas, or state snapshots are persisted.

O-BY report status
The O-BY browser-authenticated verification report exists and records:
- missing diagnostic header -> HTTP 403 PASS
- wrong diagnostic header -> HTTP 403 PASS
- valid diagnostic header with browser credentials -> HTTP 200 PASS_AUTHENTICATED_REAL_READ
- provider auth0
- authSubject present
- FINAL_O_BY_VERIFICATION_RESULT=PASS_AUTHENTICATED_REAL_READ
- no-write/no-AI/no-SQL guardrails preserved

Important correction
The O-BY report commit remains valid as a browser proof report.
However, the TypeScript proof line from the original inline execution should be treated as untrusted because npx.ps1 was blocked.
This postcheck replaces that tooling proof with a valid TypeScript check using npx.cmd.

Next step decision
Proceed to P4.10.0-C8-I-D4-L-L-O-BZ / O-CA:
server-side app user mapping read-only design gate.

Next corridor boundaries
- Define mapping Auth0 subject -> internal app user.
- Design only first.
- No controlled-intake route integration yet.
- No activity_events insert yet.
- No DB write yet.
- No AI call yet.
- First DB interaction must be a controlled read-only corridor after design/contract proof.

Anti-retest notes
Do not repeat O-BT/O-BU/O-BV/O-BW checks unless their files changed.
Do not repeat failed O-BX initial implementation.
Do not repeat O-BX-FIX1.
Do not repeat first false-positive O-BY browser JS checker.
Do not run C8-H extractor proofs unless ruleExtractor.ts changes.
Do not run /api/activity/complete canonical proof unless the relevant runtime file changes.