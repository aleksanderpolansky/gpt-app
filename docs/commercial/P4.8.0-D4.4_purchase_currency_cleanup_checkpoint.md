# P4.8.0-D4.4 — purchase currency cleanup final checkpoint

Date: 2026-05-18
Status: DONE

## Summary

P4.8.0-D4 closed the purchase confirmation currency cleanup.

The duplicate field was removed from live Supabase DB:

- removed: public.purchase_confirmations.currency
- kept: public.purchase_confirmations.purchase_currency

purchase_currency is now the single active runtime/business currency field for purchase confirmations.

## Completed steps

### D4 inventory

Created local inventory for purchase confirmation currency sync.

### D4.1 live RPC inspection

Confirmed that public.submit_purchase_confirmation:

- accepts p_purchase_currency
- computes v_purchase_currency
- inserts into purchase_confirmations.purchase_currency
- does not explicitly insert purchase_confirmations.currency
- writes purchase_currency into purchase_confirmation_events

### D4.2 conclusion

Confirmed that purchase_currency remains the active business/runtime field.

Confirmed that purchase_confirmations.currency is a duplicate less-integrated field.

### D4.3 migration

Applied safe live migration:

- checked purchase_currency exists
- checked currency exists
- checked drift between purchase_currency and currency
- dropped purchase_confirmations.currency only after drift check

Post-check confirmed only purchase_currency remains:

column_name        data_type  is_nullable  column_default
purchase_currency  text       NO           'PLN'::text

### Runtime verification

git grep found no tracked runtime references to:

purchase_confirmations.currency

TypeScript check passed:

npx.cmd tsc --noEmit

## Final decision

Target state achieved:

- purchase confirmations have one active currency field: purchase_currency
- submit_purchase_confirmation already writes purchase_currency
- purchase_confirmation_events already use purchase_currency
- cart/order/item semantics were not introduced
- Value Object work can continue after this checkpoint is committed and pushed

## Next block

After this commit/push, proceed to Category-Derived Value Object Foundation:

1. inventory existing value_objects / rubricator / category / activity tables
2. identify current conflicts with v4.2
3. design minimal additive schema for category-derived Value Objects
4. keep parent/child only
5. keep free text as first input source
6. do not start full UI/UX yet
