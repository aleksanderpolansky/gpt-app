# P4.8.0-A6 — Live Structural Verification Result

Date: 2026-05-18 09:32:15

Status: LIVE DB VERIFIED / NO MIGRATION YET

Runtime code changed: NO

Purpose: document live database verification after A5 decision checkpoint.

## 1. Git status

```text
## main...origin/main
?? docs/commercial/
?? docs/sql/P4.8.0-A2_live_schema_inventory.sql
?? docs/sql/P4.8.0-A6_live_structural_verification.sql
?? docs/commercial/
?? docs/sql/P4.8.0-A2_live_schema_inventory.sql
?? docs/sql/P4.8.0-A6_live_structural_verification.sql
```

## 2. Confirmed live schema results

### 2.1 offers.value_object_id

- Column exists: YES.
- Type: uuid.
- Nullable: YES.
- FK exists: YES.
- FK definition: FOREIGN KEY (value_object_id) REFERENCES value_objects(id) ON DELETE SET NULL.
- Index exists: YES.
- Index: offers_value_object_id_idx.
- Orphan check: OK, orphan_count = 0.

Conclusion:

No additive FK/index migration is needed for offers.value_object_id.

### 2.2 offer_items

- Table exists: YES.
- Row count: 1.
- offer_items.offer_id exists: YES, NOT NULL.
- offer_items.organization_id exists: YES, NOT NULL.
- offer_items.value_object_id exists: YES, NOT NULL.
- offer_items.quantity exists: YES, numeric, NOT NULL, default 1.
- offer_items.unit_price exists: YES, numeric, nullable.
- offer_items.total_price exists: YES, numeric, nullable.
- offer_items.sort_order exists: YES, integer, NOT NULL, default 0.
- FK offer_items.offer_id -> offers(id): YES, ON DELETE CASCADE.
- FK offer_items.organization_id -> organizations(id): YES, ON DELETE CASCADE.
- FK offer_items.value_object_id -> value_objects(id): YES, ON DELETE RESTRICT.
- Index offer_items_offer_id_idx: YES.
- Index offer_items_organization_id_idx: YES.
- Index offer_items_value_object_id_idx: YES.
- Orphan offer_id check: OK, orphan_count = 0.
- Orphan value_object_id check: OK, orphan_count = 0.

Conclusion:

offer_items is a real, structurally valid offer package/composition table. It is not a purchase confirmation item table and does not conflict with the A5 decision.

### 2.3 purchase_confirmations

- Table exists: YES.
- Row count: 8.
- organization_id exists: YES, NOT NULL, FK to organizations(id) ON DELETE CASCADE.
- buyer_user_id exists: YES, NOT NULL, FK to app_users(id) ON DELETE CASCADE.
- purchase_amount exists: YES, numeric, NOT NULL.
- status exists: YES, text, NOT NULL, default requested.
- points_awarded exists: YES, numeric, nullable, default 0.
- currency exists: NO.
- offer_id exists: NO.
- value_object_id exists: NO.
- purchase_items exists: NO.
- purchase_confirmation_items exists: NO.
- cart_id exists: NO.
- order_id exists: NO.
- Shape check: OK_ABSENT.

Conclusion:

purchase_confirmations correctly remains organization-level and amount-based. No cart/order/item structure exists. This confirms the A5 model.

Additive gap:

purchase_confirmations.currency is absent. Because purchase_amount without currency can become ambiguous, A7 should consider a small additive currency column.

### 2.4 value_objects

- Table exists: YES.
- Row count: 5.
- id exists: YES.
- organization_id exists: YES, nullable, FK to organizations(id) ON DELETE SET NULL.
- owner_actor_id exists: YES, nullable, FK to actors(id) ON DELETE SET NULL.
- value_type exists: YES, text, NOT NULL.
- commercial_usage exists: NO.
- parent_value_object_id exists: NO.

Conclusion:

The unified Value Object table already supports organization and owner actor links. commercial_usage is absent and remains the main optional additive field candidate.

### 2.5 RPC

- request_certificate_from_offer: PRESENT.
- seller_redeem_certificate: PRESENT.
- add_points_to_wallet: PRESENT.
- spend_points_from_wallet: PRESENT.

Conclusion:

Certificate and points logic is already RPC-based. A7 must not bypass this with manual balance or certificate mutations.

## 3. Confirmed no-migration-needed areas

- No migration needed for offers.value_object_id FK.
- No migration needed for offers.value_object_id index.
- No migration needed for offer_items FK/index foundation.
- No migration needed to create offer_items.
- No migration needed for purchase_confirmation_items because that table/field must not exist.
- No migration needed to add purchase_confirmations.offer_id.
- No migration needed to add purchase_confirmations.value_object_id.

## 4. Real additive candidates after A6

| Candidate | Live status | Recommendation |
|---|---|---|
| value_objects.commercial_usage | ABSENT | Consider additive nullable text field with CHECK constraint. Use only if it helps separate VO type from commercial scenario. |
| purchase_confirmations.currency | ABSENT | Consider additive non-destructive currency field. Because existing rows already exist, use nullable or default/backfill strategy carefully. |

## 5. Recommended A7 migration scope

A7 should be a small additive migration plan only. It should not change runtime code yet.

Allowed A7 candidates:

1. Add value_objects.commercial_usage as nullable text with a safe CHECK constraint:
   - none
   - catalog_info
   - certificate_base
   - both

2. Add purchase_confirmations.currency as nullable text or defaulted text:
   - likely default: 'PLN'
   - must consider existing 8 rows
   - must not introduce cart/order/item relation

3. Add comments on columns/tables to freeze semantics.

Forbidden A7 changes:

- Do not create purchase_confirmation_items.
- Do not add purchase_confirmations.offer_id.
- Do not add purchase_confirmations.value_object_id.
- Do not change offer_items semantics into cart/order.
- Do not change certificate RPC logic.
- Do not mutate points manually.
- Do not touch runtime code.

## 6. A6 conclusion

- A6 live verification passed.
- The commercial core relationship foundation is stronger than expected.
- The main structural links already exist and are protected.
- Only two additive candidates remain: value_objects.commercial_usage and purchase_confirmations.currency.
- Next step: P4.8.0-A7 additive migration plan draft.

