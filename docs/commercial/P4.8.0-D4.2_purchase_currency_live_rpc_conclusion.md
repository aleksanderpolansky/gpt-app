# P4.8.0-D4.2 — purchase currency live RPC conclusion

Date: 2026-05-18
Scope: commercial currency cleanup
Status: DONE — no DB migration required at this point.

## Main finding

Live Supabase SQL inspection confirmed:

- public.purchase_confirmations has purchase_currency.
- public.purchase_confirmations does not have a separate currency column.

Confirmed result:

has_currency = false
has_purchase_currency = true

## RPC behavior

public.submit_purchase_confirmation accepts:

p_purchase_currency text DEFAULT NULL::text

It resolves currency through this chain:

p_purchase_currency -> organization.default_currency -> PLN

Then it writes the resolved value into:

- public.purchase_confirmations.purchase_currency
- public.purchase_confirmation_events.purchase_currency

## Existing data state

At inspection time:

total_rows = 8
rows_with_purchase_currency = 8
distinct_purchase_currency_count = 1

Currency values:

purchase_currency = PLN
row_count = 8

## Decision

No migration to drop purchase_confirmations.currency is needed, because the column does not exist.

The active business/runtime field for purchase confirmations is:

purchase_confirmations.purchase_currency

## Architecture decision

The commercial currency contract remains:

organization.country_code -> organization.default_currency -> purchase_confirmations.purchase_currency

currency must not be reintroduced into purchase_confirmations.

## Next action

Perform repository-level reference audit and document whether any application code still assumes purchase_confirmations.currency.

If no dangerous references are found, close P4.8.0-D4 and return to the next Value Object / Category-Derived VO Foundation block.
