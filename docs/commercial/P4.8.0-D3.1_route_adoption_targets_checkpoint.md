# P4.8.0-D3.1 — Route Adoption Targets Checkpoint

Status: CHECKPOINT READY FOR COMMIT

Runtime code changed: NO

## 1. Completed D3 scope

Completed:

- D3 route adoption targets inventory.
- Confirmed shared helper exists.
- Confirmed no runtime route imports helper yet.
- Confirmed first safe adoption candidates.

## 2. Current helper

Shared helper:

- src/lib/commercial/currency.ts

Current helper status:

- created;
- typechecked;
- committed and pushed in D2;
- not yet adopted by runtime routes.

## 3. First safe adoption candidates

First adoption should be limited to:

- src/app/api/organizations/route.ts
- src/app/api/organizations/[id]/location/route.ts

Reason:

- both contain duplicated local getDefaultCurrencyByCountryCode logic;
- both contain duplicated local normalizeCountryCode logic;
- both are organization currency source routes;
- replacing duplicated helpers here should be behavior-preserving if done carefully.

## 4. Not first adoption candidates

Do not change yet:

- src/app/api/directory/organizations/route.ts
- src/app/api/offers/route.ts
- src/app/api/my/purchase-confirmations/route.ts
- src/app/api/purchase-confirmations/route.ts

Reason:

- directory route contains MINIMUM_PURCHASE_THRESHOLDS and should be handled later with commercial_currency_rules;
- offers route has offer-specific currency logic;
- purchase confirmation currency sync is a separate later block.

## 5. D3 finding

D3 confirmed:

- helperExists = YES;
- organizationsRouteHasLocalGetDefaultCurrencyByCountryCode = YES;
- organizationLocationRouteHasLocalGetDefaultCurrencyByCountryCode = YES;
- organizationsRouteHasLocalNormalizeCountryCode = YES;
- organizationLocationRouteHasLocalNormalizeCountryCode = YES;
- anyTargetImportsSharedHelper = NO.

## 6. Next implementation step

Recommended next implementation step:

- P4.8.0-D3.2 — adopt shared helper in organization routes only.

Expected behavior:

- remove or stop using duplicated local country/currency helpers in organization routes;
- import from "@/lib/commercial/currency";
- keep behavior unchanged;
- do not touch directory/offers/purchase-confirmation routes yet;
- run TypeScript check after patch.

## 7. Forbidden changes remain forbidden

- Do not add purchase_confirmation_items.
- Do not add purchase_confirmations.offer_id.
- Do not add purchase_confirmations.value_object_id.
- Do not turn purchase confirmation into cart/order/item flow.
- Do not let organization currency become arbitrary manual user input.
- Do not transfer burned points to seller.
- Do not bypass certificate/points RPC.
