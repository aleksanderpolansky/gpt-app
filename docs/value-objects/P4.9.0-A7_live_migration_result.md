# P4.9.0-A7 — live migration result

Date: 2026-05-18
Status: DONE

## Migration result

Live Supabase migration P4.9.0-A7 was applied successfully.

## Created tables

- public.value_object_category_links
- public.activity_event_value_object_links
- public.value_object_usage_aggregates

## Extended table

public.value_objects was extended with:

- parent_value_object_id uuid
- entity_protocol_characteristics_json jsonb NOT NULL DEFAULT {}
- needs_user_review boolean NOT NULL DEFAULT false
- ui_visibility text NOT NULL DEFAULT visible
- category_origin_json jsonb NOT NULL DEFAULT {}
- metadata_json jsonb NOT NULL DEFAULT {}

## activity_event_value_object_links

This table is the v4.2 direct Activity Event to Value Object analytical exposure projection.

Confirmed columns:

- id
- user_id
- event_id
- value_object_id
- exposure_minutes
- source
- confidence
- metadata_json
- created_at
- updated_at

Important: this v4.2 projection table does not use relation_type as the central semantic model.

## value_object_category_links

This table is the v4.2 bridge between derived Value Objects and global category/rubricator material.

Confirmed columns:

- id
- value_object_id
- category_table
- category_id
- category_role
- source
- confidence
- metadata_json
- created_at
- updated_at

## value_object_usage_aggregates

This table is the minimal object-cloud/read-optimization aggregate layer.

Confirmed columns:

- id
- user_id
- value_object_id
- usage_count
- exposure_minutes
- first_used_at
- last_used_at
- last_event_id
- source
- metadata_json
- created_at
- updated_at

## Compatibility result

The migration was additive.

It did not delete:

- value_objects
- value_object_instances
- activity_event_value_object_instance_links
- value_object_daily_aggregates
- value_object_state_snapshots

## Conclusion

P4.9.0-A7 is complete.

The minimal v4.2 Category-Derived Value Object Foundation now exists at DB schema level.

Next technical block: connect processor/code to the new v4.2 tables carefully, without breaking the existing VO/VOI pipeline.
