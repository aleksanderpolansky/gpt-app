GPT-APP / AI-NAVIGATOR
Category Derivation Layer v1
P4.10.0-C8-E-F6-E-E-F
Controlled production_persist proof route implementation result
Created: 2026-05-23

1. Purpose

This document records the result of P4.10.0-C8-E-F6-E-E-E.

This is a documentation-only step.

No runtime proof is executed here.

2. Current implementation result

Implemented route:

- /api/activity/debug/category-derivation-controlled-production-persist-proof

Implemented file:

- src/app/api/activity/debug/category-derivation-controlled-production-persist-proof/route.ts

Implementation commit:

- 8f1af0e Add Category Derivation controlled production persist proof route

3. What was implemented

A separate controlled production_persist debug proof route was created.

The route is intended to be:

- debug-only
- authenticated
- proof/test-only
- non-canonical business flow
- cleanup-aware
- production_persist-proof marked
- adapter-based
- not direct routeRunner-based

4. Important implementation markers

The implemented route contains these required markers:

- P4.10.0-C8-E-F6-E-E-controlled-production-persist-proof
- /api/activity/debug/category-derivation-controlled-production-persist-proof
- production_persist
- controlled_production_persist_proof
- cleanupEligible
- productionPersistProof
- runCategoryDerivationForCompleteRoute
- controlled_production_persist_proof_pass
- controlled_production_persist_proof_fail
- completeRouteCalled: false
- eventCompletedByThisRoute: false
- openAiCalled: false
- callsOpenAi: false
- sqlExecutedByPowerShell: false
- schemaChanged: false
- rlsChanged: false
- permissionsChanged: false
- nonCanonicalBusinessEvent: true

5. Safety result from implementation step

During implementation:

- typecheck command completed
- route file was committed
- route file was pushed
- working tree became clean
- /api/activity/complete was not modified
- no browser fetch was executed
- no SQL command was executed
- no production_persist runtime proof was executed
- no OpenAI call was executed

6. Known implementation note

The first marker check stopped because the safety key used uppercase OpenAI in callsOpenAI: false.

That was only a marker-check problem.

The route was patched from:

- callsOpenAI

to:

- callsOpenAi

After this patch, route marker verification passed.

7. What is still not approved

Still not approved:

- production_persist runtime proof
- starting dev server with CATEGORY_DERIVATION_ROUTE_RUNNER_MODE=production_persist
- calling the new controlled proof route in production_persist mode
- direct production_persist on /api/activity/complete
- strict failActivityComplete=true
- cleanup SQL
- SQL verification from PowerShell
- schema changes
- migrations
- RLS changes
- permission changes
- UI work
- OpenAI calls
- broad refactor

8. Required next step

Recommended next step:

P4.10.0-C8-E-F6-E-E-G - plan cleanup and rollback policy before controlled production_persist runtime proof

Reason:

F6-E-E-D explicitly blocked runtime proof until cleanup and rollback planning exists.

Before executing the new route in production_persist mode, we need a separate cleanup/rollback document defining:

- proof event cleanup eligibility
- exact rows that may be cleaned
- exact rows that must never be cleaned
- category_derivation_runs cleanup scope
- activity_category_derivations cleanup scope
- whether cleanup is manual or optional
- where SQL may be executed
- confirmation that SQL must not be run in PowerShell

9. Current safety gates

Until a later approved document changes them:

- no production_persist runtime execution
- no OpenAI call
- no schema or migration change
- no RLS or permission change
- no UI work
- no SQL in PowerShell
- no broad refactor
- no cleanup command without separate review
- no real /api/activity/complete production_persist proof yet

10. Definition of Done

This F6-E-E-F documentation step is complete when:

- this document exists under docs/value-objects
- only this document is changed
- document is committed and pushed
- working tree is clean
- recent git log shows this document above 8f1af0e
