# P4.9.5-A1 — reusable object-cloud SQL view plan

Date: 2026-05-18
Status: READY FOR SQL EXECUTION

## Purpose

Create the first reusable object-cloud read interface:

- public.value_object_cloud_profiles_v1

## Why SQL view first

This is safer than starting with API/UI because it verifies the stable read model directly in Supabase before exposing it through application code.

## Source tables

- value_object_category_links
- contextual_categories
- value_object_usage_aggregates
- activity_event_value_object_links
- activity_events
- activity_templates
- value_object_state_snapshots
- value_object_daily_aggregates

## Expected view output

One row per:

- user_id
- value_object_id
- contextual category link

Each row should include:

- category slug/name/status
- usage count
- total exposure minutes
- first/last used timestamps
- latest event exposure
- latest activity template
- snapshots JSON
- daily aggregates JSON

## Verification expectation

Current database should return:

- view_rows_count: 2
- categories: business-german and knee-exercises

## Next step

Run docs/sql/P4.9.5-A1_create_value_object_cloud_profiles_v1.sql in Supabase SQL Editor.
