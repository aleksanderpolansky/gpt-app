# P4.10.0-C5b — controlled text fallback inspection checkpoint

Date: 2026-05-19
Scope: Free-text processor v1 / controlled fallback
Status: INSPECTION DONE — ready for minimal C6 implementation planning

## Context

P4.10.0-C5 exact TypeScript contract inspection is closed.

Latest confirmed commit before this inspection:

- b1601b5 Inspect free-text value object contracts

## Files produced

- docs/value-objects/P4.10.0-C5b_controlled_text_fallback_mapper_evidence.txt
- docs/value-objects/P4.10.0-C5b_lifecycle_flag_usage_evidence.txt
- docs/value-objects/P4.10.0-C5b_registry_rules_fallback_evidence.txt
- docs/value-objects/P4.10.0-C5b_controlled_fallback_key_ranges.txt
- docs/value-objects/P4.10.0-C5b_controlled_fallback_summary.txt

## Evidence counts

- mapper fallback evidence lines: 1640
- lifecycle flag usage evidence lines: 766
- registry rules fallback evidence lines: 1224
- controlled fallback key range lines: 821

## Main finding

rubricatorValueObjectMapper.ts already contains a controlled fallback mechanism.

Important existing pieces:

- allowControlledTextFallback
- createMissingControlledValueObject
- ControlledRubricatorValueObjectRule
- fallbackNeedleGroups
- findOrCreateControlledValueObject
- buildControlledMapping

## Interpretation

Free-text v1 probably does not need a new freeTextValueObjectMapper.ts as the first implementation step.

The smaller implementation path is likely:

1. Keep valueObjectBridge.ts unchanged.
2. Keep routes record / complete / confirm unchanged.
3. Enable existing controlled fallback from activityValueObjectLifecycle.ts.
4. Add one small controlled rule for the first deterministic test phrase if no walking rule already exists.

## Current limitation

The visible controlled/registry rules are business-german and knee-exercises oriented.

For the first free-text v1 test, the code likely needs a controlled walking/commute rule, for example:

- walked to work for 15 minutes
- walking to work 15 minutes
- went to work on foot for 15 minutes

## C6 candidate

P4.10.0-C6 should be a minimal code change:

- inspect full controlled rule list one more time for existing walking/commute coverage;
- if missing, add one controlled walking/commute rule;
- update activityValueObjectLifecycle.ts to pass allowControlledTextFallback: true;
- probably also pass createMissingControlledValueObject: true;
- run TypeScript check;
- test with a simple free-text activity.

## Guardrails

- Do not modify valueObjectBridge.ts unless TypeScript forces it.
- Do not modify record / complete / confirm routes unless required.
- Do not add schema.
- Do not introduce OpenAI/AI classifier yet.
- Do not introduce typed relation edges.
- Keep known-template flows unchanged.
