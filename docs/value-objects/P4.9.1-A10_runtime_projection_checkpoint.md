# P4.9.1-A10 — runtime projection checkpoint

Date: 2026-05-18
Status: DONE

## Summary

P4.9.1 connected the existing Value Object Bridge runtime layer to the new minimal v4.2 foundation.

## Completed work

### A1 — runtime writer inventory

Generated local inventory of runtime writers for VO / VOI / links / aggregates / snapshots.

### A2 — writer inventory conclusion

Confirmed safest direction: additive runtime projection, not replacement of the existing VOI pipeline.

### A3 — focused writer file inspection

Identified key files and strongest integration candidates.

### A4 — full key-file extraction

Extracted full key runtime files, including dynamic confirm route using LiteralPath.

### A5 — function surface map

Mapped function surfaces, Supabase mutation points, VO/VOI references and available event/value object data.

### A6 — hot path extraction

Confirmed lib/activity/valueObjectBridge.ts as the safest first integration point.

### A7 — implementation plan

Prepared exact implementation plan for first v4.2 runtime projection.

### A8 — code change

Updated lib/activity/valueObjectBridge.ts with additive v4.2 projection support.

Added writes to:

- activity_event_value_object_links
- value_object_usage_aggregates

Preserved:

- value_object_instances
- activity_event_value_object_instance_links
- relation_type logic
- value_object_state_deltas
- value_object_daily_aggregates
- value_object_state_snapshots

### A9 — runtime verification

Confirmed with browser route + SQL that:

- old VOI link is created
- new activity_event_value_object_links row is created
- value_object_usage_aggregates row is created
- p491 metadata exists
- state deltas, daily aggregates and snapshots still work

## Verified target event

- eventId: 46ad2c6c-97a9-4b43-ad88-6e5194360473
- route: /api/activity/intake/events/[id]/confirm
- processorName: activity_confirm_route_p4_7_7
- valueObjectId: 9177fea8-de25-446b-b418-b55a766d53db
- exposure_minutes: 10

## Final decision

P4.9.1 is complete.

The v4.2 Value Object Foundation is now connected to runtime through an additive projection layer.

## Next block

Proceed to P4.9.2 only after commit/push.

Recommended next block:

P4.9.2 — connect value_object_category_links from reliable category/rubricator mapping.

Rules for next block:

- do not replace existing VOI pipeline
- do not make relation_type central v4.2 semantics
- do not create tags/entity_tags
- use existing classification metadata to populate value_object_category_links
- start with one known-template route and verify with targeted SQL
