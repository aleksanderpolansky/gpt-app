# P4.9.0-A8 — Category-Derived Value Object Foundation checkpoint

Date: 2026-05-18
Status: DONE

## Summary

P4.9.0-A1 through P4.9.0-A7 established the minimal v4.2 Category-Derived Value Object Foundation.

## Completed steps

### A1 — live schema inventory

Inspected existing Activity, Category/Rubricator, Value Object, aggregate and snapshot tables.

### A2 — v4.2 gap conclusion

Confirmed that the current schema already had a strong Activity + Category + Value Object foundation, but lacked the explicit v4.2 compatibility layer.

### A3 — local code/routes inventory

Generated local tracked code inventory for VO/category/activity references.

### A4 — local code/routes conclusion

Confirmed that v4.2 must be additive and compatibility-preserving.

### A5 — focused live schema check

Confirmed missing target tables and columns:

- value_object_category_links
- activity_event_value_object_links
- value_object_usage_aggregates
- parent_value_object_id
- entity_protocol_characteristics_json
- needs_user_review
- ui_visibility
- category_origin_json
- metadata_json

### A6 — exact migration plan

Created the SQL plan for minimal additive v4.2 foundation migration.

### A7 — live migration

Applied the migration successfully in Supabase.

## Final DB result

The database now supports:

- global category to derived Value Object bridge
- direct Activity Event to Value Object exposure projection
- exposure_minutes
- parent/child hierarchy on value_objects
- entity/protocol characteristics
- needs_user_review as UI/moderation flag
- basic object-cloud usage aggregates

## Important rules preserved

- existing VO/VOI tables were not deleted
- activity_event_value_object_instance_links was not deleted
- relation_type was not made the central v4.2 semantic model
- Activity Event remains source of truth for real time
- parent/child remains the base MVP hierarchy
- no tags/entity_tags were introduced
- no full UI/UX work was started

## Next block

Proceed to P4.9.1 — connect the existing processor/runtime layer to the new v4.2 foundation.

Recommended next steps:

1. inspect existing processor code that creates value_objects and value_object_instances
2. decide how to create value_object_category_links from contextual_categories
3. decide how to populate activity_event_value_object_links.exposure_minutes
4. decide how to update value_object_usage_aggregates
5. create one safe test scenario using an existing known-template activity
6. verify that existing VOI pipeline still works
