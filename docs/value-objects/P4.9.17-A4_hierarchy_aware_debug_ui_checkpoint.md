# P4.9.17-A4 — hierarchy-aware debug UI verification checkpoint

Date: 2026-05-18  
Scope: Value Object hierarchy read-side UI verification  
Page: `/value-objects/debug/cloud-profile`  
API: `/api/value-objects/debug/cloud-profile`

## Status

DONE.

P4.9.17-A1/A2/A3/A4 confirms that the debug UI page now reads and displays hierarchy-aware data returned by the debug API.

## What was changed

File updated:

- `src/app/value-objects/debug/cloud-profile/page.tsx`

The page now displays:

- `hierarchySummary`
- `hierarchyProfiles`
- child hierarchy profiles
- root hierarchy profiles
- verification checks
- raw hierarchy summary
- raw JSON payload

## Verified expected hierarchy

Expected parent:

- `Learning`
- id: `112bab0b-2a53-4f7a-bd62-b9fe760d0b54`

Expected child:

- `Business German writing practice`
- id: `9177fea8-de25-446b-b418-b55a766d53db`

Expected untouched root candidate:

- `Knee training practice`
- id: `b7acc958-7966-42c2-82c5-35c4de26d7ea`

## Browser visual verification

Browser page opened successfully:

- `http://localhost:3000/value-objects/debug/cloud-profile`

Visible UI results:

- `Hierarchy profiles = 2`
- `Child profiles = 1`
- `Root profiles = 1`
- `Missing parents = 0`
- `API response is OK`
- `hierarchyProfiles exists`
- `Business German writing practice is visible`
- `Business German points to Learning`
- `Business German parentExists=true`
- `Business German hierarchyRole=child`
- `Knee training practice remains root`
- `Child hierarchy profiles` section visible
- `Root hierarchy profiles` section visible
- `Raw hierarchySummary` section visible
- `Raw JSON` section visible

## Important constraints preserved

- No database write was performed in P4.9.17.
- No rollback was executed.
- The existing Learning → Business German writing practice hierarchy relation was only read and displayed.
- Health → Knee training practice was not created in this step.
- No typed relation edges were introduced.
- This remains a debug/read-side UI verification page, not final product UX.

## Technical verification

Required local checks:

- `npx.cmd tsc --noEmit`
- `Select-String` for:
  - `hierarchyProfiles`
  - `hierarchySummary`
  - `parentValueObjectId`
  - `parentExists`
  - `hierarchyRole`

## Conclusion

P4.9.17 is ready to be committed and pushed.

Next recommended block after this commit:

- return to P4.8.0-D4 commercial currency cleanup;
- inspect live RPC `submit_purchase_confirmation`;
- determine whether `purchase_confirmations.currency` is redundant;
- keep `purchase_currency` as the active runtime/business field unless live RPC inspection proves otherwise.
