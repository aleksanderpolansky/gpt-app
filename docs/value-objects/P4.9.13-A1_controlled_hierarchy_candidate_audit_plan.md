# P4.9.13-A1 — controlled hierarchy candidate audit plan

Date: 2026-05-19
Status: READY FOR SQL VERIFICATION

## Purpose

Prepare the next parent/child hierarchy step without writing data.

## SQL file

- docs/sql/P4.9.13-A1_controlled_hierarchy_candidate_audit.sql

## Source layers

- public.value_objects
- public.value_object_category_links
- public.contextual_categories
- public.value_object_hierarchy_profiles_v1

## What this audit returns

- current hierarchy rows
- all value_objects
- value_object category links
- generated parent/child candidate pairs
- no-write decision record

## Important rule

This step is read-only.

It does not update parent_value_object_id.

## Why this audit is needed

P4.9.12 created a read-only hierarchy view, but no parent/child links exist yet.

Before writing even one controlled relation, we need to choose the exact parent and child ids.

## Security incident note

Before resuming P4.9.13, the Supabase secret incident was handled:

- Supabase key replaced with sb_secret key;
- legacy API keys disabled;
- previous JWT key revoked;
- local app verified working;
- Vercel verified working;
- current repo files redacted in commit 68ac996;
- GitHub secret scanning shows 0 open alerts.

## Next step

Run the SQL in Supabase SQL Editor and send the result.

Then P4.9.13-A2 will document which relation is safe to test or whether we need a better parent object first.
