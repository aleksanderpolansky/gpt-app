GPT-APP / AI-NAVIGATOR
P4.10.0-C8-P3-B7-C2-B-SETUP-FIX1 — inspect non-completed event setup routes
Generated: 2026-05-20 17:14:50

Problem:
C2-B-FIX1 setup helper /api/activity/debug/free-text-value-object-test created an already completed/processed event.
Production /api/activity/complete returned status already_completed.

Goal:
Find an authenticated route that creates a non-completed event for a valid production complete-flow test.

Current HEAD:
dab787d Document production flow test setup inventory

============================================================
A) ROUTE EXISTENCE
============================================================
FOUND: src/app/api/activity/start/route.ts
FOUND: src/app/api/activity/record/route.ts
FOUND: src/app/api/activity/events/route.ts
FOUND: src/app/api/activity/intake/route.ts
FOUND: src/app/api/activity/intake/events/route.ts
FOUND: src/app/api/activity/complete/route.ts

============================================================
B) ROUTE CONTRACT EVIDENCE
============================================================

############################################################
ROUTE FILE: src/app/api/activity/start/route.ts
############################################################

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: type 
LABEL: contract evidence
------------------------------------------------------------

LINE 7: import { normalizeActivityEventSourceType } from "../../../../../lib/activity/activitySourceIntake";
    ACTIVITY_RECORDING_DISABLED_MESSAGE,
    ACTIVITY_RECORDING_ENABLED,
  } from "../../../../../lib/activity/activityRecordingConfig";
> import { normalizeActivityEventSourceType } from "../../../../../lib/activity/activitySourceIntake";
  import { ACTIVITY_STATUS_STARTED } from "../../../../../lib/activity/activityLifecycle";
  import {
    getDurationMs,
    safeCreateActivityProcessingLog,
  } from "../../../../../lib/activity/activityProcessingLogs";
  import { getActivityUserContext } from "../../../../../lib/activity/activityUserContext";
  import {
    createRawActivitySignal,

LINE 23: type ActivityStartBody = {
  
  export const dynamic = "force-dynamic";
  
> type ActivityStartBody = {
    templateId?: unknown;
    templateSlug?: unknown;
    shortcut?: unknown;
    input?: unknown;
    comment?: unknown;
    title?: unknown;
    startedAt?: unknown;
    startTime?: unknown;

LINE 35: type ActivityTemplateRow = {
    sourceType?: unknown;
  };
  
> type ActivityTemplateRow = {
    id: string;
    legacy_activity_code_template_id: string | null;
    owner_user_id: string | null;
    owner_actor_id: string | null;
    organization_id: string | null;
    slug: string;
    title: string;
    short_title: string | null;

LINE 73: type ActivityTemplateLinkRow = {
    updated_at: string;
  };
  
> type ActivityTemplateLinkRow = {
    id: string;
    template_id: string;
    linked_entity_type: string;
    linked_entity_id: string | null;
    linked_entity_key: string | null;
    link_role: string;
    relation_type: string;
    default_weight: number;

LINE 89: type ActivityShortcutRow = {
    metadata_json: Record<string, unknown> | null;
  };
  
> type ActivityShortcutRow = {
    id: string;
    user_id: string | null;
    template_id: string;
    shortcut_scope: string;
    shortcut_type: string;
    shortcut_value: string;
    label: string | null;
    description: string | null;

LINE 109: type ResolvedTemplate = {
    metadata_json: Record<string, unknown> | null;
  };
  
> type ResolvedTemplate = {
    template: ActivityTemplateRow;
    resolvedBy: "templateId" | "templateSlug" | "shortcut" | "legacyInputShortcut";
    shortcut: ActivityShortcutRow | null;
  };
  
  
  function asString(value: unknown): string | null {
    if (typeof value !== "string") {

LINE 331: if (sourceType === "ai_suggested") {
  }
  
  function mapTemplateLinkSourceToEventLinkSource(sourceType: string) {
>   if (sourceType === "ai_suggested") {
      return "ai";
    }
  
    if (sourceType === "imported") {
      return "import";
    }
  
    if (sourceType === "user_created" || sourceType === "organization_created") {

LINE 335: if (sourceType === "imported") {
      return "ai";
    }
  
>   if (sourceType === "imported") {
      return "import";
    }
  
    if (sourceType === "user_created" || sourceType === "organization_created") {
      return "manual";
    }
  
    if (sourceType === "system_seed" || sourceType === "legacy_migration") {

LINE 339: if (sourceType === "user_created" || sourceType === "organization_created") {
      return "import";
    }
  
>   if (sourceType === "user_created" || sourceType === "organization_created") {
      return "manual";
    }
  
    if (sourceType === "system_seed" || sourceType === "legacy_migration") {
      return "template";
    }
  
    return "template";

LINE 343: if (sourceType === "system_seed" || sourceType === "legacy_migration") {
      return "manual";
    }
  
>   if (sourceType === "system_seed" || sourceType === "legacy_migration") {
      return "template";
    }
  
    return "template";
  }
  
  async function getTemplateLinks(templateId: string) {
    const { data, error } = await supabase

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: interface 
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: export async function POST
LABEL: contract evidence
------------------------------------------------------------

LINE 445: export async function POST(request: Request) {
    });
  }
  
> export async function POST(request: Request) {
    if (!ACTIVITY_RECORDING_ENABLED) {
      return NextResponse.json(
        {
          ok: false,
          error: ACTIVITY_RECORDING_DISABLED_MESSAGE,
        },
        { status: 503 }
      );

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: request.json
LABEL: contract evidence
------------------------------------------------------------

LINE 476: body = (await request.json()) as ActivityStartBody;
    let body: ActivityStartBody;
  
    try {
>     body = (await request.json()) as ActivityStartBody;
    } catch {
      return NextResponse.json(
        {
          ok: false,
          error: "Invalid JSON body",
        },
        { status: 400 }
      );

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: eventId
LABEL: contract evidence
------------------------------------------------------------

LINE 367: eventId: string;
  }
  
  function buildEventLinkRows(params: {
>   eventId: string;
    template: ActivityTemplateRow;
    templateLinks: ActivityTemplateLinkRow[];
  }) {
    const { eventId, template, templateLinks } = params;
  
    const templateSelfLink = {
      event_id: eventId,
      linked_entity_type: "activity_template",

LINE 371: const { eventId, template, templateLinks } = params;
    template: ActivityTemplateRow;
    templateLinks: ActivityTemplateLinkRow[];
  }) {
>   const { eventId, template, templateLinks } = params;
  
    const templateSelfLink = {
      event_id: eventId,
      linked_entity_type: "activity_template",
      linked_entity_id: template.id,
      linked_entity_key: template.slug,
      link_role: "capture_template",
      relation_type: "created_from",

LINE 374: event_id: eventId,
    const { eventId, template, templateLinks } = params;
  
    const templateSelfLink = {
>     event_id: eventId,
      linked_entity_type: "activity_template",
      linked_entity_id: template.id,
      linked_entity_key: template.slug,
      link_role: "capture_template",
      relation_type: "created_from",
      weight: 1,
      confidence: 1,
      source: "template",

LINE 390: event_id: eventId,
    };
  
    const semanticLinks = templateLinks.map((link) => ({
>     event_id: eventId,
      linked_entity_type: link.linked_entity_type,
      linked_entity_id: link.linked_entity_id,
      linked_entity_key: link.linked_entity_key,
      link_role: link.link_role,
      relation_type: link.relation_type,
      weight: link.default_weight,
      confidence: link.default_confidence,
      source: mapTemplateLinkSourceToEventLinkSource(link.source_type),

LINE 721: activityEventId: createdEvent.id,
    await safeCreateActivityProcessingLog({
      userId: appUser.id,
      rawSignalId: rawSignal?.id ?? null,
>     activityEventId: createdEvent.id,
      processingRunId,
      processorName: "activity_start_route",
      processingStage: "create_event",
      processingStatus: "completed",
      severity: "info",
      message: "Activity start event created from template-first start flow.",
      input: {
        templateId: template.id,

LINE 734: activityEventId: createdEvent.id,
        title,
      },
      output: {
>       activityEventId: createdEvent.id,
        status: createdEvent.status,
        processingStatus: createdEvent.processing_status,
      },
      metadata: {
        endpoint: "/api/activity/start",
        mode: "template_first_start",
      },
      startedAt: processingStartedAt.toISOString(),

LINE 750: eventId: createdEvent.id,
    const templateLinks = await getTemplateLinks(template.id);
  
    const eventLinkRows = buildEventLinkRows({
>     eventId: createdEvent.id,
      template,
      templateLinks,
    });
  
    const { data: eventLinks, error: eventLinksError } =
      eventLinkRows.length > 0
        ? await supabase.from("event_links").insert(eventLinkRows).select()
        : { data: [], error: null };

LINE 772: activityEventId: createdEvent.id,
      await safeCreateActivityProcessingLog({
        userId: appUser.id,
        rawSignalId: rawSignal?.id ?? null,
>       activityEventId: createdEvent.id,
        processingRunId,
        processorName: "activity_start_route",
        processingStage: "link_event",
        processingStatus: "failed",
        severity: "error",
        message: "Failed to create activity start event links.",
        input: {
          eventLinkRowsCount: eventLinkRows.length,

LINE 813: activityEventId: createdEvent.id,
    await safeCreateActivityProcessingLog({
      userId: appUser.id,
      rawSignalId: rawSignal?.id ?? null,
>     activityEventId: createdEvent.id,
      processingRunId,
      processorName: "activity_start_route",
      processingStage: "link_event",
      processingStatus: "completed",
      severity: "info",
      message: "Activity start event links created.",
      input: {
        eventLinkRowsCount: eventLinkRows.length,

LINE 839: outputEventId: createdEvent.id,
      ? await markRawActivitySignalProcessed({
          signalId: rawSignal.id,
          userId: appUser.id,
>         outputEventId: createdEvent.id,
          normalizedPreview: {
            activityEventId: createdEvent.id,
            status: createdEvent.status,
            lifecycle: lifecycleStatus,
            eventLinksCount: Array.isArray(eventLinks) ? eventLinks.length : 0,
            impactsCreated: false,
          },
        })

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: inputText
LABEL: contract evidence
------------------------------------------------------------

LINE 410: function buildInputText(params: {
    return [templateSelfLink, ...semanticLinks];
  }
  
> function buildInputText(params: {
    template: ActivityTemplateRow;
    shortcut: ActivityShortcutRow | null;
    comment: string | null;
  }) {
    const { template, shortcut, comment } = params;
  
    return [
      template.title,

LINE 642: input_text: buildInputText({
          shortcut?.shortcut_type === "legacy_code"
            ? shortcut.shortcut_value
            : null,
>       input_text: buildInputText({
          template,
          shortcut,
          comment,
        }),
        title,
        description: comment ?? template.description,
        started_at: startedAt,
        ended_at: null,

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: naturalInput
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: title
LABEL: contract evidence
------------------------------------------------------------

LINE 29: title?: unknown;
    shortcut?: unknown;
    input?: unknown;
    comment?: unknown;
>   title?: unknown;
    startedAt?: unknown;
    startTime?: unknown;
    sourceType?: unknown;
  };
  
  type ActivityTemplateRow = {
    id: string;
    legacy_activity_code_template_id: string | null;

LINE 42: title: string;
    owner_actor_id: string | null;
    organization_id: string | null;
    slug: string;
>   title: string;
    short_title: string | null;
    description: string | null;
    template_group: string;
    template_scope: string;
    visibility: string;
    source_type: string;
    status: string;
    default_activity_type_id: string | null;

LINE 43: short_title: string | null;
    organization_id: string | null;
    slug: string;
    title: string;
>   short_title: string | null;
    description: string | null;
    template_group: string;
    template_scope: string;
    visibility: string;
    source_type: string;
    status: string;
    default_activity_type_id: string | null;
    default_duration_minutes: number | null;

LINE 384: template_title: template.title,
      confidence: 1,
      source: "template",
      metadata_json: {
>       template_title: template.title,
        template_scope: template.template_scope,
      },
    };
  
    const semanticLinks = templateLinks.map((link) => ({
      event_id: eventId,
      linked_entity_type: link.linked_entity_type,
      linked_entity_id: link.linked_entity_id,

LINE 418: template.title,
    const { template, shortcut, comment } = params;
  
    return [
>     template.title,
      "started",
      comment,
      shortcut ? `shortcut:${shortcut.shortcut_value}` : null,
    ]
      .filter(Boolean)
      .join(" | ");
  }
  

LINE 535: const title = asString(body.title) ?? template.title;
  
    const { template, shortcut, resolvedBy } = resolvedTemplate;
    const comment = asString(body.comment);
>   const title = asString(body.title) ?? template.title;
    const source = normalizeSourceType(
      body.sourceType,
      shortcut?.shortcut_type === "legacy_code"
        ? "legacy_code"
        : template.default_source_type
    );
  
    const lifecycleStatus = ACTIVITY_STATUS_STARTED;

LINE 561: title,
      normalizedPreview: {
        templateId: template.id,
        templateSlug: template.slug,
>       title,
        source,
        status: lifecycleStatus,
        startedAt,
        endedAt: null,
        durationMinutes: null,
      },
      occurredAt: startedAt,
      trustLevel: "medium",

LINE 647: title,
          shortcut,
          comment,
        }),
>       title,
        description: comment ?? template.description,
        started_at: startedAt,
        ended_at: null,
        duration_minutes: null,
        source,
        status: lifecycleStatus,
        privacy_scope: template.default_privacy_scope,
        processing_status: "pending",

LINE 695: title,
        input: {
          templateId: template.id,
          templateSlug: template.slug,
>         title,
        },
        error: {
          message: eventError?.message ?? "Failed to start activity event",
        },
        metadata: {
          endpoint: "/api/activity/start",
          mode: "template_first_start",
        },

LINE 731: title,
      input: {
        templateId: template.id,
        templateSlug: template.slug,
>       title,
      },
      output: {
        activityEventId: createdEvent.id,
        status: createdEvent.status,
        processingStatus: createdEvent.processing_status,
      },
      metadata: {
        endpoint: "/api/activity/start",

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: description
LABEL: contract evidence
------------------------------------------------------------

LINE 44: description: string | null;
    slug: string;
    title: string;
    short_title: string | null;
>   description: string | null;
    template_group: string;
    template_scope: string;
    visibility: string;
    source_type: string;
    status: string;
    default_activity_type_id: string | null;
    default_duration_minutes: number | null;
    quick_duration_minutes: number[] | null;

LINE 97: description: string | null;
    shortcut_type: string;
    shortcut_value: string;
    label: string | null;
>   description: string | null;
    is_favorite: boolean;
    show_in_default_ui: boolean;
    is_deprecated_alias: boolean;
    nfc_tag_id: string | null;
    voice_phrase: string | null;
    device_binding: string | null;
    sort_order: number;
    is_active: boolean;

LINE 648: description: comment ?? template.description,
          comment,
        }),
        title,
>       description: comment ?? template.description,
        started_at: startedAt,
        ended_at: null,
        duration_minutes: null,
        source,
        status: lifecycleStatus,
        privacy_scope: template.default_privacy_scope,
        processing_status: "pending",
        metadata_json: {

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: durationMinutes
LABEL: contract evidence
------------------------------------------------------------

LINE 566: durationMinutes: null,
        status: lifecycleStatus,
        startedAt,
        endedAt: null,
>       durationMinutes: null,
      },
      occurredAt: startedAt,
      trustLevel: "medium",
      privacyScope:
        template.default_privacy_scope === "shared_with_org" ||
        template.default_privacy_scope === "public_masked" ||
        template.default_privacy_scope === "public"
          ? template.default_privacy_scope

LINE 892: durationMinutes: null,
      lifecycle: {
        startedAt,
        endedAt: null,
>       durationMinutes: null,
        impactsCreated: false,
        note:
          "Activity has been started. Impacts, snapshots and aggregates will be created on completion.",
      },
      parser: {
        mode: "template_first_v2",
        resolvedBy,
        templateId: template.id,

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: startedAt
LABEL: contract evidence
------------------------------------------------------------

LINE 30: startedAt?: unknown;
    input?: unknown;
    comment?: unknown;
    title?: unknown;
>   startedAt?: unknown;
    startTime?: unknown;
    sourceType?: unknown;
  };
  
  type ActivityTemplateRow = {
    id: string;
    legacy_activity_code_template_id: string | null;
    owner_user_id: string | null;

LINE 143: function resolveStartedAt(body: ActivityStartBody) {
    return firstToken ?? null;
  }
  
> function resolveStartedAt(body: ActivityStartBody) {
    const rawStartedAt = asString(body.startedAt) ?? asString(body.startTime);
  
    if (!rawStartedAt) {
      return new Date().toISOString();
    }
  
    const startedDate = new Date(rawStartedAt);
  

LINE 144: const rawStartedAt = asString(body.startedAt) ?? asString(body.startTime);
  }
  
  function resolveStartedAt(body: ActivityStartBody) {
>   const rawStartedAt = asString(body.startedAt) ?? asString(body.startTime);
  
    if (!rawStartedAt) {
      return new Date().toISOString();
    }
  
    const startedDate = new Date(rawStartedAt);
  
    if (Number.isNaN(startedDate.getTime())) {

LINE 146: if (!rawStartedAt) {
  function resolveStartedAt(body: ActivityStartBody) {
    const rawStartedAt = asString(body.startedAt) ?? asString(body.startTime);
  
>   if (!rawStartedAt) {
      return new Date().toISOString();
    }
  
    const startedDate = new Date(rawStartedAt);
  
    if (Number.isNaN(startedDate.getTime())) {
      return null;
    }

LINE 150: const startedDate = new Date(rawStartedAt);
      return new Date().toISOString();
    }
  
>   const startedDate = new Date(rawStartedAt);
  
    if (Number.isNaN(startedDate.getTime())) {
      return null;
    }
  
    return startedDate.toISOString();
  }
  

LINE 487: const startedAt = resolveStartedAt(body);
      );
    }
  
>   const startedAt = resolveStartedAt(body);
  
    if (!startedAt) {
      return NextResponse.json(
        {
          ok: false,
          error: "Invalid startedAt or startTime",
        },
        { status: 400 }

LINE 489: if (!startedAt) {
  
    const startedAt = resolveStartedAt(body);
  
>   if (!startedAt) {
      return NextResponse.json(
        {
          ok: false,
          error: "Invalid startedAt or startTime",
        },
        { status: 400 }
      );
    }

LINE 493: error: "Invalid startedAt or startTime",
      return NextResponse.json(
        {
          ok: false,
>         error: "Invalid startedAt or startTime",
        },
        { status: 400 }
      );
    }
  
    let resolvedTemplate: ResolvedTemplate | null = null;
  
    try {

LINE 546: const processingStartedAt = new Date();
    const lifecycleStatus = ACTIVITY_STATUS_STARTED;
  
    const processingRunId = randomUUID();
>   const processingStartedAt = new Date();
  
    const rawSignalResult = await createRawActivitySignal({
      userId: appUser.id,
      sourceType: "manual_form",
      rawPayload: {
        endpoint: "/api/activity/start",
        body,
        resolvedBy,

LINE 564: startedAt,
        title,
        source,
        status: lifecycleStatus,
>       startedAt,
        endedAt: null,
        durationMinutes: null,
      },
      occurredAt: startedAt,
      trustLevel: "medium",
      privacyScope:
        template.default_privacy_scope === "shared_with_org" ||
        template.default_privacy_scope === "public_masked" ||

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: endedAt
LABEL: contract evidence
------------------------------------------------------------

LINE 565: endedAt: null,
        source,
        status: lifecycleStatus,
        startedAt,
>       endedAt: null,
        durationMinutes: null,
      },
      occurredAt: startedAt,
      trustLevel: "medium",
      privacyScope:
        template.default_privacy_scope === "shared_with_org" ||
        template.default_privacy_scope === "public_masked" ||
        template.default_privacy_scope === "public"

LINE 891: endedAt: null,
      processingRunId,
      lifecycle: {
        startedAt,
>       endedAt: null,
        durationMinutes: null,
        impactsCreated: false,
        note:
          "Activity has been started. Impacts, snapshots and aggregates will be created on completion.",
      },
      parser: {
        mode: "template_first_v2",
        resolvedBy,

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: status
LABEL: contract evidence
------------------------------------------------------------

LINE 8: import { ACTIVITY_STATUS_STARTED } from "../../../../../lib/activity/activityLifecycle";
    ACTIVITY_RECORDING_ENABLED,
  } from "../../../../../lib/activity/activityRecordingConfig";
  import { normalizeActivityEventSourceType } from "../../../../../lib/activity/activitySourceIntake";
> import { ACTIVITY_STATUS_STARTED } from "../../../../../lib/activity/activityLifecycle";
  import {
    getDurationMs,
    safeCreateActivityProcessingLog,
  } from "../../../../../lib/activity/activityProcessingLogs";
  import { getActivityUserContext } from "../../../../../lib/activity/activityUserContext";
  import {
    createRawActivitySignal,
    markRawActivitySignalFailed,

LINE 49: status: string;
    template_scope: string;
    visibility: string;
    source_type: string;
>   status: string;
    default_activity_type_id: string | null;
    default_duration_minutes: number | null;
    quick_duration_minutes: number[] | null;
    default_status: string;
    default_source_type: string;
    default_privacy_scope: string;
    icon_key: string | null;
    color_key: string | null;

LINE 53: default_status: string;
    default_activity_type_id: string | null;
    default_duration_minutes: number | null;
    quick_duration_minutes: number[] | null;
>   default_status: string;
    default_source_type: string;
    default_privacy_scope: string;
    icon_key: string | null;
    color_key: string | null;
    show_in_quick_capture: boolean;
    show_in_onboarding: boolean;
    allow_manual_duration: boolean;
    allow_comment: boolean;

LINE 433: status: ACTIVITY_RECORDING_ENABLED ? "ready" : "disabled",
      endpoint: "/api/activity/start",
      method: "POST",
      enabled: ACTIVITY_RECORDING_ENABLED,
>     status: ACTIVITY_RECORDING_ENABLED ? "ready" : "disabled",
      message: ACTIVITY_RECORDING_ENABLED
        ? "Start an activity session from a template without creating impacts yet."
        : ACTIVITY_RECORDING_DISABLED_MESSAGE,
      example: {
        templateSlug: "german-marketing-handwriting-practice",
        comment: "Started lifecycle smoke test",
        sourceType: "manual_form",
      },

LINE 452: { status: 503 }
          ok: false,
          error: ACTIVITY_RECORDING_DISABLED_MESSAGE,
        },
>       { status: 503 }
      );
    }
  
    const { appUser, personActor, errorResponse } =
      await getActivityUserContext();
  
    if (errorResponse) {
      return errorResponse;

LINE 469: { status: 500 }
          ok: false,
          error: "User context not found",
        },
>       { status: 500 }
      );
    }
  
    let body: ActivityStartBody;
  
    try {
      body = (await request.json()) as ActivityStartBody;
    } catch {

LINE 483: { status: 400 }
          ok: false,
          error: "Invalid JSON body",
        },
>       { status: 400 }
      );
    }
  
    const startedAt = resolveStartedAt(body);
  
    if (!startedAt) {
      return NextResponse.json(
        {

LINE 495: { status: 400 }
          ok: false,
          error: "Invalid startedAt or startTime",
        },
>       { status: 400 }
      );
    }
  
    let resolvedTemplate: ResolvedTemplate | null = null;
  
    try {
      resolvedTemplate = await resolveTemplate({
        body,

LINE 515: { status: 500 }
              ? error.message
              : "Failed to resolve activity template",
        },
>       { status: 500 }
      );
    }
  
    if (!resolvedTemplate) {
      return NextResponse.json(
        {
          ok: false,
          error: "templateId, templateSlug, shortcut or input is required.",

LINE 529: { status: 400 }
            comment: "Started lifecycle smoke test",
          },
        },
>       { status: 400 }
      );
    }
  
    const { template, shortcut, resolvedBy } = resolvedTemplate;
    const comment = asString(body.comment);
    const title = asString(body.title) ?? template.title;
    const source = normalizeSourceType(
      body.sourceType,

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: started
LABEL: contract evidence
------------------------------------------------------------

LINE 8: import { ACTIVITY_STATUS_STARTED } from "../../../../../lib/activity/activityLifecycle";
    ACTIVITY_RECORDING_ENABLED,
  } from "../../../../../lib/activity/activityRecordingConfig";
  import { normalizeActivityEventSourceType } from "../../../../../lib/activity/activitySourceIntake";
> import { ACTIVITY_STATUS_STARTED } from "../../../../../lib/activity/activityLifecycle";
  import {
    getDurationMs,
    safeCreateActivityProcessingLog,
  } from "../../../../../lib/activity/activityProcessingLogs";
  import { getActivityUserContext } from "../../../../../lib/activity/activityUserContext";
  import {
    createRawActivitySignal,
    markRawActivitySignalFailed,

LINE 30: startedAt?: unknown;
    input?: unknown;
    comment?: unknown;
    title?: unknown;
>   startedAt?: unknown;
    startTime?: unknown;
    sourceType?: unknown;
  };
  
  type ActivityTemplateRow = {
    id: string;
    legacy_activity_code_template_id: string | null;
    owner_user_id: string | null;

LINE 62: allow_started_at_override: boolean;
    show_in_onboarding: boolean;
    allow_manual_duration: boolean;
    allow_comment: boolean;
>   allow_started_at_override: boolean;
    allow_ended_at_override: boolean;
    input_schema_json: Record<string, unknown> | null;
    ui_schema_json: Record<string, unknown> | null;
    default_metadata_json: Record<string, unknown> | null;
    sort_order: number;
    is_active: boolean;
    created_at: string;
    updated_at: string;

LINE 143: function resolveStartedAt(body: ActivityStartBody) {
    return firstToken ?? null;
  }
  
> function resolveStartedAt(body: ActivityStartBody) {
    const rawStartedAt = asString(body.startedAt) ?? asString(body.startTime);
  
    if (!rawStartedAt) {
      return new Date().toISOString();
    }
  
    const startedDate = new Date(rawStartedAt);
  

LINE 144: const rawStartedAt = asString(body.startedAt) ?? asString(body.startTime);
  }
  
  function resolveStartedAt(body: ActivityStartBody) {
>   const rawStartedAt = asString(body.startedAt) ?? asString(body.startTime);
  
    if (!rawStartedAt) {
      return new Date().toISOString();
    }
  
    const startedDate = new Date(rawStartedAt);
  
    if (Number.isNaN(startedDate.getTime())) {

LINE 146: if (!rawStartedAt) {
  function resolveStartedAt(body: ActivityStartBody) {
    const rawStartedAt = asString(body.startedAt) ?? asString(body.startTime);
  
>   if (!rawStartedAt) {
      return new Date().toISOString();
    }
  
    const startedDate = new Date(rawStartedAt);
  
    if (Number.isNaN(startedDate.getTime())) {
      return null;
    }

LINE 150: const startedDate = new Date(rawStartedAt);
      return new Date().toISOString();
    }
  
>   const startedDate = new Date(rawStartedAt);
  
    if (Number.isNaN(startedDate.getTime())) {
      return null;
    }
  
    return startedDate.toISOString();
  }
  

LINE 152: if (Number.isNaN(startedDate.getTime())) {
  
    const startedDate = new Date(rawStartedAt);
  
>   if (Number.isNaN(startedDate.getTime())) {
      return null;
    }
  
    return startedDate.toISOString();
  }
  
  function hasTemplateAccess(template: ActivityTemplateRow, appUserId: string) {
    if (template.template_scope === "system") {

LINE 156: return startedDate.toISOString();
      return null;
    }
  
>   return startedDate.toISOString();
  }
  
  function hasTemplateAccess(template: ActivityTemplateRow, appUserId: string) {
    if (template.template_scope === "system") {
      return true;
    }
  
    if (template.template_scope === "user") {

LINE 419: "started",
  
    return [
      template.title,
>     "started",
      comment,
      shortcut ? `shortcut:${shortcut.shortcut_value}` : null,
    ]
      .filter(Boolean)
      .join(" | ");
  }
  
  export async function GET() {

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: planned
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: pending
LABEL: contract evidence
------------------------------------------------------------

LINE 655: processing_status: "pending",
        source,
        status: lifecycleStatus,
        privacy_scope: template.default_privacy_scope,
>       processing_status: "pending",
        metadata_json: {
          parser: "template_first_v2",
          lifecycle: lifecycleStatus,
          resolved_by: resolvedBy,
          ai_used: false,
          template_slug: template.slug,
          template_scope: template.template_scope,
          template_group: template.template_group,

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: completed
LABEL: contract evidence
------------------------------------------------------------

LINE 598: processingStatus: rawSignalResult.ok ? "completed" : "warning",
      processingRunId,
      processorName: "activity_start_route",
      processingStage: "ingest",
>     processingStatus: rawSignalResult.ok ? "completed" : "warning",
      severity: rawSignalResult.ok ? "info" : "warning",
      message: rawSignalResult.ok
        ? "Raw activity start signal captured."
        : "Raw activity start signal creation failed; continuing without raw signal.",
      input: {
        templateId: template.id,
        templateSlug: template.slug,
        source,

LINE 725: processingStatus: "completed",
      processingRunId,
      processorName: "activity_start_route",
      processingStage: "create_event",
>     processingStatus: "completed",
      severity: "info",
      message: "Activity start event created from template-first start flow.",
      input: {
        templateId: template.id,
        templateSlug: template.slug,
        title,
      },
      output: {

LINE 817: processingStatus: "completed",
      processingRunId,
      processorName: "activity_start_route",
      processingStage: "link_event",
>     processingStatus: "completed",
      severity: "info",
      message: "Activity start event links created.",
      input: {
        eventLinkRowsCount: eventLinkRows.length,
      },
      output: {
        eventLinksCount: Array.isArray(eventLinks) ? eventLinks.length : 0,
      },

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: .from("activity_events")
LABEL: contract evidence
------------------------------------------------------------

LINE 629: .from("activity_events")
    });
  
    const { data: createdEvent, error: eventError } = await supabase
>     .from("activity_events")
      .insert({
        user_id: appUser.id,
        performed_by_actor_id: personActor.id,
        acting_as_actor_id: personActor.id,
        acting_for_actor_id: personActor.id,
        activity_type_id: template.default_activity_type_id,
        activity_template_id: template.id,
        template_id: template.legacy_activity_code_template_id,

LINE 795: .from("activity_events")
      });
  
      await supabase
>       .from("activity_events")
        .update({ processing_status: "failed" })
        .eq("id", createdEvent.id)
        .eq("user_id", appUser.id);
  
      return NextResponse.json(
        {
          ok: false,
          error: eventLinksError.message,

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: .insert({
LABEL: contract evidence
------------------------------------------------------------

LINE 630: .insert({
  
    const { data: createdEvent, error: eventError } = await supabase
      .from("activity_events")
>     .insert({
        user_id: appUser.id,
        performed_by_actor_id: personActor.id,
        acting_as_actor_id: personActor.id,
        acting_for_actor_id: personActor.id,
        activity_type_id: template.default_activity_type_id,
        activity_template_id: template.id,
        template_id: template.legacy_activity_code_template_id,
        event_code:

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: .update({
LABEL: contract evidence
------------------------------------------------------------

LINE 796: .update({ processing_status: "failed" })
  
      await supabase
        .from("activity_events")
>       .update({ processing_status: "failed" })
        .eq("id", createdEvent.id)
        .eq("user_id", appUser.id);
  
      return NextResponse.json(
        {
          ok: false,
          error: eventLinksError.message,
          event: createdEvent,

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: processing_status
LABEL: contract evidence
------------------------------------------------------------

LINE 655: processing_status: "pending",
        source,
        status: lifecycleStatus,
        privacy_scope: template.default_privacy_scope,
>       processing_status: "pending",
        metadata_json: {
          parser: "template_first_v2",
          lifecycle: lifecycleStatus,
          resolved_by: resolvedBy,
          ai_used: false,
          template_slug: template.slug,
          template_scope: template.template_scope,
          template_group: template.template_group,

LINE 736: processingStatus: createdEvent.processing_status,
      output: {
        activityEventId: createdEvent.id,
        status: createdEvent.status,
>       processingStatus: createdEvent.processing_status,
      },
      metadata: {
        endpoint: "/api/activity/start",
        mode: "template_first_start",
      },
      startedAt: processingStartedAt.toISOString(),
      finishedAt: new Date().toISOString(),
      durationMs: getDurationMs(processingStartedAt),

LINE 796: .update({ processing_status: "failed" })
  
      await supabase
        .from("activity_events")
>       .update({ processing_status: "failed" })
        .eq("id", createdEvent.id)
        .eq("user_id", appUser.id);
  
      return NextResponse.json(
        {
          ok: false,
          error: eventLinksError.message,
          event: createdEvent,

LINE 884: processedSignalResult?.signal?.processing_status ??
        ? {
            id: rawSignal.id,
            processingStatus:
>             processedSignalResult?.signal?.processing_status ??
              rawSignal.processing_status,
          }
        : null,
      processingRunId,
      lifecycle: {
        startedAt,
        endedAt: null,
        durationMinutes: null,

LINE 885: rawSignal.processing_status,
            id: rawSignal.id,
            processingStatus:
              processedSignalResult?.signal?.processing_status ??
>             rawSignal.processing_status,
          }
        : null,
      processingRunId,
      lifecycle: {
        startedAt,
        endedAt: null,
        durationMinutes: null,
        impactsCreated: false,

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: performed_by_actor_id
LABEL: contract evidence
------------------------------------------------------------

LINE 632: performed_by_actor_id: personActor.id,
      .from("activity_events")
      .insert({
        user_id: appUser.id,
>       performed_by_actor_id: personActor.id,
        acting_as_actor_id: personActor.id,
        acting_for_actor_id: personActor.id,
        activity_type_id: template.default_activity_type_id,
        activity_template_id: template.id,
        template_id: template.legacy_activity_code_template_id,
        event_code:
          shortcut?.shortcut_type === "legacy_code"
            ? shortcut.shortcut_value

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: acting_as_actor_id
LABEL: contract evidence
------------------------------------------------------------

LINE 633: acting_as_actor_id: personActor.id,
      .insert({
        user_id: appUser.id,
        performed_by_actor_id: personActor.id,
>       acting_as_actor_id: personActor.id,
        acting_for_actor_id: personActor.id,
        activity_type_id: template.default_activity_type_id,
        activity_template_id: template.id,
        template_id: template.legacy_activity_code_template_id,
        event_code:
          shortcut?.shortcut_type === "legacy_code"
            ? shortcut.shortcut_value
            : null,

------------------------------------------------------------
FILE: src/app/api/activity/start/route.ts
PATTERN: input_text
LABEL: contract evidence
------------------------------------------------------------

LINE 642: input_text: buildInputText({
          shortcut?.shortcut_type === "legacy_code"
            ? shortcut.shortcut_value
            : null,
>       input_text: buildInputText({
          template,
          shortcut,
          comment,
        }),
        title,
        description: comment ?? template.description,
        started_at: startedAt,
        ended_at: null,

############################################################
ROUTE FILE: src/app/api/activity/record/route.ts
############################################################

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: type 
LABEL: contract evidence
------------------------------------------------------------

LINE 7: import { normalizeActivityEventSourceType } from "../../../../../lib/activity/activitySourceIntake";
    ACTIVITY_RECORDING_DISABLED_MESSAGE,
    ACTIVITY_RECORDING_ENABLED,
  } from "../../../../../lib/activity/activityRecordingConfig";
> import { normalizeActivityEventSourceType } from "../../../../../lib/activity/activitySourceIntake";
  import { normalizeActivityStatus } from "../../../../../lib/activity/activityLifecycle";
  import {
    getDurationMs,
    safeCreateActivityProcessingLog,
  } from "../../../../../lib/activity/activityProcessingLogs";
  import { getActivityUserContext } from "../../../../../lib/activity/activityUserContext";
  import { processActivityImpacts } from "../../../../../lib/activity/activityImpactProcessor";
  import { processActivityValueObjectBridge } from "../../../../../lib/activity/activityValueObjectLifecycle";

LINE 27: type ActivityRecordBody = {
  
  export const dynamic = "force-dynamic";
  
> type ActivityRecordBody = {
    templateId?: unknown;
    templateSlug?: unknown;
    shortcut?: unknown;
    naturalInput?: unknown;
    input?: unknown;
    durationMinutes?: unknown;
    comment?: unknown;
    title?: unknown;

LINE 44: type ActivityTemplateRow = {
    status?: unknown;
  };
  
> type ActivityTemplateRow = {
    id: string;
    legacy_activity_code_template_id: string | null;
    owner_user_id: string | null;
    owner_actor_id: string | null;
    organization_id: string | null;
    slug: string;
    title: string;
    short_title: string | null;

LINE 82: type ActivityTemplateLinkRow = {
    updated_at: string;
  };
  
> type ActivityTemplateLinkRow = {
    id: string;
    template_id: string;
    linked_entity_type: string;
    linked_entity_id: string | null;
    linked_entity_key: string | null;
    link_role: string;
    relation_type: string;
    default_weight: number;

LINE 98: type ActivityShortcutRow = {
    metadata_json: Record<string, unknown> | null;
  };
  
> type ActivityShortcutRow = {
    id: string;
    user_id: string | null;
    template_id: string;
    shortcut_scope: string;
    shortcut_type: string;
    shortcut_value: string;
    label: string | null;
    description: string | null;

LINE 118: type ResolvedTemplate = {
    metadata_json: Record<string, unknown> | null;
  };
  
> type ResolvedTemplate = {
    template: ActivityTemplateRow;
    resolvedBy: "templateId" | "templateSlug" | "shortcut" | "legacyInputShortcut";
    shortcut: ActivityShortcutRow | null;
  };
  
  type ResolvedTiming = {
    startedAt: string | null;
    endedAt: string | null;

LINE 124: type ResolvedTiming = {
    shortcut: ActivityShortcutRow | null;
  };
  
> type ResolvedTiming = {
    startedAt: string | null;
    endedAt: string | null;
    durationMinutes: number | null;
  };
  
  
  
  function asString(value: unknown): string | null {

LINE 514: if (sourceType === "ai_suggested") {
  }
  
  function mapTemplateLinkSourceToEventLinkSource(sourceType: string) {
>   if (sourceType === "ai_suggested") {
      return "ai";
    }
  
    if (sourceType === "imported") {
      return "import";
    }
  
    if (sourceType === "user_created" || sourceType === "organization_created") {

LINE 518: if (sourceType === "imported") {
      return "ai";
    }
  
>   if (sourceType === "imported") {
      return "import";
    }
  
    if (sourceType === "user_created" || sourceType === "organization_created") {
      return "manual";
    }
  
    if (sourceType === "system_seed" || sourceType === "legacy_migration") {

LINE 522: if (sourceType === "user_created" || sourceType === "organization_created") {
      return "import";
    }
  
>   if (sourceType === "user_created" || sourceType === "organization_created") {
      return "manual";
    }
  
    if (sourceType === "system_seed" || sourceType === "legacy_migration") {
      return "template";
    }
  
    return "template";

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: interface 
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: export async function POST
LABEL: contract evidence
------------------------------------------------------------

LINE 677: export async function POST(request: Request) {
    });
  }
  
> export async function POST(request: Request) {
    if (!ACTIVITY_RECORDING_ENABLED) {
      return NextResponse.json(
        {
          ok: false,
          error: ACTIVITY_RECORDING_DISABLED_MESSAGE,
        },
        { status: 503 }
      );

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: request.json
LABEL: contract evidence
------------------------------------------------------------

LINE 708: body = (await request.json()) as ActivityRecordBody;
    let body: ActivityRecordBody;
  
    try {
>     body = (await request.json()) as ActivityRecordBody;
    } catch {
      return NextResponse.json(
        {
          ok: false,
          error: "Invalid JSON body",
        },
        { status: 400 }
      );

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: eventId
LABEL: contract evidence
------------------------------------------------------------

LINE 550: eventId: string;
  }
  
  function buildEventLinkRows(params: {
>   eventId: string;
    template: ActivityTemplateRow;
    templateLinks: ActivityTemplateLinkRow[];
  }) {
    const { eventId, template, templateLinks } = params;
  
    const templateSelfLink = {
      event_id: eventId,
      linked_entity_type: "activity_template",

LINE 554: const { eventId, template, templateLinks } = params;
    template: ActivityTemplateRow;
    templateLinks: ActivityTemplateLinkRow[];
  }) {
>   const { eventId, template, templateLinks } = params;
  
    const templateSelfLink = {
      event_id: eventId,
      linked_entity_type: "activity_template",
      linked_entity_id: template.id,
      linked_entity_key: template.slug,
      link_role: "capture_template",
      relation_type: "created_from",

LINE 557: event_id: eventId,
    const { eventId, template, templateLinks } = params;
  
    const templateSelfLink = {
>     event_id: eventId,
      linked_entity_type: "activity_template",
      linked_entity_id: template.id,
      linked_entity_key: template.slug,
      link_role: "capture_template",
      relation_type: "created_from",
      weight: 1,
      confidence: 1,
      source: "template",

LINE 573: event_id: eventId,
    };
  
    const semanticLinks = templateLinks.map((link) => ({
>     event_id: eventId,
      linked_entity_type: link.linked_entity_type,
      linked_entity_id: link.linked_entity_id,
      linked_entity_key: link.linked_entity_key,
      link_role: link.link_role,
      relation_type: link.relation_type,
      weight: link.default_weight,
      confidence: link.default_confidence,
      source: mapTemplateLinkSourceToEventLinkSource(link.source_type),

LINE 1019: activityEventId: createdEvent.id,
    await safeCreateActivityProcessingLog({
      userId: appUser.id,
      rawSignalId: rawSignal?.id ?? null,
>     activityEventId: createdEvent.id,
      processingRunId,
      processorName: "activity_record_route",
      processingStage: "create_event",
      processingStatus: "completed",
      severity: "info",
      message: "Activity event created from template-first record flow.",
      input: {
        templateId: template.id,

LINE 1032: activityEventId: createdEvent.id,
        title,
      },
      output: {
>       activityEventId: createdEvent.id,
        status: createdEvent.status,
        processingStatus: createdEvent.processing_status,
      },
      metadata: {
        endpoint: "/api/activity/record",
        mode: "template_first_completed",
      },
      startedAt: processingStartedAt.toISOString(),

LINE 1046: eventId: createdEvent.id,
    });
  
    const eventLinkRows = buildEventLinkRows({
>     eventId: createdEvent.id,
      template,
      templateLinks,
    });
  
    const { data: eventLinks, error: eventLinksError } =
      eventLinkRows.length > 0
        ? await supabase.from("event_links").insert(eventLinkRows).select()
        : { data: [], error: null };

LINE 1068: activityEventId: createdEvent.id,
      await safeCreateActivityProcessingLog({
        userId: appUser.id,
        rawSignalId: rawSignal?.id ?? null,
>       activityEventId: createdEvent.id,
        processingRunId,
        processorName: "activity_record_route",
        processingStage: "link_event",
        processingStatus: "failed",
        severity: "error",
        message: "Failed to create activity event links.",
        input: {
          eventLinkRowsCount: eventLinkRows.length,

LINE 1109: activityEventId: createdEvent.id,
    await safeCreateActivityProcessingLog({
      userId: appUser.id,
      rawSignalId: rawSignal?.id ?? null,
>     activityEventId: createdEvent.id,
      processingRunId,
      processorName: "activity_record_route",
      processingStage: "link_event",
      processingStatus: "completed",
      severity: "info",
      message: "Activity event links created.",
      input: {
        eventLinkRowsCount: eventLinkRows.length,

LINE 1133: eventId: createdEvent.id,
  
    try {
      const impactResult = await processActivityImpacts({
>       eventId: createdEvent.id,
        userId: appUser.id,
        activityTemplateId: createdEvent.activity_template_id,
        activityTypeId: createdEvent.activity_type_id,
        durationMinutes: createdEvent.duration_minutes,
        startedAt: createdEvent.started_at,
      });
  
      const processedSignalResult = rawSignal

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: inputText
LABEL: contract evidence
------------------------------------------------------------

LINE 183: function buildInputText(params: {
    return firstToken ?? null;
  }
  
> function buildInputText(params: {
    template: ActivityTemplateRow | null;
    naturalInput: string | null;
    input: string | null;
    shortcut: ActivityShortcutRow | null;
    durationMinutes: number | null;
    comment: string | null;
  }) {
    const {

LINE 842: const inputText = buildInputText({
  
    const title = asString(body.title) ?? template.title;
  
>   const inputText = buildInputText({
      template,
      naturalInput,
      input: legacyInput,
      shortcut,
      durationMinutes: timing.durationMinutes,
      comment,
    });
  

LINE 945: input_text: inputText,
        template_id: template.legacy_activity_code_template_id,
        event_code:
          shortcut?.shortcut_type === "legacy_code" ? shortcut.shortcut_value : null,
>       input_text: inputText,
        title,
        description: comment ?? template.description,
        started_at: timing.startedAt,
        ended_at: timing.endedAt,
        duration_minutes: timing.durationMinutes,
        source,
        status,
        privacy_scope: template.default_privacy_scope,

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: naturalInput
LABEL: contract evidence
------------------------------------------------------------

LINE 31: naturalInput?: unknown;
    templateId?: unknown;
    templateSlug?: unknown;
    shortcut?: unknown;
>   naturalInput?: unknown;
    input?: unknown;
    durationMinutes?: unknown;
    comment?: unknown;
    title?: unknown;
    startedAt?: unknown;
    endedAt?: unknown;
    startTime?: unknown;
    endTime?: unknown;

LINE 185: naturalInput: string | null;
  
  function buildInputText(params: {
    template: ActivityTemplateRow | null;
>   naturalInput: string | null;
    input: string | null;
    shortcut: ActivityShortcutRow | null;
    durationMinutes: number | null;
    comment: string | null;
  }) {
    const {
      template,
      naturalInput,

LINE 193: naturalInput,
  }) {
    const {
      template,
>     naturalInput,
      input,
      shortcut,
      durationMinutes,
      comment,
    } = params;
  
    if (naturalInput) {
      return naturalInput;

LINE 200: if (naturalInput) {
      comment,
    } = params;
  
>   if (naturalInput) {
      return naturalInput;
    }
  
    if (input) {
      return input;
    }
  
    if (template) {

LINE 201: return naturalInput;
    } = params;
  
    if (naturalInput) {
>     return naturalInput;
    }
  
    if (input) {
      return input;
    }
  
    if (template) {
      return [

LINE 593: async function createNaturalInputDraft(params: {
    return [templateSelfLink, ...semanticLinks];
  }
  
> async function createNaturalInputDraft(params: {
    appUserId: string;
    actorId: string;
    naturalInput: string;
    durationMinutes: number | null;
    body: ActivityRecordBody;
  }) {
    const { appUserId, actorId, naturalInput, durationMinutes, body } = params;
  

LINE 596: naturalInput: string;
  async function createNaturalInputDraft(params: {
    appUserId: string;
    actorId: string;
>   naturalInput: string;
    durationMinutes: number | null;
    body: ActivityRecordBody;
  }) {
    const { appUserId, actorId, naturalInput, durationMinutes, body } = params;
  
    const timing = resolveTiming({
      body,
      durationMinutes,

LINE 600: const { appUserId, actorId, naturalInput, durationMinutes, body } = params;
    durationMinutes: number | null;
    body: ActivityRecordBody;
  }) {
>   const { appUserId, actorId, naturalInput, durationMinutes, body } = params;
  
    const timing = resolveTiming({
      body,
      durationMinutes,
      requireDuration: false,
    });
  
    if ("error" in timing) {

LINE 623: input_text: naturalInput,
        activity_template_id: null,
        template_id: null,
        event_code: null,
>       input_text: naturalInput,
        title: asString(body.title) ?? "Unclassified activity draft",
        description: asString(body.comment),
        started_at: timing.startedAt,
        ended_at: timing.endedAt,
        duration_minutes: timing.durationMinutes,
        source: normalizeSourceType(body.sourceType, "manual_chat"),
        status: "draft",
        privacy_scope: "private",

LINE 669: naturalInput:
        templateId: "uuid",
        shortcut: "DE writing",
        legacyShortcut: "11-341",
>       naturalInput:
          "I studied German for 25 minutes and wrote a commercial letter by hand.",
      },
      note:
        "Legacy numeric codes are supported only as optional shortcuts, not as the primary UX model.",
    });
  }
  
  export async function POST(request: Request) {

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: title
LABEL: contract evidence
------------------------------------------------------------

LINE 35: title?: unknown;
    input?: unknown;
    durationMinutes?: unknown;
    comment?: unknown;
>   title?: unknown;
    startedAt?: unknown;
    endedAt?: unknown;
    startTime?: unknown;
    endTime?: unknown;
    sourceType?: unknown;
    status?: unknown;
  };
  

LINE 51: title: string;
    owner_actor_id: string | null;
    organization_id: string | null;
    slug: string;
>   title: string;
    short_title: string | null;
    description: string | null;
    template_group: string;
    template_scope: string;
    visibility: string;
    source_type: string;
    status: string;
    default_activity_type_id: string | null;

LINE 52: short_title: string | null;
    organization_id: string | null;
    slug: string;
    title: string;
>   short_title: string | null;
    description: string | null;
    template_group: string;
    template_scope: string;
    visibility: string;
    source_type: string;
    status: string;
    default_activity_type_id: string | null;
    default_duration_minutes: number | null;

LINE 210: template.title,
  
    if (template) {
      return [
>       template.title,
        durationMinutes !== null ? `${durationMinutes} min` : null,
        comment,
        shortcut ? `shortcut:${shortcut.shortcut_value}` : null,
      ]
        .filter(Boolean)
        .join(" | ");
    }
  

LINE 567: template_title: template.title,
      confidence: 1,
      source: "template",
      metadata_json: {
>       template_title: template.title,
        template_scope: template.template_scope,
      },
    };
  
    const semanticLinks = templateLinks.map((link) => ({
      event_id: eventId,
      linked_entity_type: link.linked_entity_type,
      linked_entity_id: link.linked_entity_id,

LINE 624: title: asString(body.title) ?? "Unclassified activity draft",
        template_id: null,
        event_code: null,
        input_text: naturalInput,
>       title: asString(body.title) ?? "Unclassified activity draft",
        description: asString(body.comment),
        started_at: timing.startedAt,
        ended_at: timing.endedAt,
        duration_minutes: timing.durationMinutes,
        source: normalizeSourceType(body.sourceType, "manual_chat"),
        status: "draft",
        privacy_scope: "private",
        processing_status: "pending",

LINE 840: const title = asString(body.title) ?? template.title;
  
    const status = normalizeRecordStatus(body.status, template.default_status);
  
>   const title = asString(body.title) ?? template.title;
  
    const inputText = buildInputText({
      template,
      naturalInput,
      input: legacyInput,
      shortcut,
      durationMinutes: timing.durationMinutes,
      comment,

LINE 867: title,
      normalizedPreview: {
        templateId: template.id,
        templateSlug: template.slug,
>       title,
        source,
        status,
        startedAt: timing.startedAt,
        endedAt: timing.endedAt,
        durationMinutes: timing.durationMinutes,
      },
      occurredAt: timing.startedAt,
      trustLevel: "medium",

LINE 946: title,
        event_code:
          shortcut?.shortcut_type === "legacy_code" ? shortcut.shortcut_value : null,
        input_text: inputText,
>       title,
        description: comment ?? template.description,
        started_at: timing.startedAt,
        ended_at: timing.endedAt,
        duration_minutes: timing.durationMinutes,
        source,
        status,
        privacy_scope: template.default_privacy_scope,
        processing_status: "processed",

LINE 993: title,
        input: {
          templateId: template.id,
          templateSlug: template.slug,
>         title,
        },
        error: {
          message: eventError?.message ?? "Failed to create activity event",
        },
        metadata: {
          endpoint: "/api/activity/record",
          mode: "template_first_completed",
        },

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: description
LABEL: contract evidence
------------------------------------------------------------

LINE 53: description: string | null;
    slug: string;
    title: string;
    short_title: string | null;
>   description: string | null;
    template_group: string;
    template_scope: string;
    visibility: string;
    source_type: string;
    status: string;
    default_activity_type_id: string | null;
    default_duration_minutes: number | null;
    quick_duration_minutes: number[] | null;

LINE 106: description: string | null;
    shortcut_type: string;
    shortcut_value: string;
    label: string | null;
>   description: string | null;
    is_favorite: boolean;
    show_in_default_ui: boolean;
    is_deprecated_alias: boolean;
    nfc_tag_id: string | null;
    voice_phrase: string | null;
    device_binding: string | null;
    sort_order: number;
    is_active: boolean;

LINE 625: description: asString(body.comment),
        event_code: null,
        input_text: naturalInput,
        title: asString(body.title) ?? "Unclassified activity draft",
>       description: asString(body.comment),
        started_at: timing.startedAt,
        ended_at: timing.endedAt,
        duration_minutes: timing.durationMinutes,
        source: normalizeSourceType(body.sourceType, "manual_chat"),
        status: "draft",
        privacy_scope: "private",
        processing_status: "pending",
        metadata_json: {

LINE 947: description: comment ?? template.description,
          shortcut?.shortcut_type === "legacy_code" ? shortcut.shortcut_value : null,
        input_text: inputText,
        title,
>       description: comment ?? template.description,
        started_at: timing.startedAt,
        ended_at: timing.endedAt,
        duration_minutes: timing.durationMinutes,
        source,
        status,
        privacy_scope: template.default_privacy_scope,
        processing_status: "processed",
        metadata_json: {

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: durationMinutes
LABEL: contract evidence
------------------------------------------------------------

LINE 33: durationMinutes?: unknown;
    shortcut?: unknown;
    naturalInput?: unknown;
    input?: unknown;
>   durationMinutes?: unknown;
    comment?: unknown;
    title?: unknown;
    startedAt?: unknown;
    endedAt?: unknown;
    startTime?: unknown;
    endTime?: unknown;
    sourceType?: unknown;
    status?: unknown;

LINE 127: durationMinutes: number | null;
  type ResolvedTiming = {
    startedAt: string | null;
    endedAt: string | null;
>   durationMinutes: number | null;
  };
  
  
  
  function asString(value: unknown): string | null {
    if (typeof value !== "string") {
      return null;
    }

LINE 188: durationMinutes: number | null;
    naturalInput: string | null;
    input: string | null;
    shortcut: ActivityShortcutRow | null;
>   durationMinutes: number | null;
    comment: string | null;
  }) {
    const {
      template,
      naturalInput,
      input,
      shortcut,
      durationMinutes,

LINE 196: durationMinutes,
      naturalInput,
      input,
      shortcut,
>     durationMinutes,
      comment,
    } = params;
  
    if (naturalInput) {
      return naturalInput;
    }
  
    if (input) {

LINE 211: durationMinutes !== null ? `${durationMinutes} min` : null,
    if (template) {
      return [
        template.title,
>       durationMinutes !== null ? `${durationMinutes} min` : null,
        comment,
        shortcut ? `shortcut:${shortcut.shortcut_value}` : null,
      ]
        .filter(Boolean)
        .join(" | ");
    }
  
    return [durationMinutes !== null ? `${durationMinutes} min` : null, comment]

LINE 219: return [durationMinutes !== null ? `${durationMinutes} min` : null, comment]
        .join(" | ");
    }
  
>   return [durationMinutes !== null ? `${durationMinutes} min` : null, comment]
      .filter(Boolean)
      .join(" | ");
  }
  
  function resolveTiming(params: {
    body: ActivityRecordBody;
    durationMinutes: number | null;
    requireDuration: boolean;

LINE 226: durationMinutes: number | null;
  
  function resolveTiming(params: {
    body: ActivityRecordBody;
>   durationMinutes: number | null;
    requireDuration: boolean;
  }): ResolvedTiming | { error: string } {
    const { body, durationMinutes, requireDuration } = params;
  
    const rawStartedAt = asString(body.startedAt) ?? asString(body.startTime);
    const rawEndedAt = asString(body.endedAt) ?? asString(body.endTime);
  
    if (durationMinutes !== null && durationMinutes < 0) {

LINE 229: const { body, durationMinutes, requireDuration } = params;
    durationMinutes: number | null;
    requireDuration: boolean;
  }): ResolvedTiming | { error: string } {
>   const { body, durationMinutes, requireDuration } = params;
  
    const rawStartedAt = asString(body.startedAt) ?? asString(body.startTime);
    const rawEndedAt = asString(body.endedAt) ?? asString(body.endTime);
  
    if (durationMinutes !== null && durationMinutes < 0) {
      return { error: "durationMinutes must be greater than or equal to 0" };
    }
  

LINE 234: if (durationMinutes !== null && durationMinutes < 0) {
    const rawStartedAt = asString(body.startedAt) ?? asString(body.startTime);
    const rawEndedAt = asString(body.endedAt) ?? asString(body.endTime);
  
>   if (durationMinutes !== null && durationMinutes < 0) {
      return { error: "durationMinutes must be greater than or equal to 0" };
    }
  
    if (rawStartedAt && rawEndedAt) {
      const startedDate = new Date(rawStartedAt);
      const endedDate = new Date(rawEndedAt);
  
      if (

LINE 235: return { error: "durationMinutes must be greater than or equal to 0" };
    const rawEndedAt = asString(body.endedAt) ?? asString(body.endTime);
  
    if (durationMinutes !== null && durationMinutes < 0) {
>     return { error: "durationMinutes must be greater than or equal to 0" };
    }
  
    if (rawStartedAt && rawEndedAt) {
      const startedDate = new Date(rawStartedAt);
      const endedDate = new Date(rawEndedAt);
  
      if (
        Number.isNaN(startedDate.getTime()) ||

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: startedAt
LABEL: contract evidence
------------------------------------------------------------

LINE 36: startedAt?: unknown;
    durationMinutes?: unknown;
    comment?: unknown;
    title?: unknown;
>   startedAt?: unknown;
    endedAt?: unknown;
    startTime?: unknown;
    endTime?: unknown;
    sourceType?: unknown;
    status?: unknown;
  };
  
  type ActivityTemplateRow = {

LINE 125: startedAt: string | null;
  };
  
  type ResolvedTiming = {
>   startedAt: string | null;
    endedAt: string | null;
    durationMinutes: number | null;
  };
  
  
  
  function asString(value: unknown): string | null {
    if (typeof value !== "string") {

LINE 231: const rawStartedAt = asString(body.startedAt) ?? asString(body.startTime);
  }): ResolvedTiming | { error: string } {
    const { body, durationMinutes, requireDuration } = params;
  
>   const rawStartedAt = asString(body.startedAt) ?? asString(body.startTime);
    const rawEndedAt = asString(body.endedAt) ?? asString(body.endTime);
  
    if (durationMinutes !== null && durationMinutes < 0) {
      return { error: "durationMinutes must be greater than or equal to 0" };
    }
  
    if (rawStartedAt && rawEndedAt) {
      const startedDate = new Date(rawStartedAt);

LINE 238: if (rawStartedAt && rawEndedAt) {
      return { error: "durationMinutes must be greater than or equal to 0" };
    }
  
>   if (rawStartedAt && rawEndedAt) {
      const startedDate = new Date(rawStartedAt);
      const endedDate = new Date(rawEndedAt);
  
      if (
        Number.isNaN(startedDate.getTime()) ||
        Number.isNaN(endedDate.getTime())
      ) {
        return { error: "Invalid startedAt or endedAt" };

LINE 239: const startedDate = new Date(rawStartedAt);
    }
  
    if (rawStartedAt && rawEndedAt) {
>     const startedDate = new Date(rawStartedAt);
      const endedDate = new Date(rawEndedAt);
  
      if (
        Number.isNaN(startedDate.getTime()) ||
        Number.isNaN(endedDate.getTime())
      ) {
        return { error: "Invalid startedAt or endedAt" };
      }

LINE 246: return { error: "Invalid startedAt or endedAt" };
        Number.isNaN(startedDate.getTime()) ||
        Number.isNaN(endedDate.getTime())
      ) {
>       return { error: "Invalid startedAt or endedAt" };
      }
  
      if (endedDate.getTime() < startedDate.getTime()) {
        return { error: "endedAt must be greater than or equal to startedAt" };
      }
  
      const calculatedDurationMinutes = Math.round(
        (endedDate.getTime() - startedDate.getTime()) / 60000

LINE 250: return { error: "endedAt must be greater than or equal to startedAt" };
      }
  
      if (endedDate.getTime() < startedDate.getTime()) {
>       return { error: "endedAt must be greater than or equal to startedAt" };
      }
  
      const calculatedDurationMinutes = Math.round(
        (endedDate.getTime() - startedDate.getTime()) / 60000
      );
  
      return {
        startedAt: startedDate.toISOString(),

LINE 258: startedAt: startedDate.toISOString(),
      );
  
      return {
>       startedAt: startedDate.toISOString(),
        endedAt: endedDate.toISOString(),
        durationMinutes: calculatedDurationMinutes,
      };
    }
  
    if (rawStartedAt) {
      const startedDate = new Date(rawStartedAt);
  

LINE 264: if (rawStartedAt) {
      };
    }
  
>   if (rawStartedAt) {
      const startedDate = new Date(rawStartedAt);
  
      if (Number.isNaN(startedDate.getTime())) {
        return { error: "Invalid startedAt" };
      }
  
      if (durationMinutes === null) {
        if (requireDuration) {

LINE 265: const startedDate = new Date(rawStartedAt);
    }
  
    if (rawStartedAt) {
>     const startedDate = new Date(rawStartedAt);
  
      if (Number.isNaN(startedDate.getTime())) {
        return { error: "Invalid startedAt" };
      }
  
      if (durationMinutes === null) {
        if (requireDuration) {
          return { error: "durationMinutes is required for this activity" };

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: endedAt
LABEL: contract evidence
------------------------------------------------------------

LINE 37: endedAt?: unknown;
    comment?: unknown;
    title?: unknown;
    startedAt?: unknown;
>   endedAt?: unknown;
    startTime?: unknown;
    endTime?: unknown;
    sourceType?: unknown;
    status?: unknown;
  };
  
  type ActivityTemplateRow = {
    id: string;

LINE 126: endedAt: string | null;
  
  type ResolvedTiming = {
    startedAt: string | null;
>   endedAt: string | null;
    durationMinutes: number | null;
  };
  
  
  
  function asString(value: unknown): string | null {
    if (typeof value !== "string") {
      return null;

LINE 232: const rawEndedAt = asString(body.endedAt) ?? asString(body.endTime);
    const { body, durationMinutes, requireDuration } = params;
  
    const rawStartedAt = asString(body.startedAt) ?? asString(body.startTime);
>   const rawEndedAt = asString(body.endedAt) ?? asString(body.endTime);
  
    if (durationMinutes !== null && durationMinutes < 0) {
      return { error: "durationMinutes must be greater than or equal to 0" };
    }
  
    if (rawStartedAt && rawEndedAt) {
      const startedDate = new Date(rawStartedAt);
      const endedDate = new Date(rawEndedAt);

LINE 238: if (rawStartedAt && rawEndedAt) {
      return { error: "durationMinutes must be greater than or equal to 0" };
    }
  
>   if (rawStartedAt && rawEndedAt) {
      const startedDate = new Date(rawStartedAt);
      const endedDate = new Date(rawEndedAt);
  
      if (
        Number.isNaN(startedDate.getTime()) ||
        Number.isNaN(endedDate.getTime())
      ) {
        return { error: "Invalid startedAt or endedAt" };

LINE 240: const endedDate = new Date(rawEndedAt);
  
    if (rawStartedAt && rawEndedAt) {
      const startedDate = new Date(rawStartedAt);
>     const endedDate = new Date(rawEndedAt);
  
      if (
        Number.isNaN(startedDate.getTime()) ||
        Number.isNaN(endedDate.getTime())
      ) {
        return { error: "Invalid startedAt or endedAt" };
      }
  

LINE 246: return { error: "Invalid startedAt or endedAt" };
        Number.isNaN(startedDate.getTime()) ||
        Number.isNaN(endedDate.getTime())
      ) {
>       return { error: "Invalid startedAt or endedAt" };
      }
  
      if (endedDate.getTime() < startedDate.getTime()) {
        return { error: "endedAt must be greater than or equal to startedAt" };
      }
  
      const calculatedDurationMinutes = Math.round(
        (endedDate.getTime() - startedDate.getTime()) / 60000

LINE 250: return { error: "endedAt must be greater than or equal to startedAt" };
      }
  
      if (endedDate.getTime() < startedDate.getTime()) {
>       return { error: "endedAt must be greater than or equal to startedAt" };
      }
  
      const calculatedDurationMinutes = Math.round(
        (endedDate.getTime() - startedDate.getTime()) / 60000
      );
  
      return {
        startedAt: startedDate.toISOString(),

LINE 259: endedAt: endedDate.toISOString(),
  
      return {
        startedAt: startedDate.toISOString(),
>       endedAt: endedDate.toISOString(),
        durationMinutes: calculatedDurationMinutes,
      };
    }
  
    if (rawStartedAt) {
      const startedDate = new Date(rawStartedAt);
  
      if (Number.isNaN(startedDate.getTime())) {

LINE 278: endedAt: null,
  
        return {
          startedAt: startedDate.toISOString(),
>         endedAt: null,
          durationMinutes: null,
        };
      }
  
      const endedDate = new Date(startedDate.getTime() + durationMinutes * 60000);
  
      return {
        startedAt: startedDate.toISOString(),

LINE 287: endedAt: endedDate.toISOString(),
  
      return {
        startedAt: startedDate.toISOString(),
>       endedAt: endedDate.toISOString(),
        durationMinutes,
      };
    }
  
    if (rawEndedAt) {
      const endedDate = new Date(rawEndedAt);
  
      if (Number.isNaN(endedDate.getTime())) {

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: status
LABEL: contract evidence
------------------------------------------------------------

LINE 8: import { normalizeActivityStatus } from "../../../../../lib/activity/activityLifecycle";
    ACTIVITY_RECORDING_ENABLED,
  } from "../../../../../lib/activity/activityRecordingConfig";
  import { normalizeActivityEventSourceType } from "../../../../../lib/activity/activitySourceIntake";
> import { normalizeActivityStatus } from "../../../../../lib/activity/activityLifecycle";
  import {
    getDurationMs,
    safeCreateActivityProcessingLog,
  } from "../../../../../lib/activity/activityProcessingLogs";
  import { getActivityUserContext } from "../../../../../lib/activity/activityUserContext";
  import { processActivityImpacts } from "../../../../../lib/activity/activityImpactProcessor";
  import { processActivityValueObjectBridge } from "../../../../../lib/activity/activityValueObjectLifecycle";
  import { ensureActivityEventRubricatorClassificationForKnownTemplate } from "../../../../../lib/activity/activityRubricatorClassificationLifecycle";

LINE 41: status?: unknown;
    startTime?: unknown;
    endTime?: unknown;
    sourceType?: unknown;
>   status?: unknown;
  };
  
  type ActivityTemplateRow = {
    id: string;
    legacy_activity_code_template_id: string | null;
    owner_user_id: string | null;
    owner_actor_id: string | null;
    organization_id: string | null;

LINE 58: status: string;
    template_scope: string;
    visibility: string;
    source_type: string;
>   status: string;
    default_activity_type_id: string | null;
    default_duration_minutes: number | null;
    quick_duration_minutes: number[] | null;
    default_status: string;
    default_source_type: string;
    default_privacy_scope: string;
    icon_key: string | null;
    color_key: string | null;

LINE 62: default_status: string;
    default_activity_type_id: string | null;
    default_duration_minutes: number | null;
    quick_duration_minutes: number[] | null;
>   default_status: string;
    default_source_type: string;
    default_privacy_scope: string;
    icon_key: string | null;
    color_key: string | null;
    show_in_quick_capture: boolean;
    show_in_onboarding: boolean;
    allow_manual_duration: boolean;
    allow_comment: boolean;

LINE 166: function normalizeRecordStatus(value: unknown, fallback: string) {
    );
  }
  
> function normalizeRecordStatus(value: unknown, fallback: string) {
    return normalizeActivityStatus(
      value,
      normalizeActivityStatus(fallback, "completed")
    );
  }
  
  function inferLegacyShortcutFromInput(input: string | null) {
    if (!input) {

LINE 167: return normalizeActivityStatus(
  }
  
  function normalizeRecordStatus(value: unknown, fallback: string) {
>   return normalizeActivityStatus(
      value,
      normalizeActivityStatus(fallback, "completed")
    );
  }
  
  function inferLegacyShortcutFromInput(input: string | null) {
    if (!input) {
      return null;

LINE 169: normalizeActivityStatus(fallback, "completed")
  function normalizeRecordStatus(value: unknown, fallback: string) {
    return normalizeActivityStatus(
      value,
>     normalizeActivityStatus(fallback, "completed")
    );
  }
  
  function inferLegacyShortcutFromInput(input: string | null) {
    if (!input) {
      return null;
    }
  

LINE 630: status: "draft",
        ended_at: timing.endedAt,
        duration_minutes: timing.durationMinutes,
        source: normalizeSourceType(body.sourceType, "manual_chat"),
>       status: "draft",
        privacy_scope: "private",
        processing_status: "pending",
        metadata_json: {
          parser: "template_first_v2",
          natural_input_draft: true,
          ai_used: false,
          needs_classification: true,
        },

LINE 632: processing_status: "pending",
        source: normalizeSourceType(body.sourceType, "manual_chat"),
        status: "draft",
        privacy_scope: "private",
>       processing_status: "pending",
        metadata_json: {
          parser: "template_first_v2",
          natural_input_draft: true,
          ai_used: false,
          needs_classification: true,
        },
      })
      .select()

LINE 656: status: ACTIVITY_RECORDING_ENABLED ? "ready" : "disabled",
      endpoint: "/api/activity/record",
      method: "POST",
      enabled: ACTIVITY_RECORDING_ENABLED,
>     status: ACTIVITY_RECORDING_ENABLED ? "ready" : "disabled",
      message: ACTIVITY_RECORDING_ENABLED
        ? "Activity recording endpoint is template-first."
        : ACTIVITY_RECORDING_DISABLED_MESSAGE,
      primaryInputExample: {
        templateSlug: "german-marketing-handwriting-practice",
        durationMinutes: 25,
        comment: "commercial letter handwriting practice",
      },

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: started
LABEL: contract evidence
------------------------------------------------------------

LINE 36: startedAt?: unknown;
    durationMinutes?: unknown;
    comment?: unknown;
    title?: unknown;
>   startedAt?: unknown;
    endedAt?: unknown;
    startTime?: unknown;
    endTime?: unknown;
    sourceType?: unknown;
    status?: unknown;
  };
  
  type ActivityTemplateRow = {

LINE 71: allow_started_at_override: boolean;
    show_in_onboarding: boolean;
    allow_manual_duration: boolean;
    allow_comment: boolean;
>   allow_started_at_override: boolean;
    allow_ended_at_override: boolean;
    input_schema_json: Record<string, unknown> | null;
    ui_schema_json: Record<string, unknown> | null;
    default_metadata_json: Record<string, unknown> | null;
    sort_order: number;
    is_active: boolean;
    created_at: string;
    updated_at: string;

LINE 125: startedAt: string | null;
  };
  
  type ResolvedTiming = {
>   startedAt: string | null;
    endedAt: string | null;
    durationMinutes: number | null;
  };
  
  
  
  function asString(value: unknown): string | null {
    if (typeof value !== "string") {

LINE 231: const rawStartedAt = asString(body.startedAt) ?? asString(body.startTime);
  }): ResolvedTiming | { error: string } {
    const { body, durationMinutes, requireDuration } = params;
  
>   const rawStartedAt = asString(body.startedAt) ?? asString(body.startTime);
    const rawEndedAt = asString(body.endedAt) ?? asString(body.endTime);
  
    if (durationMinutes !== null && durationMinutes < 0) {
      return { error: "durationMinutes must be greater than or equal to 0" };
    }
  
    if (rawStartedAt && rawEndedAt) {
      const startedDate = new Date(rawStartedAt);

LINE 238: if (rawStartedAt && rawEndedAt) {
      return { error: "durationMinutes must be greater than or equal to 0" };
    }
  
>   if (rawStartedAt && rawEndedAt) {
      const startedDate = new Date(rawStartedAt);
      const endedDate = new Date(rawEndedAt);
  
      if (
        Number.isNaN(startedDate.getTime()) ||
        Number.isNaN(endedDate.getTime())
      ) {
        return { error: "Invalid startedAt or endedAt" };

LINE 239: const startedDate = new Date(rawStartedAt);
    }
  
    if (rawStartedAt && rawEndedAt) {
>     const startedDate = new Date(rawStartedAt);
      const endedDate = new Date(rawEndedAt);
  
      if (
        Number.isNaN(startedDate.getTime()) ||
        Number.isNaN(endedDate.getTime())
      ) {
        return { error: "Invalid startedAt or endedAt" };
      }

LINE 243: Number.isNaN(startedDate.getTime()) ||
      const endedDate = new Date(rawEndedAt);
  
      if (
>       Number.isNaN(startedDate.getTime()) ||
        Number.isNaN(endedDate.getTime())
      ) {
        return { error: "Invalid startedAt or endedAt" };
      }
  
      if (endedDate.getTime() < startedDate.getTime()) {
        return { error: "endedAt must be greater than or equal to startedAt" };
      }

LINE 246: return { error: "Invalid startedAt or endedAt" };
        Number.isNaN(startedDate.getTime()) ||
        Number.isNaN(endedDate.getTime())
      ) {
>       return { error: "Invalid startedAt or endedAt" };
      }
  
      if (endedDate.getTime() < startedDate.getTime()) {
        return { error: "endedAt must be greater than or equal to startedAt" };
      }
  
      const calculatedDurationMinutes = Math.round(
        (endedDate.getTime() - startedDate.getTime()) / 60000

LINE 249: if (endedDate.getTime() < startedDate.getTime()) {
        return { error: "Invalid startedAt or endedAt" };
      }
  
>     if (endedDate.getTime() < startedDate.getTime()) {
        return { error: "endedAt must be greater than or equal to startedAt" };
      }
  
      const calculatedDurationMinutes = Math.round(
        (endedDate.getTime() - startedDate.getTime()) / 60000
      );
  
      return {

LINE 250: return { error: "endedAt must be greater than or equal to startedAt" };
      }
  
      if (endedDate.getTime() < startedDate.getTime()) {
>       return { error: "endedAt must be greater than or equal to startedAt" };
      }
  
      const calculatedDurationMinutes = Math.round(
        (endedDate.getTime() - startedDate.getTime()) / 60000
      );
  
      return {
        startedAt: startedDate.toISOString(),

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: planned
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: pending
LABEL: contract evidence
------------------------------------------------------------

LINE 632: processing_status: "pending",
        source: normalizeSourceType(body.sourceType, "manual_chat"),
        status: "draft",
        privacy_scope: "private",
>       processing_status: "pending",
        metadata_json: {
          parser: "template_first_v2",
          natural_input_draft: true,
          ai_used: false,
          needs_classification: true,
        },
      })
      .select()

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: completed
LABEL: contract evidence
------------------------------------------------------------

LINE 169: normalizeActivityStatus(fallback, "completed")
  function normalizeRecordStatus(value: unknown, fallback: string) {
    return normalizeActivityStatus(
      value,
>     normalizeActivityStatus(fallback, "completed")
    );
  }
  
  function inferLegacyShortcutFromInput(input: string | null) {
    if (!input) {
      return null;
    }
  

LINE 886: mode: "template_first_completed",
      metadata: {
        parser: "template_first_v2",
        processingRunId,
>       mode: "template_first_completed",
        templateScope: template.template_scope,
        templateGroup: template.template_group,
        shortcutId: shortcut?.id ?? null,
        shortcutType: shortcut?.shortcut_type ?? null,
        shortcutValue: shortcut?.shortcut_value ?? null,
      },
    });
  

LINE 903: processingStatus: rawSignalResult.ok ? "completed" : "warning",
      processingRunId,
      processorName: "activity_record_route",
      processingStage: "ingest",
>     processingStatus: rawSignalResult.ok ? "completed" : "warning",
      severity: rawSignalResult.ok ? "info" : "warning",
      message: rawSignalResult.ok
        ? "Raw activity signal captured."
        : "Raw activity signal creation failed; continuing without raw signal.",
      input: {
        templateId: template.id,
        templateSlug: template.slug,
        source,

LINE 926: mode: "template_first_completed",
          },
      metadata: {
        endpoint: "/api/activity/record",
>       mode: "template_first_completed",
      },
      startedAt: processingStartedAt.toISOString(),
      finishedAt: new Date().toISOString(),
      durationMs: getDurationMs(processingStartedAt),
    });
  
    const { data: createdEvent, error: eventError } = await supabase
      .from("activity_events")

LINE 1000: mode: "template_first_completed",
        },
        metadata: {
          endpoint: "/api/activity/record",
>         mode: "template_first_completed",
        },
        startedAt: processingStartedAt.toISOString(),
        finishedAt: new Date().toISOString(),
        durationMs: getDurationMs(processingStartedAt),
      });
  
      return NextResponse.json(
        {

LINE 1023: processingStatus: "completed",
      processingRunId,
      processorName: "activity_record_route",
      processingStage: "create_event",
>     processingStatus: "completed",
      severity: "info",
      message: "Activity event created from template-first record flow.",
      input: {
        templateId: template.id,
        templateSlug: template.slug,
        title,
      },
      output: {

LINE 1038: mode: "template_first_completed",
      },
      metadata: {
        endpoint: "/api/activity/record",
>       mode: "template_first_completed",
      },
      startedAt: processingStartedAt.toISOString(),
      finishedAt: new Date().toISOString(),
      durationMs: getDurationMs(processingStartedAt),
    });
  
    const eventLinkRows = buildEventLinkRows({
      eventId: createdEvent.id,

LINE 1083: mode: "template_first_completed",
        },
        metadata: {
          endpoint: "/api/activity/record",
>         mode: "template_first_completed",
        },
        startedAt: processingStartedAt.toISOString(),
        finishedAt: new Date().toISOString(),
        durationMs: getDurationMs(processingStartedAt),
      });
  
      await supabase
        .from("activity_events")

LINE 1113: processingStatus: "completed",
      processingRunId,
      processorName: "activity_record_route",
      processingStage: "link_event",
>     processingStatus: "completed",
      severity: "info",
      message: "Activity event links created.",
      input: {
        eventLinkRowsCount: eventLinkRows.length,
      },
      output: {
        eventLinksCount: Array.isArray(eventLinks) ? eventLinks.length : 0,
      },

LINE 1124: mode: "template_first_completed",
      },
      metadata: {
        endpoint: "/api/activity/record",
>       mode: "template_first_completed",
      },
      startedAt: processingStartedAt.toISOString(),
      finishedAt: new Date().toISOString(),
      durationMs: getDurationMs(processingStartedAt),
    });
  
    try {
      const impactResult = await processActivityImpacts({

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: .from("activity_events")
LABEL: contract evidence
------------------------------------------------------------

LINE 613: .from("activity_events")
    }
  
    const { data: createdEvent, error: eventError } = await supabase
>     .from("activity_events")
      .insert({
        user_id: appUserId,
        performed_by_actor_id: actorId,
        acting_as_actor_id: actorId,
        acting_for_actor_id: actorId,
        activity_type_id: null,
        activity_template_id: null,
        template_id: null,

LINE 934: .from("activity_events")
    });
  
    const { data: createdEvent, error: eventError } = await supabase
>     .from("activity_events")
      .insert({
        user_id: appUser.id,
        performed_by_actor_id: personActor.id,
        acting_as_actor_id: personActor.id,
        acting_for_actor_id: personActor.id,
        activity_type_id: template.default_activity_type_id,
        activity_template_id: template.id,
        template_id: template.legacy_activity_code_template_id,

LINE 1091: .from("activity_events")
      });
  
      await supabase
>       .from("activity_events")
        .update({ processing_status: "failed" })
        .eq("id", createdEvent.id)
        .eq("user_id", appUser.id);
  
      return NextResponse.json(
        {
          ok: false,
          error: eventLinksError.message,

LINE 1449: .from("activity_events")
      });
  
      await supabase
>       .from("activity_events")
        .update({ processing_status: "failed" })
        .eq("id", createdEvent.id)
        .eq("user_id", appUser.id);
  
      return NextResponse.json(
        {
          ok: false,
          error:

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: .insert({
LABEL: contract evidence
------------------------------------------------------------

LINE 614: .insert({
  
    const { data: createdEvent, error: eventError } = await supabase
      .from("activity_events")
>     .insert({
        user_id: appUserId,
        performed_by_actor_id: actorId,
        acting_as_actor_id: actorId,
        acting_for_actor_id: actorId,
        activity_type_id: null,
        activity_template_id: null,
        template_id: null,
        event_code: null,

LINE 935: .insert({
  
    const { data: createdEvent, error: eventError } = await supabase
      .from("activity_events")
>     .insert({
        user_id: appUser.id,
        performed_by_actor_id: personActor.id,
        acting_as_actor_id: personActor.id,
        acting_for_actor_id: personActor.id,
        activity_type_id: template.default_activity_type_id,
        activity_template_id: template.id,
        template_id: template.legacy_activity_code_template_id,
        event_code:

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: .update({
LABEL: contract evidence
------------------------------------------------------------

LINE 1092: .update({ processing_status: "failed" })
  
      await supabase
        .from("activity_events")
>       .update({ processing_status: "failed" })
        .eq("id", createdEvent.id)
        .eq("user_id", appUser.id);
  
      return NextResponse.json(
        {
          ok: false,
          error: eventLinksError.message,
          event: createdEvent,

LINE 1450: .update({ processing_status: "failed" })
  
      await supabase
        .from("activity_events")
>       .update({ processing_status: "failed" })
        .eq("id", createdEvent.id)
        .eq("user_id", appUser.id);
  
      return NextResponse.json(
        {
          ok: false,
          error:
            error instanceof Error

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: processing_status
LABEL: contract evidence
------------------------------------------------------------

LINE 632: processing_status: "pending",
        source: normalizeSourceType(body.sourceType, "manual_chat"),
        status: "draft",
        privacy_scope: "private",
>       processing_status: "pending",
        metadata_json: {
          parser: "template_first_v2",
          natural_input_draft: true,
          ai_used: false,
          needs_classification: true,
        },
      })
      .select()

LINE 954: processing_status: "processed",
        source,
        status,
        privacy_scope: template.default_privacy_scope,
>       processing_status: "processed",
        metadata_json: {
          parser: "template_first_v2",
          resolved_by: resolvedBy,
          ai_used: false,
          template_slug: template.slug,
          template_scope: template.template_scope,
          template_group: template.template_group,
          shortcut_id: shortcut?.id ?? null,

LINE 1034: processingStatus: createdEvent.processing_status,
      output: {
        activityEventId: createdEvent.id,
        status: createdEvent.status,
>       processingStatus: createdEvent.processing_status,
      },
      metadata: {
        endpoint: "/api/activity/record",
        mode: "template_first_completed",
      },
      startedAt: processingStartedAt.toISOString(),
      finishedAt: new Date().toISOString(),
      durationMs: getDurationMs(processingStartedAt),

LINE 1092: .update({ processing_status: "failed" })
  
      await supabase
        .from("activity_events")
>       .update({ processing_status: "failed" })
        .eq("id", createdEvent.id)
        .eq("user_id", appUser.id);
  
      return NextResponse.json(
        {
          ok: false,
          error: eventLinksError.message,
          event: createdEvent,

LINE 1386: processedSignalResult?.signal?.processing_status ??
          ? {
              id: rawSignal.id,
              processingStatus:
>               processedSignalResult?.signal?.processing_status ??
                rawSignal.processing_status,
            }
          : null,
        processingRunId,
        parser: {
          mode: "template_first_v2",
          resolvedBy,
          templateId: template.id,

LINE 1387: rawSignal.processing_status,
              id: rawSignal.id,
              processingStatus:
                processedSignalResult?.signal?.processing_status ??
>               rawSignal.processing_status,
            }
          : null,
        processingRunId,
        parser: {
          mode: "template_first_v2",
          resolvedBy,
          templateId: template.id,
          templateSlug: template.slug,

LINE 1450: .update({ processing_status: "failed" })
  
      await supabase
        .from("activity_events")
>       .update({ processing_status: "failed" })
        .eq("id", createdEvent.id)
        .eq("user_id", appUser.id);
  
      return NextResponse.json(
        {
          ok: false,
          error:
            error instanceof Error

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: performed_by_actor_id
LABEL: contract evidence
------------------------------------------------------------

LINE 616: performed_by_actor_id: actorId,
      .from("activity_events")
      .insert({
        user_id: appUserId,
>       performed_by_actor_id: actorId,
        acting_as_actor_id: actorId,
        acting_for_actor_id: actorId,
        activity_type_id: null,
        activity_template_id: null,
        template_id: null,
        event_code: null,
        input_text: naturalInput,
        title: asString(body.title) ?? "Unclassified activity draft",

LINE 937: performed_by_actor_id: personActor.id,
      .from("activity_events")
      .insert({
        user_id: appUser.id,
>       performed_by_actor_id: personActor.id,
        acting_as_actor_id: personActor.id,
        acting_for_actor_id: personActor.id,
        activity_type_id: template.default_activity_type_id,
        activity_template_id: template.id,
        template_id: template.legacy_activity_code_template_id,
        event_code:
          shortcut?.shortcut_type === "legacy_code" ? shortcut.shortcut_value : null,
        input_text: inputText,

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: acting_as_actor_id
LABEL: contract evidence
------------------------------------------------------------

LINE 617: acting_as_actor_id: actorId,
      .insert({
        user_id: appUserId,
        performed_by_actor_id: actorId,
>       acting_as_actor_id: actorId,
        acting_for_actor_id: actorId,
        activity_type_id: null,
        activity_template_id: null,
        template_id: null,
        event_code: null,
        input_text: naturalInput,
        title: asString(body.title) ?? "Unclassified activity draft",
        description: asString(body.comment),

LINE 938: acting_as_actor_id: personActor.id,
      .insert({
        user_id: appUser.id,
        performed_by_actor_id: personActor.id,
>       acting_as_actor_id: personActor.id,
        acting_for_actor_id: personActor.id,
        activity_type_id: template.default_activity_type_id,
        activity_template_id: template.id,
        template_id: template.legacy_activity_code_template_id,
        event_code:
          shortcut?.shortcut_type === "legacy_code" ? shortcut.shortcut_value : null,
        input_text: inputText,
        title,

------------------------------------------------------------
FILE: src/app/api/activity/record/route.ts
PATTERN: input_text
LABEL: contract evidence
------------------------------------------------------------

LINE 623: input_text: naturalInput,
        activity_template_id: null,
        template_id: null,
        event_code: null,
>       input_text: naturalInput,
        title: asString(body.title) ?? "Unclassified activity draft",
        description: asString(body.comment),
        started_at: timing.startedAt,
        ended_at: timing.endedAt,
        duration_minutes: timing.durationMinutes,
        source: normalizeSourceType(body.sourceType, "manual_chat"),
        status: "draft",
        privacy_scope: "private",

LINE 945: input_text: inputText,
        template_id: template.legacy_activity_code_template_id,
        event_code:
          shortcut?.shortcut_type === "legacy_code" ? shortcut.shortcut_value : null,
>       input_text: inputText,
        title,
        description: comment ?? template.description,
        started_at: timing.startedAt,
        ended_at: timing.endedAt,
        duration_minutes: timing.durationMinutes,
        source,
        status,
        privacy_scope: template.default_privacy_scope,

############################################################
ROUTE FILE: src/app/api/activity/events/route.ts
############################################################

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: type 
LABEL: contract evidence
------------------------------------------------------------

LINE 13: type Row = Record<string, unknown>;
  
  export const dynamic = "force-dynamic";
  
> type Row = Record<string, unknown>;
  
  type DateRange = {
    from: string;
    to: string;
  };
  
  function parseLimit(searchParams: URLSearchParams): number {
    const rawLimit = searchParams.get("limit");

LINE 15: type DateRange = {
  
  type Row = Record<string, unknown>;
  
> type DateRange = {
    from: string;
    to: string;
  };
  
  function parseLimit(searchParams: URLSearchParams): number {
    const rawLimit = searchParams.get("limit");
  
    if (!rawLimit) {

LINE 286: const sourceType = parseOptionalString(url.searchParams, "sourceType");
    const url = new URL(request.url);
    const limit = parseLimit(url.searchParams);
    const status = parseOptionalString(url.searchParams, "status");
>   const sourceType = parseOptionalString(url.searchParams, "sourceType");
    const templateId = parseOptionalString(url.searchParams, "templateId");
    const dateRange = parseDateRange(url.searchParams);
    const includeDetails = parseBooleanFlag(url.searchParams, "includeDetails");
  
    const mode = includeDetails ? "details" : "summary";
  
    let eventsQuery = supabase
      .from("activity_events")

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: interface 
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: export async function POST
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: request.json
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: eventId
LABEL: contract evidence
------------------------------------------------------------

LINE 138: function groupByEventId(rows: Row[]): Map<string, Row[]> {
    return indexed;
  }
  
> function groupByEventId(rows: Row[]): Map<string, Row[]> {
    const grouped = new Map<string, Row[]>();
  
    for (const row of rows) {
      const eventId = asString(row.event_id);
  
      if (!eventId) {
        continue;
      }

LINE 142: const eventId = asString(row.event_id);
    const grouped = new Map<string, Row[]>();
  
    for (const row of rows) {
>     const eventId = asString(row.event_id);
  
      if (!eventId) {
        continue;
      }
  
      const existingRows = grouped.get(eventId) ?? [];
      existingRows.push(row);
      grouped.set(eventId, existingRows);

LINE 144: if (!eventId) {
    for (const row of rows) {
      const eventId = asString(row.event_id);
  
>     if (!eventId) {
        continue;
      }
  
      const existingRows = grouped.get(eventId) ?? [];
      existingRows.push(row);
      grouped.set(eventId, existingRows);
    }
  

LINE 148: const existingRows = grouped.get(eventId) ?? [];
        continue;
      }
  
>     const existingRows = grouped.get(eventId) ?? [];
      existingRows.push(row);
      grouped.set(eventId, existingRows);
    }
  
    return grouped;
  }
  
  function normalizeEventLink(row: Row) {

LINE 150: grouped.set(eventId, existingRows);
  
      const existingRows = grouped.get(eventId) ?? [];
      existingRows.push(row);
>     grouped.set(eventId, existingRows);
    }
  
    return grouped;
  }
  
  function normalizeEventLink(row: Row) {
    return {
      id: asString(row.id),

LINE 159: eventId: asString(row.event_id),
  function normalizeEventLink(row: Row) {
    return {
      id: asString(row.id),
>     eventId: asString(row.event_id),
      entityType:
        asString(row.linked_entity_type) ??
        asString(row.entity_type) ??
        asString(row.target_type),
      entityId:
        asString(row.linked_entity_id) ??
        asString(row.entity_id) ??
        asString(row.target_id),

LINE 185: eventId: asString(row.event_id),
  function normalizeImpactEvent(row: Row) {
    return {
      id: asString(row.id),
>     eventId: asString(row.event_id),
      targetType: asString(row.impact_target_type) ?? asString(row.target_type),
      targetKey: asString(row.impact_target_key) ?? asString(row.target_key),
      metric:
        asString(row.impact_metric) ??
        asString(row.metric) ??
        asString(row.metric_key),
      valueNumeric:
        asNumber(row.impact_value_numeric) ??

LINE 331: const eventIds = uniqueStrings(eventRows.map((event) => asString(event.id)));
    }
  
    const eventRows = toRows(eventRowsRaw);
>   const eventIds = uniqueStrings(eventRows.map((event) => asString(event.id)));
  
    const filters = {
      status,
      sourceType,
      templateId,
      date: url.searchParams.get("date"),
      includeDetails,
    };

LINE 341: if (eventIds.length === 0) {
      includeDetails,
    };
  
>   if (eventIds.length === 0) {
      return NextResponse.json({
        ok: true,
        mode,
        limit,
        filters,
        count: 0,
        events: [],
      });

LINE 358: .in("event_id", eventIds);
    const { data: eventLinkRowsRaw, error: eventLinksError } = await supabase
      .from("event_links")
      .select(eventLinkSelect)
>     .in("event_id", eventIds);
  
    if (eventLinksError) {
      return NextResponse.json(
        {
          ok: false,
          error: eventLinksError.message,
        },
        { status: 500 }

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: inputText
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: naturalInput
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: title
LABEL: contract evidence
------------------------------------------------------------

LINE 219: title: asString(row.title),
    return {
      id: asString(row.id),
      slug: asString(row.slug),
>     title: asString(row.title),
      description: asString(row.description),
      templateScope: asString(row.template_scope),
      visibility: asString(row.visibility),
      defaultDurationMinutes: asNumber(row.default_duration_minutes),
      defaultSourceType: asString(row.default_source_type),
      defaultPrivacyScope: asString(row.default_privacy_scope),
      isActive: asBoolean(row.is_active),
    };

LINE 238: title: asString(row.title),
    return {
      id: asString(row.id),
      code: asString(row.code),
>     title: asString(row.title),
      description: asString(row.description),
    };
  }
  
  function normalizeActivityType(row: Row | null) {
    if (!row) {
      return null;
    }

LINE 251: title: asString(row.title),
    return {
      id: asString(row.id),
      code: asString(row.code),
>     title: asString(row.title),
      description: asString(row.description),
    };
  }
  
  export async function GET(request: Request) {
    if (!ACTIVITY_RECORDING_ENABLED) {
      return NextResponse.json(
        {

LINE 398: title: asString(event.title),
  
        return {
          id: eventId,
>         title: asString(event.title),
          status: asString(event.status),
          source,
          sourceType: source,
          privacyScope: asString(event.privacy_scope),
          processingStatus: asString(event.processing_status),
          startedAt: asString(event.started_at),
          endedAt: asString(event.ended_at),
          durationMinutes:

LINE 531: title: asString(event.title),
  
      return {
        id: eventId,
>       title: asString(event.title),
        status: asString(event.status),
        source,
        sourceType: source,
        privacyScope: asString(event.privacy_scope),
        processingStatus: asString(event.processing_status),
        startedAt: asString(event.started_at),
        endedAt: asString(event.ended_at),
        durationMinutes:

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: description
LABEL: contract evidence
------------------------------------------------------------

LINE 220: description: asString(row.description),
      id: asString(row.id),
      slug: asString(row.slug),
      title: asString(row.title),
>     description: asString(row.description),
      templateScope: asString(row.template_scope),
      visibility: asString(row.visibility),
      defaultDurationMinutes: asNumber(row.default_duration_minutes),
      defaultSourceType: asString(row.default_source_type),
      defaultPrivacyScope: asString(row.default_privacy_scope),
      isActive: asBoolean(row.is_active),
    };
  }

LINE 239: description: asString(row.description),
      id: asString(row.id),
      code: asString(row.code),
      title: asString(row.title),
>     description: asString(row.description),
    };
  }
  
  function normalizeActivityType(row: Row | null) {
    if (!row) {
      return null;
    }
  

LINE 252: description: asString(row.description),
      id: asString(row.id),
      code: asString(row.code),
      title: asString(row.title),
>     description: asString(row.description),
    };
  }
  
  export async function GET(request: Request) {
    if (!ACTIVITY_RECORDING_ENABLED) {
      return NextResponse.json(
        {
          ok: false,

LINE 409: asString(event.description) ??
          durationMinutes:
            asNumber(event.duration_minutes) ?? asNumber(event.durationMinutes),
          comment:
>           asString(event.description) ??
            asString(event.comment) ??
            asString(event.input_text),
          activityTypeId: asString(event.activity_type_id),
          activityTemplateId: asString(event.activity_template_id),
          legacyTemplateId: asString(event.template_id),
          createdAt: asString(event.created_at),
          updatedAt: asString(event.updated_at),
          eventLinksCount: eventId

LINE 542: asString(event.description) ??
        durationMinutes:
          asNumber(event.duration_minutes) ?? asNumber(event.durationMinutes),
        comment:
>         asString(event.description) ??
          asString(event.comment) ??
          asString(event.input_text),
        activityTypeId,
        activityTemplateId,
        legacyTemplateId,
        createdAt: asString(event.created_at),
        updatedAt: asString(event.updated_at),
        activityTemplate: normalizeActivityTemplate(

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: durationMinutes
LABEL: contract evidence
------------------------------------------------------------

LINE 223: defaultDurationMinutes: asNumber(row.default_duration_minutes),
      description: asString(row.description),
      templateScope: asString(row.template_scope),
      visibility: asString(row.visibility),
>     defaultDurationMinutes: asNumber(row.default_duration_minutes),
      defaultSourceType: asString(row.default_source_type),
      defaultPrivacyScope: asString(row.default_privacy_scope),
      isActive: asBoolean(row.is_active),
    };
  }
  
  function normalizeLegacyTemplate(row: Row | null) {
    if (!row) {

LINE 406: durationMinutes:
          processingStatus: asString(event.processing_status),
          startedAt: asString(event.started_at),
          endedAt: asString(event.ended_at),
>         durationMinutes:
            asNumber(event.duration_minutes) ?? asNumber(event.durationMinutes),
          comment:
            asString(event.description) ??
            asString(event.comment) ??
            asString(event.input_text),
          activityTypeId: asString(event.activity_type_id),
          activityTemplateId: asString(event.activity_template_id),
          legacyTemplateId: asString(event.template_id),

LINE 407: asNumber(event.duration_minutes) ?? asNumber(event.durationMinutes),
          startedAt: asString(event.started_at),
          endedAt: asString(event.ended_at),
          durationMinutes:
>           asNumber(event.duration_minutes) ?? asNumber(event.durationMinutes),
          comment:
            asString(event.description) ??
            asString(event.comment) ??
            asString(event.input_text),
          activityTypeId: asString(event.activity_type_id),
          activityTemplateId: asString(event.activity_template_id),
          legacyTemplateId: asString(event.template_id),
          createdAt: asString(event.created_at),

LINE 539: durationMinutes:
        processingStatus: asString(event.processing_status),
        startedAt: asString(event.started_at),
        endedAt: asString(event.ended_at),
>       durationMinutes:
          asNumber(event.duration_minutes) ?? asNumber(event.durationMinutes),
        comment:
          asString(event.description) ??
          asString(event.comment) ??
          asString(event.input_text),
        activityTypeId,
        activityTemplateId,
        legacyTemplateId,

LINE 540: asNumber(event.duration_minutes) ?? asNumber(event.durationMinutes),
        startedAt: asString(event.started_at),
        endedAt: asString(event.ended_at),
        durationMinutes:
>         asNumber(event.duration_minutes) ?? asNumber(event.durationMinutes),
        comment:
          asString(event.description) ??
          asString(event.comment) ??
          asString(event.input_text),
        activityTypeId,
        activityTemplateId,
        legacyTemplateId,
        createdAt: asString(event.created_at),

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: startedAt
LABEL: contract evidence
------------------------------------------------------------

LINE 404: startedAt: asString(event.started_at),
          sourceType: source,
          privacyScope: asString(event.privacy_scope),
          processingStatus: asString(event.processing_status),
>         startedAt: asString(event.started_at),
          endedAt: asString(event.ended_at),
          durationMinutes:
            asNumber(event.duration_minutes) ?? asNumber(event.durationMinutes),
          comment:
            asString(event.description) ??
            asString(event.comment) ??
            asString(event.input_text),
          activityTypeId: asString(event.activity_type_id),

LINE 537: startedAt: asString(event.started_at),
        sourceType: source,
        privacyScope: asString(event.privacy_scope),
        processingStatus: asString(event.processing_status),
>       startedAt: asString(event.started_at),
        endedAt: asString(event.ended_at),
        durationMinutes:
          asNumber(event.duration_minutes) ?? asNumber(event.durationMinutes),
        comment:
          asString(event.description) ??
          asString(event.comment) ??
          asString(event.input_text),
        activityTypeId,

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: endedAt
LABEL: contract evidence
------------------------------------------------------------

LINE 405: endedAt: asString(event.ended_at),
          privacyScope: asString(event.privacy_scope),
          processingStatus: asString(event.processing_status),
          startedAt: asString(event.started_at),
>         endedAt: asString(event.ended_at),
          durationMinutes:
            asNumber(event.duration_minutes) ?? asNumber(event.durationMinutes),
          comment:
            asString(event.description) ??
            asString(event.comment) ??
            asString(event.input_text),
          activityTypeId: asString(event.activity_type_id),
          activityTemplateId: asString(event.activity_template_id),

LINE 538: endedAt: asString(event.ended_at),
        privacyScope: asString(event.privacy_scope),
        processingStatus: asString(event.processing_status),
        startedAt: asString(event.started_at),
>       endedAt: asString(event.ended_at),
        durationMinutes:
          asNumber(event.duration_minutes) ?? asNumber(event.durationMinutes),
        comment:
          asString(event.description) ??
          asString(event.comment) ??
          asString(event.input_text),
        activityTypeId,
        activityTemplateId,

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: status
LABEL: contract evidence
------------------------------------------------------------

LINE 263: { status: 503 }
          ok: false,
          error: ACTIVITY_RECORDING_DISABLED_MESSAGE,
        },
>       { status: 503 }
      );
    }
  
    const { appUser, errorResponse } = await getActivityUserContext();
  
    if (errorResponse) {
      return errorResponse;
    }

LINE 279: { status: 500 }
          ok: false,
          error: "User context not found",
        },
>       { status: 500 }
      );
    }
  
    const url = new URL(request.url);
    const limit = parseLimit(url.searchParams);
    const status = parseOptionalString(url.searchParams, "status");
    const sourceType = parseOptionalString(url.searchParams, "sourceType");
    const templateId = parseOptionalString(url.searchParams, "templateId");

LINE 285: const status = parseOptionalString(url.searchParams, "status");
  
    const url = new URL(request.url);
    const limit = parseLimit(url.searchParams);
>   const status = parseOptionalString(url.searchParams, "status");
    const sourceType = parseOptionalString(url.searchParams, "sourceType");
    const templateId = parseOptionalString(url.searchParams, "templateId");
    const dateRange = parseDateRange(url.searchParams);
    const includeDetails = parseBooleanFlag(url.searchParams, "includeDetails");
  
    const mode = includeDetails ? "details" : "summary";
  
    let eventsQuery = supabase

LINE 298: if (status) {
      .select("*")
      .eq("user_id", appUser.id);
  
>   if (status) {
      eventsQuery = eventsQuery.eq("status", status);
    }
  
    if (sourceType) {
      eventsQuery = eventsQuery.eq("source", sourceType);
    }
  
    if (templateId) {

LINE 299: eventsQuery = eventsQuery.eq("status", status);
      .eq("user_id", appUser.id);
  
    if (status) {
>     eventsQuery = eventsQuery.eq("status", status);
    }
  
    if (sourceType) {
      eventsQuery = eventsQuery.eq("source", sourceType);
    }
  
    if (templateId) {
      eventsQuery = eventsQuery.eq("activity_template_id", templateId);

LINE 326: { status: 500 }
          ok: false,
          error: eventsError.message,
        },
>       { status: 500 }
      );
    }
  
    const eventRows = toRows(eventRowsRaw);
    const eventIds = uniqueStrings(eventRows.map((event) => asString(event.id)));
  
    const filters = {
      status,

LINE 334: status,
    const eventIds = uniqueStrings(eventRows.map((event) => asString(event.id)));
  
    const filters = {
>     status,
      sourceType,
      templateId,
      date: url.searchParams.get("date"),
      includeDetails,
    };
  
    if (eventIds.length === 0) {
      return NextResponse.json({

LINE 366: { status: 500 }
          ok: false,
          error: eventLinksError.message,
        },
>       { status: 500 }
      );
    }
  
    const { data: impactEventRowsRaw, error: impactEventsError } = await supabase
      .from("impact_events")
      .select(impactEventSelect)
      .in("event_id", eventIds);
  

LINE 381: { status: 500 }
          ok: false,
          error: impactEventsError.message,
        },
>       { status: 500 }
      );
    }
  
    const eventLinkRows = toRows(eventLinkRowsRaw);
    const impactEventRows = toRows(impactEventRowsRaw);
  
    const linksByEventId = groupByEventId(eventLinkRows);
    const impactsByEventId = groupByEventId(impactEventRows);

LINE 399: status: asString(event.status),
        return {
          id: eventId,
          title: asString(event.title),
>         status: asString(event.status),
          source,
          sourceType: source,
          privacyScope: asString(event.privacy_scope),
          processingStatus: asString(event.processing_status),
          startedAt: asString(event.started_at),
          endedAt: asString(event.ended_at),
          durationMinutes:
            asNumber(event.duration_minutes) ?? asNumber(event.durationMinutes),

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: started
LABEL: contract evidence
------------------------------------------------------------

LINE 404: startedAt: asString(event.started_at),
          sourceType: source,
          privacyScope: asString(event.privacy_scope),
          processingStatus: asString(event.processing_status),
>         startedAt: asString(event.started_at),
          endedAt: asString(event.ended_at),
          durationMinutes:
            asNumber(event.duration_minutes) ?? asNumber(event.durationMinutes),
          comment:
            asString(event.description) ??
            asString(event.comment) ??
            asString(event.input_text),
          activityTypeId: asString(event.activity_type_id),

LINE 537: startedAt: asString(event.started_at),
        sourceType: source,
        privacyScope: asString(event.privacy_scope),
        processingStatus: asString(event.processing_status),
>       startedAt: asString(event.started_at),
        endedAt: asString(event.ended_at),
        durationMinutes:
          asNumber(event.duration_minutes) ?? asNumber(event.durationMinutes),
        comment:
          asString(event.description) ??
          asString(event.comment) ??
          asString(event.input_text),
        activityTypeId,

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: planned
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: pending
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: completed
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: .from("activity_events")
LABEL: contract evidence
------------------------------------------------------------

LINE 294: .from("activity_events")
    const mode = includeDetails ? "details" : "summary";
  
    let eventsQuery = supabase
>     .from("activity_events")
      .select("*")
      .eq("user_id", appUser.id);
  
    if (status) {
      eventsQuery = eventsQuery.eq("status", status);
    }
  
    if (sourceType) {

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: .insert({
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: .update({
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: processing_status
LABEL: contract evidence
------------------------------------------------------------

LINE 403: processingStatus: asString(event.processing_status),
          source,
          sourceType: source,
          privacyScope: asString(event.privacy_scope),
>         processingStatus: asString(event.processing_status),
          startedAt: asString(event.started_at),
          endedAt: asString(event.ended_at),
          durationMinutes:
            asNumber(event.duration_minutes) ?? asNumber(event.durationMinutes),
          comment:
            asString(event.description) ??
            asString(event.comment) ??
            asString(event.input_text),

LINE 536: processingStatus: asString(event.processing_status),
        source,
        sourceType: source,
        privacyScope: asString(event.privacy_scope),
>       processingStatus: asString(event.processing_status),
        startedAt: asString(event.started_at),
        endedAt: asString(event.ended_at),
        durationMinutes:
          asNumber(event.duration_minutes) ?? asNumber(event.durationMinutes),
        comment:
          asString(event.description) ??
          asString(event.comment) ??
          asString(event.input_text),

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: performed_by_actor_id
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: acting_as_actor_id
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/events/route.ts
PATTERN: input_text
LABEL: contract evidence
------------------------------------------------------------

LINE 411: asString(event.input_text),
          comment:
            asString(event.description) ??
            asString(event.comment) ??
>           asString(event.input_text),
          activityTypeId: asString(event.activity_type_id),
          activityTemplateId: asString(event.activity_template_id),
          legacyTemplateId: asString(event.template_id),
          createdAt: asString(event.created_at),
          updatedAt: asString(event.updated_at),
          eventLinksCount: eventId
            ? linksByEventId.get(eventId)?.length ?? 0
            : 0,

LINE 544: asString(event.input_text),
        comment:
          asString(event.description) ??
          asString(event.comment) ??
>         asString(event.input_text),
        activityTypeId,
        activityTemplateId,
        legacyTemplateId,
        createdAt: asString(event.created_at),
        updatedAt: asString(event.updated_at),
        activityTemplate: normalizeActivityTemplate(
          activityTemplateId
            ? activityTemplatesById.get(activityTemplateId) ?? null

############################################################
ROUTE FILE: src/app/api/activity/intake/route.ts
############################################################

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: type 
LABEL: contract evidence
------------------------------------------------------------

LINE 14: type RawActivitySignalPrivacyScope,
  } from "../../../../../lib/activity/activitySourceIntake";
  import {
    createRawActivitySignal,
>   type RawActivitySignalPrivacyScope,
    type RawActivitySignalRow,
    type RawActivitySignalSourceType,
    type RawActivitySignalTrustLevel,
  } from "../../../../../lib/activity/rawActivitySignals";
  import { supabase } from "../../../../../lib/supabase";
  
  export const dynamic = "force-dynamic";
  

LINE 15: type RawActivitySignalRow,
  import {
    createRawActivitySignal,
    type RawActivitySignalPrivacyScope,
>   type RawActivitySignalRow,
    type RawActivitySignalSourceType,
    type RawActivitySignalTrustLevel,
  } from "../../../../../lib/activity/rawActivitySignals";
  import { supabase } from "../../../../../lib/supabase";
  
  export const dynamic = "force-dynamic";
  
  type ActivityIntakeBody = {

LINE 16: type RawActivitySignalSourceType,
    createRawActivitySignal,
    type RawActivitySignalPrivacyScope,
    type RawActivitySignalRow,
>   type RawActivitySignalSourceType,
    type RawActivitySignalTrustLevel,
  } from "../../../../../lib/activity/rawActivitySignals";
  import { supabase } from "../../../../../lib/supabase";
  
  export const dynamic = "force-dynamic";
  
  type ActivityIntakeBody = {
    sourceType?: unknown;

LINE 17: type RawActivitySignalTrustLevel,
    type RawActivitySignalPrivacyScope,
    type RawActivitySignalRow,
    type RawActivitySignalSourceType,
>   type RawActivitySignalTrustLevel,
  } from "../../../../../lib/activity/rawActivitySignals";
  import { supabase } from "../../../../../lib/supabase";
  
  export const dynamic = "force-dynamic";
  
  type ActivityIntakeBody = {
    sourceType?: unknown;
    sourceEventId?: unknown;

LINE 23: type ActivityIntakeBody = {
  
  export const dynamic = "force-dynamic";
  
> type ActivityIntakeBody = {
    sourceType?: unknown;
    sourceEventId?: unknown;
    externalId?: unknown;
    idempotencyKey?: unknown;
    occurredAt?: unknown;
    measuredAt?: unknown;
    trustLevel?: unknown;
    privacyScope?: unknown;

LINE 38: type ExistingRawSignalMatch = {
    metadata?: unknown;
  };
  
> type ExistingRawSignalMatch = {
    signal: RawActivitySignalRow;
    matchedBy: "idempotency_key" | "source_event_id";
  };
  
  const ENDPOINT = "/api/activity/intake";
  
  const ALLOWED_TRUST_LEVELS = new Set([
    "untrusted",

LINE 315: const rawSourceType = normalizeRawActivitySignalSourceType(body.sourceType);
      );
    }
  
>   const rawSourceType = normalizeRawActivitySignalSourceType(body.sourceType);
    const decision = decideActivityIntake({
      sourceType: rawSourceType,
    });
  
    const occurredAt = normalizeOptionalIsoDate(body.occurredAt);
    const measuredAt = normalizeOptionalIsoDate(body.measuredAt);
    const sourceEventId = asString(body.sourceEventId) ?? asString(body.externalId);
  

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: interface 
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: export async function POST
LABEL: contract evidence
------------------------------------------------------------

LINE 274: export async function POST(request: Request) {
    });
  }
  
> export async function POST(request: Request) {
    if (!ACTIVITY_RECORDING_ENABLED) {
      return NextResponse.json(
        {
          ok: false,
          error: ACTIVITY_RECORDING_DISABLED_MESSAGE,
        },
        { status: 503 }
      );

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: request.json
LABEL: contract evidence
------------------------------------------------------------

LINE 304: body = (await request.json()) as ActivityIntakeBody;
    let body: ActivityIntakeBody;
  
    try {
>     body = (await request.json()) as ActivityIntakeBody;
    } catch {
      return NextResponse.json(
        {
          ok: false,
          error: "Invalid JSON body",
        },
        { status: 400 }
      );

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: eventId
LABEL: contract evidence
------------------------------------------------------------

LINE 25: sourceEventId?: unknown;
  
  type ActivityIntakeBody = {
    sourceType?: unknown;
>   sourceEventId?: unknown;
    externalId?: unknown;
    idempotencyKey?: unknown;
    occurredAt?: unknown;
    measuredAt?: unknown;
    trustLevel?: unknown;
    privacyScope?: unknown;
    payload?: unknown;
    rawPayload?: unknown;

LINE 125: sourceEventId: rawSignal.source_event_id,
    return {
      id: rawSignal.id,
      sourceType: rawSignal.source_type,
>     sourceEventId: rawSignal.source_event_id,
      idempotencyKey: rawSignal.idempotency_key,
      trustLevel: rawSignal.trust_level,
      privacyScope: rawSignal.privacy_scope,
      processingStatus: rawSignal.processing_status,
      occurredAt: rawSignal.occurred_at,
      measuredAt: rawSignal.measured_at,
      outputEventId: rawSignal.output_event_id,
      createdAt: rawSignal.created_at,

LINE 132: outputEventId: rawSignal.output_event_id,
      processingStatus: rawSignal.processing_status,
      occurredAt: rawSignal.occurred_at,
      measuredAt: rawSignal.measured_at,
>     outputEventId: rawSignal.output_event_id,
      createdAt: rawSignal.created_at,
    };
  }
  
  function buildNormalizedPreview(params: {
    body: ActivityIntakeBody;
    decision: ReturnType<typeof decideActivityIntake>;
    sourceEventId: string | null;

LINE 140: sourceEventId: string | null;
  function buildNormalizedPreview(params: {
    body: ActivityIntakeBody;
    decision: ReturnType<typeof decideActivityIntake>;
>   sourceEventId: string | null;
    idempotencyKey: string;
    occurredAt: string | null;
    measuredAt: string | null;
  }) {
    const {
      body,
      decision,
      sourceEventId,

LINE 148: sourceEventId,
    const {
      body,
      decision,
>     sourceEventId,
      idempotencyKey,
      occurredAt,
      measuredAt,
    } = params;
  
    return {
      ...(asRecord(body.normalizedPreview)),
      intake: {

LINE 159: sourceEventId,
      intake: {
        sourceType: decision.rawSourceType,
        activityEventSource: decision.activityEventSource,
>       sourceEventId,
        idempotencyKey,
        occurredAt,
        measuredAt,
        defaultActivityStatus: decision.defaultActivityStatus,
        defaultRawProcessingStatus: decision.defaultRawProcessingStatus,
        requiresHumanReview: decision.requiresHumanReview,
        shouldCreateImportedPendingEvent:
          decision.shouldCreateImportedPendingEvent,

LINE 177: sourceEventId: string | null;
  async function findExistingRawSignal(params: {
    userId: string;
    sourceType: RawActivitySignalSourceType;
>   sourceEventId: string | null;
    idempotencyKey: string | null;
  }): Promise<ExistingRawSignalMatch | null> {
    const { userId, sourceType, sourceEventId, idempotencyKey } = params;
  
    if (idempotencyKey) {
      const { data, error } = await supabase
        .from("raw_activity_signals")
        .select("*")

LINE 180: const { userId, sourceType, sourceEventId, idempotencyKey } = params;
    sourceEventId: string | null;
    idempotencyKey: string | null;
  }): Promise<ExistingRawSignalMatch | null> {
>   const { userId, sourceType, sourceEventId, idempotencyKey } = params;
  
    if (idempotencyKey) {
      const { data, error } = await supabase
        .from("raw_activity_signals")
        .select("*")
        .eq("user_id", userId)
        .eq("source_type", sourceType)
        .eq("idempotency_key", idempotencyKey)

LINE 203: if (sourceEventId) {
      }
    }
  
>   if (sourceEventId) {
      const { data, error } = await supabase
        .from("raw_activity_signals")
        .select("*")
        .eq("user_id", userId)
        .eq("source_type", sourceType)
        .eq("source_event_id", sourceEventId)
        .maybeSingle();
  

LINE 209: .eq("source_event_id", sourceEventId)
        .select("*")
        .eq("user_id", userId)
        .eq("source_type", sourceType)
>       .eq("source_event_id", sourceEventId)
        .maybeSingle();
  
      if (error) {
        throw new Error(error.message);
      }
  
      if (data) {
        return {

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: inputText
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: naturalInput
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: title
LABEL: contract evidence
------------------------------------------------------------

LINE 242: title: "External activity signal",
        externalId: "external-event-123",
        occurredAt: new Date().toISOString(),
        payload: {
>         title: "External activity signal",
          durationMinutes: 10,
        },
        metadata: {
          adapter: "example",
        },
      },
      duplicateHandling: {
        status: "P4.2.7",

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: description
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: durationMinutes
LABEL: contract evidence
------------------------------------------------------------

LINE 243: durationMinutes: 10,
        occurredAt: new Date().toISOString(),
        payload: {
          title: "External activity signal",
>         durationMinutes: 10,
        },
        metadata: {
          adapter: "example",
        },
      },
      duplicateHandling: {
        status: "P4.2.7",
        behavior:

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: startedAt
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: endedAt
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: status
LABEL: contract evidence
------------------------------------------------------------

LINE 129: processingStatus: rawSignal.processing_status,
      idempotencyKey: rawSignal.idempotency_key,
      trustLevel: rawSignal.trust_level,
      privacyScope: rawSignal.privacy_scope,
>     processingStatus: rawSignal.processing_status,
      occurredAt: rawSignal.occurred_at,
      measuredAt: rawSignal.measured_at,
      outputEventId: rawSignal.output_event_id,
      createdAt: rawSignal.created_at,
    };
  }
  
  function buildNormalizedPreview(params: {

LINE 163: defaultActivityStatus: decision.defaultActivityStatus,
        idempotencyKey,
        occurredAt,
        measuredAt,
>       defaultActivityStatus: decision.defaultActivityStatus,
        defaultRawProcessingStatus: decision.defaultRawProcessingStatus,
        requiresHumanReview: decision.requiresHumanReview,
        shouldCreateImportedPendingEvent:
          decision.shouldCreateImportedPendingEvent,
        shouldCreateDraftEvent: decision.shouldCreateDraftEvent,
        shouldCreateCompletedEvent: decision.shouldCreateCompletedEvent,
      },
    };

LINE 164: defaultRawProcessingStatus: decision.defaultRawProcessingStatus,
        occurredAt,
        measuredAt,
        defaultActivityStatus: decision.defaultActivityStatus,
>       defaultRawProcessingStatus: decision.defaultRawProcessingStatus,
        requiresHumanReview: decision.requiresHumanReview,
        shouldCreateImportedPendingEvent:
          decision.shouldCreateImportedPendingEvent,
        shouldCreateDraftEvent: decision.shouldCreateDraftEvent,
        shouldCreateCompletedEvent: decision.shouldCreateCompletedEvent,
      },
    };
  }

LINE 233: status: ACTIVITY_RECORDING_ENABLED ? "ready" : "disabled",
      endpoint: ENDPOINT,
      method: "POST",
      enabled: ACTIVITY_RECORDING_ENABLED,
>     status: ACTIVITY_RECORDING_ENABLED ? "ready" : "disabled",
      message: ACTIVITY_RECORDING_ENABLED
        ? "API-ready raw activity intake endpoint. This endpoint captures raw signals only and does not create completed activity events automatically."
        : ACTIVITY_RECORDING_DISABLED_MESSAGE,
      example: {
        sourceType: "api_webhook",
        externalId: "external-event-123",
        occurredAt: new Date().toISOString(),
        payload: {

LINE 250: status: "P4.2.7",
        },
      },
      duplicateHandling: {
>       status: "P4.2.7",
        behavior:
          "Repeated external signals with the same sourceEventId or idempotencyKey return a controlled duplicate response instead of a server error.",
      },
      supportedSourceTypes: [
        "manual_chat",
        "manual_form",
        "voice_input",
        "app_action",

LINE 281: { status: 503 }
          ok: false,
          error: ACTIVITY_RECORDING_DISABLED_MESSAGE,
        },
>       { status: 503 }
      );
    }
  
    const { appUser, errorResponse } = await getActivityUserContext();
  
    if (errorResponse) {
      return errorResponse;
    }

LINE 297: { status: 500 }
          ok: false,
          error: "User context not found",
        },
>       { status: 500 }
      );
    }
  
    let body: ActivityIntakeBody;
  
    try {
      body = (await request.json()) as ActivityIntakeBody;
    } catch {

LINE 311: { status: 400 }
          ok: false,
          error: "Invalid JSON body",
        },
>       { status: 400 }
      );
    }
  
    const rawSourceType = normalizeRawActivitySignalSourceType(body.sourceType);
    const decision = decideActivityIntake({
      sourceType: rawSourceType,
    });
  

LINE 373: processingStatus: decision.defaultRawProcessingStatus,
      measuredAt,
      trustLevel,
      privacyScope,
>     processingStatus: decision.defaultRawProcessingStatus,
      metadata: {
        ...(asRecord(body.metadata)),
        endpoint: ENDPOINT,
        intakeVersion: "P4.2.7",
        activityEventSource: decision.activityEventSource,
        defaultActivityStatus: decision.defaultActivityStatus,
        requiresHumanReview: decision.requiresHumanReview,
        noActivityEventCreated: true,

LINE 379: defaultActivityStatus: decision.defaultActivityStatus,
        endpoint: ENDPOINT,
        intakeVersion: "P4.2.7",
        activityEventSource: decision.activityEventSource,
>       defaultActivityStatus: decision.defaultActivityStatus,
        requiresHumanReview: decision.requiresHumanReview,
        noActivityEventCreated: true,
      },
    });
  
    if (!rawSignalResult.ok || !rawSignalResult.signal) {
      try {
        const existingRawSignal = await findExistingRawSignal({

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: started
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: planned
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: pending
LABEL: contract evidence
------------------------------------------------------------

LINE 166: shouldCreateImportedPendingEvent:
        defaultActivityStatus: decision.defaultActivityStatus,
        defaultRawProcessingStatus: decision.defaultRawProcessingStatus,
        requiresHumanReview: decision.requiresHumanReview,
>       shouldCreateImportedPendingEvent:
          decision.shouldCreateImportedPendingEvent,
        shouldCreateDraftEvent: decision.shouldCreateDraftEvent,
        shouldCreateCompletedEvent: decision.shouldCreateCompletedEvent,
      },
    };
  }
  
  async function findExistingRawSignal(params: {

LINE 167: decision.shouldCreateImportedPendingEvent,
        defaultRawProcessingStatus: decision.defaultRawProcessingStatus,
        requiresHumanReview: decision.requiresHumanReview,
        shouldCreateImportedPendingEvent:
>         decision.shouldCreateImportedPendingEvent,
        shouldCreateDraftEvent: decision.shouldCreateDraftEvent,
        shouldCreateCompletedEvent: decision.shouldCreateCompletedEvent,
      },
    };
  }
  
  async function findExistingRawSignal(params: {
    userId: string;

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: completed
LABEL: contract evidence
------------------------------------------------------------

LINE 169: shouldCreateCompletedEvent: decision.shouldCreateCompletedEvent,
        shouldCreateImportedPendingEvent:
          decision.shouldCreateImportedPendingEvent,
        shouldCreateDraftEvent: decision.shouldCreateDraftEvent,
>       shouldCreateCompletedEvent: decision.shouldCreateCompletedEvent,
      },
    };
  }
  
  async function findExistingRawSignal(params: {
    userId: string;
    sourceType: RawActivitySignalSourceType;
    sourceEventId: string | null;

LINE 235: ? "API-ready raw activity intake endpoint. This endpoint captures raw signals only and does not create completed activity events automatically."
      enabled: ACTIVITY_RECORDING_ENABLED,
      status: ACTIVITY_RECORDING_ENABLED ? "ready" : "disabled",
      message: ACTIVITY_RECORDING_ENABLED
>       ? "API-ready raw activity intake endpoint. This endpoint captures raw signals only and does not create completed activity events automatically."
        : ACTIVITY_RECORDING_DISABLED_MESSAGE,
      example: {
        sourceType: "api_webhook",
        externalId: "external-event-123",
        occurredAt: new Date().toISOString(),
        payload: {
          title: "External activity signal",
          durationMinutes: 10,

LINE 270: "P4.2.7 stores raw_activity_signals only. Imported or external signals should be reviewed before becoming completed activities.",
        "unknown",
      ],
      note:
>       "P4.2.7 stores raw_activity_signals only. Imported or external signals should be reviewed before becoming completed activities.",
    });
  }
  
  export async function POST(request: Request) {
    if (!ACTIVITY_RECORDING_ENABLED) {
      return NextResponse.json(
        {
          ok: false,

LINE 411: "Duplicate raw activity signal was not inserted. Existing raw signal is returned. No completed activity event was created.",
            rawSignal: summarizeRawSignal(existingRawSignal.signal),
            activityEvent: null,
            note:
>             "Duplicate raw activity signal was not inserted. Existing raw signal is returned. No completed activity event was created.",
          });
        }
      } catch (error) {
        return NextResponse.json(
          {
            ok: false,
            error:
              error instanceof Error

LINE 451: "P4.2.7 stores the raw signal only. Imported/external signals are not completed activities until a later review or adapter flow creates or confirms an activity event.",
      rawSignal: summarizeRawSignal(rawSignal),
      activityEvent: null,
      note:
>       "P4.2.7 stores the raw signal only. Imported/external signals are not completed activities until a later review or adapter flow creates or confirms an activity event.",
    });
  }

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: .from("activity_events")
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: .insert({
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: .update({
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: processing_status
LABEL: contract evidence
------------------------------------------------------------

LINE 129: processingStatus: rawSignal.processing_status,
      idempotencyKey: rawSignal.idempotency_key,
      trustLevel: rawSignal.trust_level,
      privacyScope: rawSignal.privacy_scope,
>     processingStatus: rawSignal.processing_status,
      occurredAt: rawSignal.occurred_at,
      measuredAt: rawSignal.measured_at,
      outputEventId: rawSignal.output_event_id,
      createdAt: rawSignal.created_at,
    };
  }
  
  function buildNormalizedPreview(params: {

LINE 405: existingRawSignal.signal.processing_status,
              matchedBy: existingRawSignal.matchedBy,
              originalRawSignalId: existingRawSignal.signal.id,
              originalProcessingStatus:
>               existingRawSignal.signal.processing_status,
              insertError: rawSignalResult.error,
            },
            rawSignal: summarizeRawSignal(existingRawSignal.signal),
            activityEvent: null,
            note:
              "Duplicate raw activity signal was not inserted. Existing raw signal is returned. No completed activity event was created.",
          });
        }

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: performed_by_actor_id
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: acting_as_actor_id
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/intake/route.ts
PATTERN: input_text
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

############################################################
ROUTE FILE: src/app/api/activity/intake/events/route.ts
############################################################

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: type 
LABEL: contract evidence
------------------------------------------------------------

LINE 9: type GenericRow = Record<string, unknown>;
  import { getActivityUserContext } from "../../../../../../lib/activity/activityUserContext";
  import { supabase } from "../../../../../../lib/supabase";
  
> type GenericRow = Record<string, unknown>;
  
  
  export const dynamic = "force-dynamic";
  
  type ResponseMode = "summary" | "full";
  
  type ActivityEventQueueRow = Record<string, unknown>;
  

LINE 14: type ResponseMode = "summary" | "full";
  
  export const dynamic = "force-dynamic";
  
> type ResponseMode = "summary" | "full";
  
  type ActivityEventQueueRow = Record<string, unknown>;
  
  const ENDPOINT = "/api/activity/intake/events";
  
  const DEFAULT_EVENT_STATUSES = ["imported_pending"] as const;
  
  const ACTIVITY_EVENT_STATUS_VALUES = [

LINE 16: type ActivityEventQueueRow = Record<string, unknown>;
  
  type ResponseMode = "summary" | "full";
  
> type ActivityEventQueueRow = Record<string, unknown>;
  
  const ENDPOINT = "/api/activity/intake/events";
  
  const DEFAULT_EVENT_STATUSES = ["imported_pending"] as const;
  
  const ACTIVITY_EVENT_STATUS_VALUES = [
    "draft",
    "planned",

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: interface 
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: export async function POST
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: request.json
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: eventId
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: inputText
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: naturalInput
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: title
LABEL: contract evidence
------------------------------------------------------------

LINE 251: title: getFirstNullableString(row, ["title", "name"]),
  function summarizeActivityEvent(row: ActivityEventQueueRow) {
    return {
      id: getFirstString(row, ["id"]),
>     title: getFirstNullableString(row, ["title", "name"]),
      status: getFirstString(row, ["status"]),
      source: getFirstString(row, ["source"]),
      privacyScope: getFirstNullableString(row, ["privacy_scope", "privacyScope"]),
      processingStatus: getFirstNullableString(row, [
        "processing_status",
        "processingStatus",
      ]),
      startedAt: getFirstNullableString(row, ["started_at", "startedAt"]),

LINE 368: selectedTemplateTitle: p447StringFromRecord(mapping, "selectedTemplateTitle"),
      confidence: p447NumberFromRecord(mapping, "confidence"),
      reason: p447StringFromRecord(mapping, "reason"),
      selectedTemplateId: p447StringFromRecord(mapping, "selectedTemplateId"),
>     selectedTemplateTitle: p447StringFromRecord(mapping, "selectedTemplateTitle"),
      selectedTemplateSlug: p447StringFromRecord(mapping, "selectedTemplateSlug"),
      selectedActivityTypeId: p447StringFromRecord(mapping, "selectedActivityTypeId"),
      selectedLegacyTemplateId: p447StringFromRecord(mapping, "selectedLegacyTemplateId"),
      explicitActivityTemplateId: p447StringFromRecord(mapping, "explicitActivityTemplateId"),
      explicitLegacyTemplateId: p447StringFromRecord(mapping, "explicitLegacyTemplateId"),
      explicitActivityTypeId: p447StringFromRecord(mapping, "explicitActivityTypeId"),
      candidatesCount: p447NumberFromRecord(mapping, "candidatesCount"),
      searchTextPreview: p447StringFromRecord(mapping, "searchTextPreview"),

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: description
LABEL: contract evidence
------------------------------------------------------------

LINE 267: "description",
      ]),
      comment: getFirstNullableString(row, [
        "comment",
>       "description",
        "notes",
        "note",
      ]),
      activityTypeId: getFirstNullableString(row, [
        "activity_type_id",
        "activityTypeId",
      ]),
      activityTemplateId: getFirstNullableString(row, [

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: durationMinutes
LABEL: contract evidence
------------------------------------------------------------

LINE 261: durationMinutes: getFirstNumber(row, [
      ]),
      startedAt: getFirstNullableString(row, ["started_at", "startedAt"]),
      endedAt: getFirstNullableString(row, ["ended_at", "endedAt"]),
>     durationMinutes: getFirstNumber(row, [
        "duration_minutes",
        "durationMinutes",
      ]),
      comment: getFirstNullableString(row, [
        "comment",
        "description",
        "notes",
        "note",

LINE 263: "durationMinutes",
      endedAt: getFirstNullableString(row, ["ended_at", "endedAt"]),
      durationMinutes: getFirstNumber(row, [
        "duration_minutes",
>       "durationMinutes",
      ]),
      comment: getFirstNullableString(row, [
        "comment",
        "description",
        "notes",
        "note",
      ]),
      activityTypeId: getFirstNullableString(row, [

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: startedAt
LABEL: contract evidence
------------------------------------------------------------

LINE 259: startedAt: getFirstNullableString(row, ["started_at", "startedAt"]),
        "processing_status",
        "processingStatus",
      ]),
>     startedAt: getFirstNullableString(row, ["started_at", "startedAt"]),
      endedAt: getFirstNullableString(row, ["ended_at", "endedAt"]),
      durationMinutes: getFirstNumber(row, [
        "duration_minutes",
        "durationMinutes",
      ]),
      comment: getFirstNullableString(row, [
        "comment",
        "description",

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: endedAt
LABEL: contract evidence
------------------------------------------------------------

LINE 260: endedAt: getFirstNullableString(row, ["ended_at", "endedAt"]),
        "processingStatus",
      ]),
      startedAt: getFirstNullableString(row, ["started_at", "startedAt"]),
>     endedAt: getFirstNullableString(row, ["ended_at", "endedAt"]),
      durationMinutes: getFirstNumber(row, [
        "duration_minutes",
        "durationMinutes",
      ]),
      comment: getFirstNullableString(row, [
        "comment",
        "description",
        "notes",

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: status
LABEL: contract evidence
------------------------------------------------------------

LINE 20: const DEFAULT_EVENT_STATUSES = ["imported_pending"] as const;
  
  const ENDPOINT = "/api/activity/intake/events";
  
> const DEFAULT_EVENT_STATUSES = ["imported_pending"] as const;
  
  const ACTIVITY_EVENT_STATUS_VALUES = [
    "draft",
    "planned",
    "pending",
    "started",
    "paused",
    "imported_pending",

LINE 22: const ACTIVITY_EVENT_STATUS_VALUES = [
  
  const DEFAULT_EVENT_STATUSES = ["imported_pending"] as const;
  
> const ACTIVITY_EVENT_STATUS_VALUES = [
    "draft",
    "planned",
    "pending",
    "started",
    "paused",
    "imported_pending",
    "completed",
    "cancelled",

LINE 35: const ACTIVITY_EVENT_PROCESSING_STATUS_VALUES = [
    "missed",
  ] as const;
  
> const ACTIVITY_EVENT_PROCESSING_STATUS_VALUES = [
    "pending",
    "processing",
    "processed",
    "failed",
    "skipped",
  ] as const;
  
  const ACTIVITY_EVENT_SOURCE_VALUES = [

LINE 59: const ACTIVITY_EVENT_STATUS_SET = new Set<string>(
    "unknown",
  ] as const;
  
> const ACTIVITY_EVENT_STATUS_SET = new Set<string>(
    ACTIVITY_EVENT_STATUS_VALUES
  );
  
  const ACTIVITY_EVENT_PROCESSING_STATUS_SET = new Set<string>(
    ACTIVITY_EVENT_PROCESSING_STATUS_VALUES
  );
  
  const ACTIVITY_EVENT_SOURCE_SET = new Set<string>(ACTIVITY_EVENT_SOURCE_VALUES);

LINE 60: ACTIVITY_EVENT_STATUS_VALUES
  ] as const;
  
  const ACTIVITY_EVENT_STATUS_SET = new Set<string>(
>   ACTIVITY_EVENT_STATUS_VALUES
  );
  
  const ACTIVITY_EVENT_PROCESSING_STATUS_SET = new Set<string>(
    ACTIVITY_EVENT_PROCESSING_STATUS_VALUES
  );
  
  const ACTIVITY_EVENT_SOURCE_SET = new Set<string>(ACTIVITY_EVENT_SOURCE_VALUES);
  

LINE 63: const ACTIVITY_EVENT_PROCESSING_STATUS_SET = new Set<string>(
    ACTIVITY_EVENT_STATUS_VALUES
  );
  
> const ACTIVITY_EVENT_PROCESSING_STATUS_SET = new Set<string>(
    ACTIVITY_EVENT_PROCESSING_STATUS_VALUES
  );
  
  const ACTIVITY_EVENT_SOURCE_SET = new Set<string>(ACTIVITY_EVENT_SOURCE_VALUES);
  
  function asString(value: unknown): string | null {
    if (typeof value !== "string") {
      return null;

LINE 64: ACTIVITY_EVENT_PROCESSING_STATUS_VALUES
  );
  
  const ACTIVITY_EVENT_PROCESSING_STATUS_SET = new Set<string>(
>   ACTIVITY_EVENT_PROCESSING_STATUS_VALUES
  );
  
  const ACTIVITY_EVENT_SOURCE_SET = new Set<string>(ACTIVITY_EVENT_SOURCE_VALUES);
  
  function asString(value: unknown): string | null {
    if (typeof value !== "string") {
      return null;
    }

LINE 169: function parseEventStatuses(
      .filter(Boolean);
  }
  
> function parseEventStatuses(
    searchParams: URLSearchParams
  ): string[] | "all" | { error: string } {
    const rawValues = splitParamValues([
      ...searchParams.getAll("status"),
      ...searchParams.getAll("statuses"),
    ]);
  
    if (rawValues.length === 0) {

LINE 173: ...searchParams.getAll("status"),
    searchParams: URLSearchParams
  ): string[] | "all" | { error: string } {
    const rawValues = splitParamValues([
>     ...searchParams.getAll("status"),
      ...searchParams.getAll("statuses"),
    ]);
  
    if (rawValues.length === 0) {
      return [...DEFAULT_EVENT_STATUSES];
    }
  
    if (rawValues.includes("all")) {

LINE 174: ...searchParams.getAll("statuses"),
  ): string[] | "all" | { error: string } {
    const rawValues = splitParamValues([
      ...searchParams.getAll("status"),
>     ...searchParams.getAll("statuses"),
    ]);
  
    if (rawValues.length === 0) {
      return [...DEFAULT_EVENT_STATUSES];
    }
  
    if (rawValues.includes("all")) {
      return "all";

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: started
LABEL: contract evidence
------------------------------------------------------------

LINE 26: "started",
    "draft",
    "planned",
    "pending",
>   "started",
    "paused",
    "imported_pending",
    "completed",
    "cancelled",
    "archived",
    "missed",
  ] as const;
  

LINE 259: startedAt: getFirstNullableString(row, ["started_at", "startedAt"]),
        "processing_status",
        "processingStatus",
      ]),
>     startedAt: getFirstNullableString(row, ["started_at", "startedAt"]),
      endedAt: getFirstNullableString(row, ["ended_at", "endedAt"]),
      durationMinutes: getFirstNumber(row, [
        "duration_minutes",
        "durationMinutes",
      ]),
      comment: getFirstNullableString(row, [
        "comment",
        "description",

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: planned
LABEL: contract evidence
------------------------------------------------------------

LINE 24: "planned",
  
  const ACTIVITY_EVENT_STATUS_VALUES = [
    "draft",
>   "planned",
    "pending",
    "started",
    "paused",
    "imported_pending",
    "completed",
    "cancelled",
    "archived",
    "missed",

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: pending
LABEL: contract evidence
------------------------------------------------------------

LINE 20: const DEFAULT_EVENT_STATUSES = ["imported_pending"] as const;
  
  const ENDPOINT = "/api/activity/intake/events";
  
> const DEFAULT_EVENT_STATUSES = ["imported_pending"] as const;
  
  const ACTIVITY_EVENT_STATUS_VALUES = [
    "draft",
    "planned",
    "pending",
    "started",
    "paused",
    "imported_pending",

LINE 25: "pending",
  const ACTIVITY_EVENT_STATUS_VALUES = [
    "draft",
    "planned",
>   "pending",
    "started",
    "paused",
    "imported_pending",
    "completed",
    "cancelled",
    "archived",
    "missed",
  ] as const;

LINE 28: "imported_pending",
    "pending",
    "started",
    "paused",
>   "imported_pending",
    "completed",
    "cancelled",
    "archived",
    "missed",
  ] as const;
  
  const ACTIVITY_EVENT_PROCESSING_STATUS_VALUES = [
    "pending",

LINE 36: "pending",
  ] as const;
  
  const ACTIVITY_EVENT_PROCESSING_STATUS_VALUES = [
>   "pending",
    "processing",
    "processed",
    "failed",
    "skipped",
  ] as const;
  
  const ACTIVITY_EVENT_SOURCE_VALUES = [
    "manual_chat",

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: completed
LABEL: contract evidence
------------------------------------------------------------

LINE 29: "completed",
    "started",
    "paused",
    "imported_pending",
>   "completed",
    "cancelled",
    "archived",
    "missed",
  ] as const;
  
  const ACTIVITY_EVENT_PROCESSING_STATUS_VALUES = [
    "pending",
    "processing",

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: .from("activity_events")
LABEL: contract evidence
------------------------------------------------------------

LINE 457: .from("activity_events")
  
    try {
      let query = supabase
>       .from("activity_events")
        .select("*")
        .eq("user_id", appUser.id)
        .order("created_at", { ascending: false })
        .limit(limit);
  
      if (statuses !== "all") {
        query = query.in("status", statuses);
      }

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: .insert({
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: .update({
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: processing_status
LABEL: contract evidence
------------------------------------------------------------

LINE 35: const ACTIVITY_EVENT_PROCESSING_STATUS_VALUES = [
    "missed",
  ] as const;
  
> const ACTIVITY_EVENT_PROCESSING_STATUS_VALUES = [
    "pending",
    "processing",
    "processed",
    "failed",
    "skipped",
  ] as const;
  
  const ACTIVITY_EVENT_SOURCE_VALUES = [

LINE 63: const ACTIVITY_EVENT_PROCESSING_STATUS_SET = new Set<string>(
    ACTIVITY_EVENT_STATUS_VALUES
  );
  
> const ACTIVITY_EVENT_PROCESSING_STATUS_SET = new Set<string>(
    ACTIVITY_EVENT_PROCESSING_STATUS_VALUES
  );
  
  const ACTIVITY_EVENT_SOURCE_SET = new Set<string>(ACTIVITY_EVENT_SOURCE_VALUES);
  
  function asString(value: unknown): string | null {
    if (typeof value !== "string") {
      return null;

LINE 64: ACTIVITY_EVENT_PROCESSING_STATUS_VALUES
  );
  
  const ACTIVITY_EVENT_PROCESSING_STATUS_SET = new Set<string>(
>   ACTIVITY_EVENT_PROCESSING_STATUS_VALUES
  );
  
  const ACTIVITY_EVENT_SOURCE_SET = new Set<string>(ACTIVITY_EVENT_SOURCE_VALUES);
  
  function asString(value: unknown): string | null {
    if (typeof value !== "string") {
      return null;
    }

LINE 211: (value) => !ACTIVITY_EVENT_PROCESSING_STATUS_SET.has(value)
    }
  
    const invalidValues = rawValues.filter(
>     (value) => !ACTIVITY_EVENT_PROCESSING_STATUS_SET.has(value)
    );
  
    if (invalidValues.length > 0) {
      return {
        error: `Unsupported processingStatus value: ${invalidValues.join(", ")}`,
      };
    }
  

LINE 256: "processing_status",
      source: getFirstString(row, ["source"]),
      privacyScope: getFirstNullableString(row, ["privacy_scope", "privacyScope"]),
      processingStatus: getFirstNullableString(row, [
>       "processing_status",
        "processingStatus",
      ]),
      startedAt: getFirstNullableString(row, ["started_at", "startedAt"]),
      endedAt: getFirstNullableString(row, ["ended_at", "endedAt"]),
      durationMinutes: getFirstNumber(row, [
        "duration_minutes",
        "durationMinutes",
      ]),

LINE 435: supportedProcessingStatuses: ACTIVITY_EVENT_PROCESSING_STATUS_VALUES,
          ok: false,
          endpoint: ENDPOINT,
          error: processingStatuses.error,
>         supportedProcessingStatuses: ACTIVITY_EVENT_PROCESSING_STATUS_VALUES,
        },
        { status: 400 }
      );
    }
  
    const sources = parseSources(url.searchParams);
  
    if (typeof sources === "object" && "error" in sources) {

LINE 468: query = query.in("processing_status", processingStatuses);
      }
  
      if (processingStatuses !== "all") {
>       query = query.in("processing_status", processingStatuses);
      }
  
      if (sources !== "all") {
        query = query.in("source", sources);
      }
  
      const { data, error } = await query;
  

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: performed_by_actor_id
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: acting_as_actor_id
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/intake/events/route.ts
PATTERN: input_text
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

############################################################
ROUTE FILE: src/app/api/activity/complete/route.ts
############################################################

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: type 
LABEL: contract evidence
------------------------------------------------------------

LINE 23: type CategoryResolverCreatePolicy,
  import { deriveCategoryCandidates } from "../../../../../lib/activity/categoryDerivation/ruleExtractor";
  import {
    resolveCategoryCandidates,
>   type CategoryResolverCreatePolicy,
    type CategoryResolverSupabaseClient,
  } from "../../../../../lib/activity/categoryDerivation/resolver";
  import {
    persistCategoryDerivations,
    type CategoryDerivationPersistenceSupabaseClient,
  } from "../../../../../lib/activity/categoryDerivation/persistDerivations";
  import type { CategoryDerivationInput } from "../../../../../lib/activity/categoryDerivation/types";
  import type { AdditionalValueObjectCategoryLink } from "../../../../../lib/activity/valueObjectBridge";

LINE 24: type CategoryResolverSupabaseClient,
  import {
    resolveCategoryCandidates,
    type CategoryResolverCreatePolicy,
>   type CategoryResolverSupabaseClient,
  } from "../../../../../lib/activity/categoryDerivation/resolver";
  import {
    persistCategoryDerivations,
    type CategoryDerivationPersistenceSupabaseClient,
  } from "../../../../../lib/activity/categoryDerivation/persistDerivations";
  import type { CategoryDerivationInput } from "../../../../../lib/activity/categoryDerivation/types";
  import type { AdditionalValueObjectCategoryLink } from "../../../../../lib/activity/valueObjectBridge";
  import { ensureActivityEventRubricatorClassificationForKnownTemplate } from "../../../../../lib/activity/activityRubricatorClassificationLifecycle";

LINE 28: type CategoryDerivationPersistenceSupabaseClient,
  } from "../../../../../lib/activity/categoryDerivation/resolver";
  import {
    persistCategoryDerivations,
>   type CategoryDerivationPersistenceSupabaseClient,
  } from "../../../../../lib/activity/categoryDerivation/persistDerivations";
  import type { CategoryDerivationInput } from "../../../../../lib/activity/categoryDerivation/types";
  import type { AdditionalValueObjectCategoryLink } from "../../../../../lib/activity/valueObjectBridge";
  import { ensureActivityEventRubricatorClassificationForKnownTemplate } from "../../../../../lib/activity/activityRubricatorClassificationLifecycle";
  import { buildRubricatorResolverLogMetadata } from "../../../../../lib/activity/rubricatorResolverLogMetadata";
  import {
    createRawActivitySignal,
    markRawActivitySignalFailed,

LINE 30: import type { CategoryDerivationInput } from "../../../../../lib/activity/categoryDerivation/types";
    persistCategoryDerivations,
    type CategoryDerivationPersistenceSupabaseClient,
  } from "../../../../../lib/activity/categoryDerivation/persistDerivations";
> import type { CategoryDerivationInput } from "../../../../../lib/activity/categoryDerivation/types";
  import type { AdditionalValueObjectCategoryLink } from "../../../../../lib/activity/valueObjectBridge";
  import { ensureActivityEventRubricatorClassificationForKnownTemplate } from "../../../../../lib/activity/activityRubricatorClassificationLifecycle";
  import { buildRubricatorResolverLogMetadata } from "../../../../../lib/activity/rubricatorResolverLogMetadata";
  import {
    createRawActivitySignal,
    markRawActivitySignalFailed,
    markRawActivitySignalProcessed,
  } from "../../../../../lib/activity/rawActivitySignals";

LINE 31: import type { AdditionalValueObjectCategoryLink } from "../../../../../lib/activity/valueObjectBridge";
    type CategoryDerivationPersistenceSupabaseClient,
  } from "../../../../../lib/activity/categoryDerivation/persistDerivations";
  import type { CategoryDerivationInput } from "../../../../../lib/activity/categoryDerivation/types";
> import type { AdditionalValueObjectCategoryLink } from "../../../../../lib/activity/valueObjectBridge";
  import { ensureActivityEventRubricatorClassificationForKnownTemplate } from "../../../../../lib/activity/activityRubricatorClassificationLifecycle";
  import { buildRubricatorResolverLogMetadata } from "../../../../../lib/activity/rubricatorResolverLogMetadata";
  import {
    createRawActivitySignal,
    markRawActivitySignalFailed,
    markRawActivitySignalProcessed,
  } from "../../../../../lib/activity/rawActivitySignals";
  import { supabase } from "../../../../../lib/supabase";

LINE 43: type ActivityCompleteBody = {
  
  export const dynamic = "force-dynamic";
  
> type ActivityCompleteBody = {
    eventId?: unknown;
    endedAt?: unknown;
    endTime?: unknown;
    durationMinutes?: unknown;
    comment?: unknown;
  };
  
  type ActivityEventRow = {

LINE 51: type ActivityEventRow = {
    comment?: unknown;
  };
  
> type ActivityEventRow = {
    id: string;
    user_id: string;
    performed_by_actor_id: string | null;
    acting_as_actor_id: string | null;
    acting_for_actor_id: string | null;
    activity_type_id: string | null;
    activity_template_id: string | null;
    template_id: string | null;

LINE 76: type CompletionTiming =
    updated_at: string;
  };
  
> type CompletionTiming =
    | {
        ok: true;
        startedAt: string;
        endedAt: string;
        durationMinutes: number;
      }
    | {
        ok: false;

LINE 92: type CategoryDerivationCompleteRoutePolicy = {
  const CATEGORY_DERIVATION_COMPLETE_ROUTE_CREATE_POLICY: CategoryResolverCreatePolicy =
    "suggested_only";
  
> type CategoryDerivationCompleteRoutePolicy = {
    enabled: boolean;
    reason: string | null;
    inputText: string | null;
    actorId: string | null;
  };
  
  type CategoryDerivationCompleteRouteResult = {
    enabled: boolean;

LINE 99: type CategoryDerivationCompleteRouteResult = {
    actorId: string | null;
  };
  
> type CategoryDerivationCompleteRouteResult = {
    enabled: boolean;
    ok: boolean | null;
    skipped: boolean;
    reason: string | null;
    error: string | null;
    derivationRunId: string | null;
    extractionCandidateCount: number;
    resolutionCandidateCount: number;

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: interface 
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: export async function POST
LABEL: contract evidence
------------------------------------------------------------

LINE 734: export async function POST(request: Request) {
    });
  }
  
> export async function POST(request: Request) {
    if (!ACTIVITY_RECORDING_ENABLED) {
      return NextResponse.json(
        {
          ok: false,
          error: ACTIVITY_RECORDING_DISABLED_MESSAGE,
        },
        { status: 503 }
      );

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: request.json
LABEL: contract evidence
------------------------------------------------------------

LINE 764: body = (await request.json()) as ActivityCompleteBody;
    let body: ActivityCompleteBody;
  
    try {
>     body = (await request.json()) as ActivityCompleteBody;
    } catch {
      return NextResponse.json(
        {
          ok: false,
          error: "Invalid JSON body",
        },
        { status: 400 }
      );

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: eventId
LABEL: contract evidence
------------------------------------------------------------

LINE 44: eventId?: unknown;
  export const dynamic = "force-dynamic";
  
  type ActivityCompleteBody = {
>   eventId?: unknown;
    endedAt?: unknown;
    endTime?: unknown;
    durationMinutes?: unknown;
    comment?: unknown;
  };
  
  type ActivityEventRow = {
    id: string;

LINE 336: activityEventId: string;
  
  function buildAdditionalCategoryLinksForCompleteRoute(params: {
    enabled: boolean;
>   activityEventId: string;
    derivationRunId: string | null;
    resolutionResult: unknown;
  }): AdditionalValueObjectCategoryLink[] {
    const { enabled, activityEventId, derivationRunId, resolutionResult } = params;
  
    if (!enabled) {
      return [];
    }

LINE 340: const { enabled, activityEventId, derivationRunId, resolutionResult } = params;
    derivationRunId: string | null;
    resolutionResult: unknown;
  }): AdditionalValueObjectCategoryLink[] {
>   const { enabled, activityEventId, derivationRunId, resolutionResult } = params;
  
    if (!enabled) {
      return [];
    }
  
    const resolutionRecord = asRecordForCategoryDerivation(resolutionResult);
    const possibleCandidates = resolutionRecord.candidates;
    const resolvedCandidates = Array.isArray(possibleCandidates)

LINE 402: activityEventId,
        ]),
        derivationRunId,
        activityCategoryDerivationId: null,
>       activityEventId,
        candidateSlug,
        candidateTitle: readStringFieldForCategoryDerivation(candidate, [
          "candidateTitle",
          "candidate_title",
          "title",
          "label",
          "name",
        ]),

LINE 450: activityEventId: event.id,
      const { event } = params;
  
      const derivationInput: CategoryDerivationInput = {
>       activityEventId: event.id,
        inputText: policy.inputText,
        title: event.title,
        description: event.description,
        durationMinutes: event.duration_minutes ?? 0,
        inputLanguage: null,
        actorId: policy.actorId,
        organizationId: null,
        metadata: {

LINE 481: activityEventId: event.id,
      const persistenceResult = await persistCategoryDerivations(
        supabase as unknown as CategoryDerivationPersistenceSupabaseClient,
        {
>         activityEventId: event.id,
          input: derivationInput,
          derivationResult: extractionResult,
          resolvedCandidates: resolutionResult.candidates,
          actorId: policy.actorId,
          organizationId: null,
          modelName: null,
          promptVersion: null,
          needsUserConfirmation:

LINE 512: activityEventId: event.id,
  
      const additionalCategoryLinks = buildAdditionalCategoryLinksForCompleteRoute({
        enabled: true,
>       activityEventId: event.id,
        derivationRunId,
        resolutionResult,
      });
  
      const warnings = [
        ...extractionResult.warnings,
        ...resolutionResult.warnings,
        ...readStringArrayFieldForCategoryDerivation(persistenceResult, ["warnings"]),

LINE 696: async function getExistingImpactEventsCount(eventId: string) {
    };
  }
  
> async function getExistingImpactEventsCount(eventId: string) {
    const { count, error } = await supabase
      .from("impact_events")
      .select("id", {
        count: "exact",
        head: true,
      })
      .eq("event_id", eventId);
  

LINE 703: .eq("event_id", eventId);
        count: "exact",
        head: true,
      })
>     .eq("event_id", eventId);
  
    if (error) {
      throw new Error(error.message);
    }
  
    return count ?? 0;
  }
  

LINE 723: eventId: "activity-event-uuid",
        ? "Complete a previously started activity event and process rule-based impacts."
        : ACTIVITY_RECORDING_DISABLED_MESSAGE,
      example: {
>       eventId: "activity-event-uuid",
        comment: "Completed lifecycle smoke test",
      },
      deterministicTestExample: {
        eventId: "activity-event-uuid",
        durationMinutes: 5,
        comment: "Completed lifecycle smoke test with fixed duration",
      },
    });

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: inputText
LABEL: contract evidence
------------------------------------------------------------

LINE 95: inputText: string | null;
  type CategoryDerivationCompleteRoutePolicy = {
    enabled: boolean;
    reason: string | null;
>   inputText: string | null;
    actorId: string | null;
  };
  
  type CategoryDerivationCompleteRouteResult = {
    enabled: boolean;
    ok: boolean | null;
    skipped: boolean;
    reason: string | null;

LINE 237: function getCategoryDerivationInputTextForCompleteRoute(
    );
  }
  
> function getCategoryDerivationInputTextForCompleteRoute(
    event: ActivityEventRow
  ): string | null {
    return (
      asStringForCategoryDerivation(event.input_text) ??
      asStringForCategoryDerivation(event.title) ??
      asStringForCategoryDerivation(event.description)
    );
  }

LINE 287: inputText: null,
      return {
        enabled: false,
        reason: "not_completed_status",
>       inputText: null,
        actorId: null,
      };
    }
  
    if (!isCategoryDerivationSourceAllowedForCompleteRoute(event.source)) {
      return {
        enabled: false,
        reason: "source_not_allowed",

LINE 296: inputText: null,
      return {
        enabled: false,
        reason: "source_not_allowed",
>       inputText: null,
        actorId: null,
      };
    }
  
    const inputText = getCategoryDerivationInputTextForCompleteRoute(event);
  
    if (inputText === null) {
      return {

LINE 301: const inputText = getCategoryDerivationInputTextForCompleteRoute(event);
      };
    }
  
>   const inputText = getCategoryDerivationInputTextForCompleteRoute(event);
  
    if (inputText === null) {
      return {
        enabled: false,
        reason: "missing_textual_input",
        inputText: null,
        actorId: null,
      };

LINE 303: if (inputText === null) {
  
    const inputText = getCategoryDerivationInputTextForCompleteRoute(event);
  
>   if (inputText === null) {
      return {
        enabled: false,
        reason: "missing_textual_input",
        inputText: null,
        actorId: null,
      };
    }
  

LINE 307: inputText: null,
      return {
        enabled: false,
        reason: "missing_textual_input",
>       inputText: null,
        actorId: null,
      };
    }
  
    const actorId =
      event.performed_by_actor_id ??
      event.acting_as_actor_id ??
      event.acting_for_actor_id;

LINE 321: inputText,
      return {
        enabled: false,
        reason: "missing_actor_id",
>       inputText,
        actorId: null,
      };
    }
  
    return {
      enabled: true,
      reason: null,
      inputText,

LINE 329: inputText,
    return {
      enabled: true,
      reason: null,
>     inputText,
      actorId,
    };
  }
  
  function buildAdditionalCategoryLinksForCompleteRoute(params: {
    enabled: boolean;
    activityEventId: string;
    derivationRunId: string | null;

LINE 427: if (!policy.enabled || policy.inputText === null || policy.actorId === null) {
  }): Promise<CategoryDerivationCompleteRouteResult> {
    const policy = getCategoryDerivationPolicyForCompleteRoute(params);
  
>   if (!policy.enabled || policy.inputText === null || policy.actorId === null) {
      return {
        enabled: false,
        ok: null,
        skipped: true,
        reason: policy.reason,
        error: null,
        derivationRunId: null,
        extractionCandidateCount: 0,

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: naturalInput
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: title
LABEL: contract evidence
------------------------------------------------------------

LINE 62: title: string | null;
    template_id: string | null;
    event_code: string | null;
    input_text: string | null;
>   title: string | null;
    description: string | null;
    started_at: string | null;
    ended_at: string | null;
    duration_minutes: number | null;
    source: string | null;
    status: string;
    privacy_scope: string | null;
    processing_status: string | null;

LINE 242: asStringForCategoryDerivation(event.title) ??
  ): string | null {
    return (
      asStringForCategoryDerivation(event.input_text) ??
>     asStringForCategoryDerivation(event.title) ??
      asStringForCategoryDerivation(event.description)
    );
  }
  
  function isCategoryDerivationSourceAllowedForCompleteRoute(
    source: string | null
  ): boolean {
    const normalizedSource = asStringForCategoryDerivation(source)?.toLowerCase() ?? null;

LINE 404: candidateTitle: readStringFieldForCategoryDerivation(candidate, [
        activityCategoryDerivationId: null,
        activityEventId,
        candidateSlug,
>       candidateTitle: readStringFieldForCategoryDerivation(candidate, [
          "candidateTitle",
          "candidate_title",
          "title",
          "label",
          "name",
        ]),
        semanticLayer: readStringFieldForCategoryDerivation(candidate, [
          "semanticLayer",

LINE 405: "candidateTitle",
        activityEventId,
        candidateSlug,
        candidateTitle: readStringFieldForCategoryDerivation(candidate, [
>         "candidateTitle",
          "candidate_title",
          "title",
          "label",
          "name",
        ]),
        semanticLayer: readStringFieldForCategoryDerivation(candidate, [
          "semanticLayer",
          "semantic_layer",

LINE 406: "candidate_title",
        candidateSlug,
        candidateTitle: readStringFieldForCategoryDerivation(candidate, [
          "candidateTitle",
>         "candidate_title",
          "title",
          "label",
          "name",
        ]),
        semanticLayer: readStringFieldForCategoryDerivation(candidate, [
          "semanticLayer",
          "semantic_layer",
        ]),

LINE 407: "title",
        candidateTitle: readStringFieldForCategoryDerivation(candidate, [
          "candidateTitle",
          "candidate_title",
>         "title",
          "label",
          "name",
        ]),
        semanticLayer: readStringFieldForCategoryDerivation(candidate, [
          "semanticLayer",
          "semantic_layer",
        ]),
      });

LINE 452: title: event.title,
      const derivationInput: CategoryDerivationInput = {
        activityEventId: event.id,
        inputText: policy.inputText,
>       title: event.title,
        description: event.description,
        durationMinutes: event.duration_minutes ?? 0,
        inputLanguage: null,
        actorId: policy.actorId,
        organizationId: null,
        metadata: {
          endpoint: "/api/activity/complete",
          p4Step: "P4.10.0-C8-P3-B7-C1-B",

LINE 913: title: event.title,
      },
      normalizedPreview: {
        activityEventId: event.id,
>       title: event.title,
        previousStatus: event.status,
        nextStatus: completedStatus,
        startedAt: timing.startedAt,
        endedAt: timing.endedAt,
        durationMinutes: timing.durationMinutes,
      },
      occurredAt: timing.endedAt,
      trustLevel: "medium",

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: description
LABEL: contract evidence
------------------------------------------------------------

LINE 63: description: string | null;
    event_code: string | null;
    input_text: string | null;
    title: string | null;
>   description: string | null;
    started_at: string | null;
    ended_at: string | null;
    duration_minutes: number | null;
    source: string | null;
    status: string;
    privacy_scope: string | null;
    processing_status: string | null;
    metadata_json: Record<string, unknown> | null;

LINE 243: asStringForCategoryDerivation(event.description)
    return (
      asStringForCategoryDerivation(event.input_text) ??
      asStringForCategoryDerivation(event.title) ??
>     asStringForCategoryDerivation(event.description)
    );
  }
  
  function isCategoryDerivationSourceAllowedForCompleteRoute(
    source: string | null
  ): boolean {
    const normalizedSource = asStringForCategoryDerivation(source)?.toLowerCase() ?? null;
  

LINE 453: description: event.description,
        activityEventId: event.id,
        inputText: policy.inputText,
        title: event.title,
>       description: event.description,
        durationMinutes: event.duration_minutes ?? 0,
        inputLanguage: null,
        actorId: policy.actorId,
        organizationId: null,
        metadata: {
          endpoint: "/api/activity/complete",
          p4Step: "P4.10.0-C8-P3-B7-C1-B",
          featureFlag: "categoryDerivation",

LINE 986: description: comment ?? event.description,
        duration_minutes: timing.durationMinutes,
        status: completedStatus,
        processing_status: "processed",
>       description: comment ?? event.description,
        metadata_json: {
          ...existingMetadata,
          lifecycle: completedStatus,
          lifecycle_completed_at: nowIso,
          previous_status: event.status,
          completion_comment: comment,
          completion_duration_source:
            asNumber(body.durationMinutes) !== null

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: durationMinutes
LABEL: contract evidence
------------------------------------------------------------

LINE 47: durationMinutes?: unknown;
    eventId?: unknown;
    endedAt?: unknown;
    endTime?: unknown;
>   durationMinutes?: unknown;
    comment?: unknown;
  };
  
  type ActivityEventRow = {
    id: string;
    user_id: string;
    performed_by_actor_id: string | null;
    acting_as_actor_id: string | null;

LINE 81: durationMinutes: number;
        ok: true;
        startedAt: string;
        endedAt: string;
>       durationMinutes: number;
      }
    | {
        ok: false;
        error: string;
      };
  
  
  const CATEGORY_DERIVATION_COMPLETE_ROUTE_CREATE_POLICY: CategoryResolverCreatePolicy =

LINE 454: durationMinutes: event.duration_minutes ?? 0,
        inputText: policy.inputText,
        title: event.title,
        description: event.description,
>       durationMinutes: event.duration_minutes ?? 0,
        inputLanguage: null,
        actorId: policy.actorId,
        organizationId: null,
        metadata: {
          endpoint: "/api/activity/complete",
          p4Step: "P4.10.0-C8-P3-B7-C1-B",
          featureFlag: "categoryDerivation",
        },

LINE 628: const explicitDurationMinutes = asNumber(body.durationMinutes);
    }
  
    const rawEndedAt = asString(body.endedAt) ?? asString(body.endTime);
>   const explicitDurationMinutes = asNumber(body.durationMinutes);
  
    if (explicitDurationMinutes !== null && explicitDurationMinutes < 0) {
      return {
        ok: false,
        error: "durationMinutes must be greater than or equal to 0.",
      };
    }
  

LINE 630: if (explicitDurationMinutes !== null && explicitDurationMinutes < 0) {
    const rawEndedAt = asString(body.endedAt) ?? asString(body.endTime);
    const explicitDurationMinutes = asNumber(body.durationMinutes);
  
>   if (explicitDurationMinutes !== null && explicitDurationMinutes < 0) {
      return {
        ok: false,
        error: "durationMinutes must be greater than or equal to 0.",
      };
    }
  
    if (rawEndedAt) {
      const endedDate = new Date(rawEndedAt);

LINE 633: error: "durationMinutes must be greater than or equal to 0.",
    if (explicitDurationMinutes !== null && explicitDurationMinutes < 0) {
      return {
        ok: false,
>       error: "durationMinutes must be greater than or equal to 0.",
      };
    }
  
    if (rawEndedAt) {
      const endedDate = new Date(rawEndedAt);
  
      if (Number.isNaN(endedDate.getTime())) {
        return {

LINE 658: durationMinutes: Math.round(
        ok: true,
        startedAt: startedDate.toISOString(),
        endedAt: endedDate.toISOString(),
>       durationMinutes: Math.round(
          (endedDate.getTime() - startedDate.getTime()) / 60000
        ),
      };
    }
  
    if (explicitDurationMinutes !== null) {
      const endedDate = new Date(
        startedDate.getTime() + explicitDurationMinutes * 60000

LINE 664: if (explicitDurationMinutes !== null) {
      };
    }
  
>   if (explicitDurationMinutes !== null) {
      const endedDate = new Date(
        startedDate.getTime() + explicitDurationMinutes * 60000
      );
  
      return {
        ok: true,
        startedAt: startedDate.toISOString(),
        endedAt: endedDate.toISOString(),

LINE 666: startedDate.getTime() + explicitDurationMinutes * 60000
  
    if (explicitDurationMinutes !== null) {
      const endedDate = new Date(
>       startedDate.getTime() + explicitDurationMinutes * 60000
      );
  
      return {
        ok: true,
        startedAt: startedDate.toISOString(),
        endedAt: endedDate.toISOString(),
        durationMinutes: explicitDurationMinutes,
      };

LINE 673: durationMinutes: explicitDurationMinutes,
        ok: true,
        startedAt: startedDate.toISOString(),
        endedAt: endedDate.toISOString(),
>       durationMinutes: explicitDurationMinutes,
      };
    }
  
    const endedDate = new Date();
  
    if (endedDate.getTime() < startedDate.getTime()) {
      return {
        ok: false,

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: startedAt
LABEL: contract evidence
------------------------------------------------------------

LINE 79: startedAt: string;
  type CompletionTiming =
    | {
        ok: true;
>       startedAt: string;
        endedAt: string;
        durationMinutes: number;
      }
    | {
        ok: false;
        error: string;
      };
  

LINE 650: error: "endedAt must be greater than or equal to startedAt.",
      if (endedDate.getTime() < startedDate.getTime()) {
        return {
          ok: false,
>         error: "endedAt must be greater than or equal to startedAt.",
        };
      }
  
      return {
        ok: true,
        startedAt: startedDate.toISOString(),
        endedAt: endedDate.toISOString(),
        durationMinutes: Math.round(

LINE 656: startedAt: startedDate.toISOString(),
  
      return {
        ok: true,
>       startedAt: startedDate.toISOString(),
        endedAt: endedDate.toISOString(),
        durationMinutes: Math.round(
          (endedDate.getTime() - startedDate.getTime()) / 60000
        ),
      };
    }
  
    if (explicitDurationMinutes !== null) {

LINE 671: startedAt: startedDate.toISOString(),
  
      return {
        ok: true,
>       startedAt: startedDate.toISOString(),
        endedAt: endedDate.toISOString(),
        durationMinutes: explicitDurationMinutes,
      };
    }
  
    const endedDate = new Date();
  
    if (endedDate.getTime() < startedDate.getTime()) {

LINE 682: error: "Current time is earlier than startedAt.",
    if (endedDate.getTime() < startedDate.getTime()) {
      return {
        ok: false,
>       error: "Current time is earlier than startedAt.",
      };
    }
  
    return {
      ok: true,
      startedAt: startedDate.toISOString(),
      endedAt: endedDate.toISOString(),
      durationMinutes: Math.round(

LINE 688: startedAt: startedDate.toISOString(),
  
    return {
      ok: true,
>     startedAt: startedDate.toISOString(),
      endedAt: endedDate.toISOString(),
      durationMinutes: Math.round(
        (endedDate.getTime() - startedDate.getTime()) / 60000
      ),
    };
  }
  
  async function getExistingImpactEventsCount(eventId: string) {

LINE 829: startedAt: event.started_at,
          activityTemplateId: event.activity_template_id,
          activityTypeId: event.activity_type_id,
          durationMinutes: event.duration_minutes,
>         startedAt: event.started_at,
        });
  
        return NextResponse.json({
          ok: true,
          status: "already_completed",
          event,
          impactEvents: impactResult.impactEvents,
          dailyAggregates: impactResult.dailyAggregates,

LINE 897: const processingStartedAt = new Date();
    const nowIso = new Date().toISOString();
  
    const processingRunId = randomUUID();
>   const processingStartedAt = new Date();
  
    const rawSignalResult = await createRawActivitySignal({
      userId: appUser.id,
      sourceType: "manual_form",
      sourceEventId: event.id,
      idempotencyKey: `${event.id}:complete:${timing.endedAt}:${timing.durationMinutes}`,
      rawPayload: {
        endpoint: "/api/activity/complete",

LINE 916: startedAt: timing.startedAt,
        title: event.title,
        previousStatus: event.status,
        nextStatus: completedStatus,
>       startedAt: timing.startedAt,
        endedAt: timing.endedAt,
        durationMinutes: timing.durationMinutes,
      },
      occurredAt: timing.endedAt,
      trustLevel: "medium",
      privacyScope:
        event.privacy_scope === "shared_with_org" ||
        event.privacy_scope === "public_masked" ||

LINE 974: startedAt: processingStartedAt.toISOString(),
        endpoint: "/api/activity/complete",
        mode: "template_first_complete",
      },
>     startedAt: processingStartedAt.toISOString(),
      finishedAt: new Date().toISOString(),
      durationMs: getDurationMs(processingStartedAt),
    });
  
    const { data: updatedEventData, error: updateError } = await supabase
      .from("activity_events")
      .update({
        ended_at: timing.endedAt,

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: endedAt
LABEL: contract evidence
------------------------------------------------------------

LINE 45: endedAt?: unknown;
  
  type ActivityCompleteBody = {
    eventId?: unknown;
>   endedAt?: unknown;
    endTime?: unknown;
    durationMinutes?: unknown;
    comment?: unknown;
  };
  
  type ActivityEventRow = {
    id: string;
    user_id: string;

LINE 80: endedAt: string;
    | {
        ok: true;
        startedAt: string;
>       endedAt: string;
        durationMinutes: number;
      }
    | {
        ok: false;
        error: string;
      };
  
  

LINE 627: const rawEndedAt = asString(body.endedAt) ?? asString(body.endTime);
      };
    }
  
>   const rawEndedAt = asString(body.endedAt) ?? asString(body.endTime);
    const explicitDurationMinutes = asNumber(body.durationMinutes);
  
    if (explicitDurationMinutes !== null && explicitDurationMinutes < 0) {
      return {
        ok: false,
        error: "durationMinutes must be greater than or equal to 0.",
      };
    }

LINE 637: if (rawEndedAt) {
      };
    }
  
>   if (rawEndedAt) {
      const endedDate = new Date(rawEndedAt);
  
      if (Number.isNaN(endedDate.getTime())) {
        return {
          ok: false,
          error: "Invalid endedAt or endTime.",
        };
      }

LINE 638: const endedDate = new Date(rawEndedAt);
    }
  
    if (rawEndedAt) {
>     const endedDate = new Date(rawEndedAt);
  
      if (Number.isNaN(endedDate.getTime())) {
        return {
          ok: false,
          error: "Invalid endedAt or endTime.",
        };
      }
  

LINE 643: error: "Invalid endedAt or endTime.",
      if (Number.isNaN(endedDate.getTime())) {
        return {
          ok: false,
>         error: "Invalid endedAt or endTime.",
        };
      }
  
      if (endedDate.getTime() < startedDate.getTime()) {
        return {
          ok: false,
          error: "endedAt must be greater than or equal to startedAt.",
        };

LINE 650: error: "endedAt must be greater than or equal to startedAt.",
      if (endedDate.getTime() < startedDate.getTime()) {
        return {
          ok: false,
>         error: "endedAt must be greater than or equal to startedAt.",
        };
      }
  
      return {
        ok: true,
        startedAt: startedDate.toISOString(),
        endedAt: endedDate.toISOString(),
        durationMinutes: Math.round(

LINE 657: endedAt: endedDate.toISOString(),
      return {
        ok: true,
        startedAt: startedDate.toISOString(),
>       endedAt: endedDate.toISOString(),
        durationMinutes: Math.round(
          (endedDate.getTime() - startedDate.getTime()) / 60000
        ),
      };
    }
  
    if (explicitDurationMinutes !== null) {
      const endedDate = new Date(

LINE 672: endedAt: endedDate.toISOString(),
      return {
        ok: true,
        startedAt: startedDate.toISOString(),
>       endedAt: endedDate.toISOString(),
        durationMinutes: explicitDurationMinutes,
      };
    }
  
    const endedDate = new Date();
  
    if (endedDate.getTime() < startedDate.getTime()) {
      return {

LINE 689: endedAt: endedDate.toISOString(),
    return {
      ok: true,
      startedAt: startedDate.toISOString(),
>     endedAt: endedDate.toISOString(),
      durationMinutes: Math.round(
        (endedDate.getTime() - startedDate.getTime()) / 60000
      ),
    };
  }
  
  async function getExistingImpactEventsCount(eventId: string) {
    const { count, error } = await supabase

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: status
LABEL: contract evidence
------------------------------------------------------------

LINE 8: ACTIVITY_COMPLETABLE_STATUSES,
    ACTIVITY_RECORDING_ENABLED,
  } from "../../../../../lib/activity/activityRecordingConfig";
  import {
>   ACTIVITY_COMPLETABLE_STATUSES,
    ACTIVITY_STATUS_COMPLETED,
    canTransitionActivityStatus,
    isCompletableActivityStatus,
  } from "../../../../../lib/activity/activityLifecycle";
  import {
    getDurationMs,
    safeCreateActivityProcessingLog,
  } from "../../../../../lib/activity/activityProcessingLogs";

LINE 9: ACTIVITY_STATUS_COMPLETED,
  } from "../../../../../lib/activity/activityRecordingConfig";
  import {
    ACTIVITY_COMPLETABLE_STATUSES,
>   ACTIVITY_STATUS_COMPLETED,
    canTransitionActivityStatus,
    isCompletableActivityStatus,
  } from "../../../../../lib/activity/activityLifecycle";
  import {
    getDurationMs,
    safeCreateActivityProcessingLog,
  } from "../../../../../lib/activity/activityProcessingLogs";
  import { getActivityUserContext } from "../../../../../lib/activity/activityUserContext";

LINE 10: canTransitionActivityStatus,
  import {
    ACTIVITY_COMPLETABLE_STATUSES,
    ACTIVITY_STATUS_COMPLETED,
>   canTransitionActivityStatus,
    isCompletableActivityStatus,
  } from "../../../../../lib/activity/activityLifecycle";
  import {
    getDurationMs,
    safeCreateActivityProcessingLog,
  } from "../../../../../lib/activity/activityProcessingLogs";
  import { getActivityUserContext } from "../../../../../lib/activity/activityUserContext";
  import { processActivityImpacts } from "../../../../../lib/activity/activityImpactProcessor";

LINE 11: isCompletableActivityStatus,
    ACTIVITY_COMPLETABLE_STATUSES,
    ACTIVITY_STATUS_COMPLETED,
    canTransitionActivityStatus,
>   isCompletableActivityStatus,
  } from "../../../../../lib/activity/activityLifecycle";
  import {
    getDurationMs,
    safeCreateActivityProcessingLog,
  } from "../../../../../lib/activity/activityProcessingLogs";
  import { getActivityUserContext } from "../../../../../lib/activity/activityUserContext";
  import { processActivityImpacts } from "../../../../../lib/activity/activityImpactProcessor";
  import { processActivityValueObjectBridge } from "../../../../../lib/activity/activityValueObjectLifecycle";

LINE 68: status: string;
    ended_at: string | null;
    duration_minutes: number | null;
    source: string | null;
>   status: string;
    privacy_scope: string | null;
    processing_status: string | null;
    metadata_json: Record<string, unknown> | null;
    created_at: string;
    updated_at: string;
  };
  
  type CompletionTiming =

LINE 70: processing_status: string | null;
    source: string | null;
    status: string;
    privacy_scope: string | null;
>   processing_status: string | null;
    metadata_json: Record<string, unknown> | null;
    created_at: string;
    updated_at: string;
  };
  
  type CompletionTiming =
    | {
        ok: true;

LINE 279: completedStatus: string;
  
  function getCategoryDerivationPolicyForCompleteRoute(params: {
    event: ActivityEventRow;
>   completedStatus: string;
  }): CategoryDerivationCompleteRoutePolicy {
    const { event, completedStatus } = params;
  
    if (event.status !== completedStatus) {
      return {
        enabled: false,
        reason: "not_completed_status",
        inputText: null,

LINE 281: const { event, completedStatus } = params;
    event: ActivityEventRow;
    completedStatus: string;
  }): CategoryDerivationCompleteRoutePolicy {
>   const { event, completedStatus } = params;
  
    if (event.status !== completedStatus) {
      return {
        enabled: false,
        reason: "not_completed_status",
        inputText: null,
        actorId: null,
      };

LINE 283: if (event.status !== completedStatus) {
  }): CategoryDerivationCompleteRoutePolicy {
    const { event, completedStatus } = params;
  
>   if (event.status !== completedStatus) {
      return {
        enabled: false,
        reason: "not_completed_status",
        inputText: null,
        actorId: null,
      };
    }
  

LINE 286: reason: "not_completed_status",
    if (event.status !== completedStatus) {
      return {
        enabled: false,
>       reason: "not_completed_status",
        inputText: null,
        actorId: null,
      };
    }
  
    if (!isCategoryDerivationSourceAllowedForCompleteRoute(event.source)) {
      return {
        enabled: false,

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: started
LABEL: contract evidence
------------------------------------------------------------

LINE 64: started_at: string | null;
    input_text: string | null;
    title: string | null;
    description: string | null;
>   started_at: string | null;
    ended_at: string | null;
    duration_minutes: number | null;
    source: string | null;
    status: string;
    privacy_scope: string | null;
    processing_status: string | null;
    metadata_json: Record<string, unknown> | null;
    created_at: string;

LINE 79: startedAt: string;
  type CompletionTiming =
    | {
        ok: true;
>       startedAt: string;
        endedAt: string;
        durationMinutes: number;
      }
    | {
        ok: false;
        error: string;
      };
  

LINE 611: if (!event.started_at) {
  }): CompletionTiming {
    const { event, body } = params;
  
>   if (!event.started_at) {
      return {
        ok: false,
        error: "Cannot complete activity event without started_at.",
      };
    }
  
    const startedDate = new Date(event.started_at);
  

LINE 614: error: "Cannot complete activity event without started_at.",
    if (!event.started_at) {
      return {
        ok: false,
>       error: "Cannot complete activity event without started_at.",
      };
    }
  
    const startedDate = new Date(event.started_at);
  
    if (Number.isNaN(startedDate.getTime())) {
      return {
        ok: false,

LINE 618: const startedDate = new Date(event.started_at);
      };
    }
  
>   const startedDate = new Date(event.started_at);
  
    if (Number.isNaN(startedDate.getTime())) {
      return {
        ok: false,
        error: "Stored started_at is invalid.",
      };
    }
  

LINE 620: if (Number.isNaN(startedDate.getTime())) {
  
    const startedDate = new Date(event.started_at);
  
>   if (Number.isNaN(startedDate.getTime())) {
      return {
        ok: false,
        error: "Stored started_at is invalid.",
      };
    }
  
    const rawEndedAt = asString(body.endedAt) ?? asString(body.endTime);
    const explicitDurationMinutes = asNumber(body.durationMinutes);

LINE 623: error: "Stored started_at is invalid.",
    if (Number.isNaN(startedDate.getTime())) {
      return {
        ok: false,
>       error: "Stored started_at is invalid.",
      };
    }
  
    const rawEndedAt = asString(body.endedAt) ?? asString(body.endTime);
    const explicitDurationMinutes = asNumber(body.durationMinutes);
  
    if (explicitDurationMinutes !== null && explicitDurationMinutes < 0) {
      return {

LINE 647: if (endedDate.getTime() < startedDate.getTime()) {
        };
      }
  
>     if (endedDate.getTime() < startedDate.getTime()) {
        return {
          ok: false,
          error: "endedAt must be greater than or equal to startedAt.",
        };
      }
  
      return {
        ok: true,

LINE 650: error: "endedAt must be greater than or equal to startedAt.",
      if (endedDate.getTime() < startedDate.getTime()) {
        return {
          ok: false,
>         error: "endedAt must be greater than or equal to startedAt.",
        };
      }
  
      return {
        ok: true,
        startedAt: startedDate.toISOString(),
        endedAt: endedDate.toISOString(),
        durationMinutes: Math.round(

LINE 656: startedAt: startedDate.toISOString(),
  
      return {
        ok: true,
>       startedAt: startedDate.toISOString(),
        endedAt: endedDate.toISOString(),
        durationMinutes: Math.round(
          (endedDate.getTime() - startedDate.getTime()) / 60000
        ),
      };
    }
  
    if (explicitDurationMinutes !== null) {

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: planned
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: pending
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: completed
LABEL: contract evidence
------------------------------------------------------------

LINE 9: ACTIVITY_STATUS_COMPLETED,
  } from "../../../../../lib/activity/activityRecordingConfig";
  import {
    ACTIVITY_COMPLETABLE_STATUSES,
>   ACTIVITY_STATUS_COMPLETED,
    canTransitionActivityStatus,
    isCompletableActivityStatus,
  } from "../../../../../lib/activity/activityLifecycle";
  import {
    getDurationMs,
    safeCreateActivityProcessingLog,
  } from "../../../../../lib/activity/activityProcessingLogs";
  import { getActivityUserContext } from "../../../../../lib/activity/activityUserContext";

LINE 279: completedStatus: string;
  
  function getCategoryDerivationPolicyForCompleteRoute(params: {
    event: ActivityEventRow;
>   completedStatus: string;
  }): CategoryDerivationCompleteRoutePolicy {
    const { event, completedStatus } = params;
  
    if (event.status !== completedStatus) {
      return {
        enabled: false,
        reason: "not_completed_status",
        inputText: null,

LINE 281: const { event, completedStatus } = params;
    event: ActivityEventRow;
    completedStatus: string;
  }): CategoryDerivationCompleteRoutePolicy {
>   const { event, completedStatus } = params;
  
    if (event.status !== completedStatus) {
      return {
        enabled: false,
        reason: "not_completed_status",
        inputText: null,
        actorId: null,
      };

LINE 283: if (event.status !== completedStatus) {
  }): CategoryDerivationCompleteRoutePolicy {
    const { event, completedStatus } = params;
  
>   if (event.status !== completedStatus) {
      return {
        enabled: false,
        reason: "not_completed_status",
        inputText: null,
        actorId: null,
      };
    }
  

LINE 286: reason: "not_completed_status",
    if (event.status !== completedStatus) {
      return {
        enabled: false,
>       reason: "not_completed_status",
        inputText: null,
        actorId: null,
      };
    }
  
    if (!isCategoryDerivationSourceAllowedForCompleteRoute(event.source)) {
      return {
        enabled: false,

LINE 423: completedStatus: string;
  
  async function runCategoryDerivationForCompleteRoute(params: {
    event: ActivityEventRow;
>   completedStatus: string;
  }): Promise<CategoryDerivationCompleteRouteResult> {
    const policy = getCategoryDerivationPolicyForCompleteRoute(params);
  
    if (!policy.enabled || policy.inputText === null || policy.actorId === null) {
      return {
        enabled: false,
        ok: null,
        skipped: true,

LINE 724: comment: "Completed lifecycle smoke test",
        : ACTIVITY_RECORDING_DISABLED_MESSAGE,
      example: {
        eventId: "activity-event-uuid",
>       comment: "Completed lifecycle smoke test",
      },
      deterministicTestExample: {
        eventId: "activity-event-uuid",
        durationMinutes: 5,
        comment: "Completed lifecycle smoke test with fixed duration",
      },
    });
  }

LINE 729: comment: "Completed lifecycle smoke test with fixed duration",
      deterministicTestExample: {
        eventId: "activity-event-uuid",
        durationMinutes: 5,
>       comment: "Completed lifecycle smoke test with fixed duration",
      },
    });
  }
  
  export async function POST(request: Request) {
    if (!ACTIVITY_RECORDING_ENABLED) {
      return NextResponse.json(
        {

LINE 815: const completedStatus = ACTIVITY_STATUS_COMPLETED;
    }
  
    const event = eventData as ActivityEventRow;
>   const completedStatus = ACTIVITY_STATUS_COMPLETED;
  
    if (event.status === completedStatus) {
      try {
        const existingImpactEventsCount = await getExistingImpactEventsCount(
          event.id
        );
  
        const impactResult = await processActivityImpacts({

LINE 817: if (event.status === completedStatus) {
    const event = eventData as ActivityEventRow;
    const completedStatus = ACTIVITY_STATUS_COMPLETED;
  
>   if (event.status === completedStatus) {
      try {
        const existingImpactEventsCount = await getExistingImpactEventsCount(
          event.id
        );
  
        const impactResult = await processActivityImpacts({
          eventId: event.id,
          userId: appUser.id,

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: .from("activity_events")
LABEL: contract evidence
------------------------------------------------------------

LINE 788: .from("activity_events")
    }
  
    const { data: eventData, error: eventError } = await supabase
>     .from("activity_events")
      .select("*")
      .eq("id", eventId)
      .eq("user_id", appUser.id)
      .maybeSingle();
  
    if (eventError) {
      return NextResponse.json(
        {

LINE 980: .from("activity_events")
    });
  
    const { data: updatedEventData, error: updateError } = await supabase
>     .from("activity_events")
      .update({
        ended_at: timing.endedAt,
        duration_minutes: timing.durationMinutes,
        status: completedStatus,
        processing_status: "processed",
        description: comment ?? event.description,
        metadata_json: {
          ...existingMetadata,

LINE 1471: .from("activity_events")
      });
  
      await supabase
>       .from("activity_events")
        .update({
          processing_status: "failed",
          updated_at: new Date().toISOString(),
        })
        .eq("id", updatedEvent.id)
        .eq("user_id", appUser.id);
  
      return NextResponse.json(

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: .insert({
LABEL: contract evidence
------------------------------------------------------------
NOT FOUND

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: .update({
LABEL: contract evidence
------------------------------------------------------------

LINE 981: .update({
  
    const { data: updatedEventData, error: updateError } = await supabase
      .from("activity_events")
>     .update({
        ended_at: timing.endedAt,
        duration_minutes: timing.durationMinutes,
        status: completedStatus,
        processing_status: "processed",
        description: comment ?? event.description,
        metadata_json: {
          ...existingMetadata,
          lifecycle: completedStatus,

LINE 1472: .update({
  
      await supabase
        .from("activity_events")
>       .update({
          processing_status: "failed",
          updated_at: new Date().toISOString(),
        })
        .eq("id", updatedEvent.id)
        .eq("user_id", appUser.id);
  
      return NextResponse.json(
        {

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: processing_status
LABEL: contract evidence
------------------------------------------------------------

LINE 70: processing_status: string | null;
    source: string | null;
    status: string;
    privacy_scope: string | null;
>   processing_status: string | null;
    metadata_json: Record<string, unknown> | null;
    created_at: string;
    updated_at: string;
  };
  
  type CompletionTiming =
    | {
        ok: true;

LINE 985: processing_status: "processed",
        ended_at: timing.endedAt,
        duration_minutes: timing.durationMinutes,
        status: completedStatus,
>       processing_status: "processed",
        description: comment ?? event.description,
        metadata_json: {
          ...existingMetadata,
          lifecycle: completedStatus,
          lifecycle_completed_at: nowIso,
          previous_status: event.status,
          completion_comment: comment,
          completion_duration_source:

LINE 1074: processingStatus: updatedEvent.processing_status,
      output: {
        activityEventId: updatedEvent.id,
        status: updatedEvent.status,
>       processingStatus: updatedEvent.processing_status,
      },
      metadata: {
        endpoint: "/api/activity/complete",
        mode: "template_first_complete",
      },
      startedAt: processingStartedAt.toISOString(),
      finishedAt: new Date().toISOString(),
      durationMs: getDurationMs(processingStartedAt),

LINE 1413: processedSignalResult?.signal?.processing_status ??
          ? {
              id: rawSignal.id,
              processingStatus:
>               processedSignalResult?.signal?.processing_status ??
                rawSignal.processing_status,
            }
          : null,
        processingRunId,
        lifecycle: {
          startedAt: timing.startedAt,
          endedAt: timing.endedAt,
          durationMinutes: timing.durationMinutes,

LINE 1414: rawSignal.processing_status,
              id: rawSignal.id,
              processingStatus:
                processedSignalResult?.signal?.processing_status ??
>               rawSignal.processing_status,
            }
          : null,
        processingRunId,
        lifecycle: {
          startedAt: timing.startedAt,
          endedAt: timing.endedAt,
          durationMinutes: timing.durationMinutes,
          impactsCreated: impactResult.counts.impactEvents > 0,

LINE 1473: processing_status: "failed",
      await supabase
        .from("activity_events")
        .update({
>         processing_status: "failed",
          updated_at: new Date().toISOString(),
        })
        .eq("id", updatedEvent.id)
        .eq("user_id", appUser.id);
  
      return NextResponse.json(
        {
          ok: false,

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: performed_by_actor_id
LABEL: contract evidence
------------------------------------------------------------

LINE 54: performed_by_actor_id: string | null;
  type ActivityEventRow = {
    id: string;
    user_id: string;
>   performed_by_actor_id: string | null;
    acting_as_actor_id: string | null;
    acting_for_actor_id: string | null;
    activity_type_id: string | null;
    activity_template_id: string | null;
    template_id: string | null;
    event_code: string | null;
    input_text: string | null;
    title: string | null;

LINE 313: event.performed_by_actor_id ??
    }
  
    const actorId =
>     event.performed_by_actor_id ??
      event.acting_as_actor_id ??
      event.acting_for_actor_id;
  
    if (!isUuidLikeForCategoryDerivation(actorId)) {
      return {
        enabled: false,
        reason: "missing_actor_id",
        inputText,

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: acting_as_actor_id
LABEL: contract evidence
------------------------------------------------------------

LINE 55: acting_as_actor_id: string | null;
    id: string;
    user_id: string;
    performed_by_actor_id: string | null;
>   acting_as_actor_id: string | null;
    acting_for_actor_id: string | null;
    activity_type_id: string | null;
    activity_template_id: string | null;
    template_id: string | null;
    event_code: string | null;
    input_text: string | null;
    title: string | null;
    description: string | null;

LINE 314: event.acting_as_actor_id ??
  
    const actorId =
      event.performed_by_actor_id ??
>     event.acting_as_actor_id ??
      event.acting_for_actor_id;
  
    if (!isUuidLikeForCategoryDerivation(actorId)) {
      return {
        enabled: false,
        reason: "missing_actor_id",
        inputText,
        actorId: null,

------------------------------------------------------------
FILE: src/app/api/activity/complete/route.ts
PATTERN: input_text
LABEL: contract evidence
------------------------------------------------------------

LINE 61: input_text: string | null;
    activity_template_id: string | null;
    template_id: string | null;
    event_code: string | null;
>   input_text: string | null;
    title: string | null;
    description: string | null;
    started_at: string | null;
    ended_at: string | null;
    duration_minutes: number | null;
    source: string | null;
    status: string;
    privacy_scope: string | null;

LINE 241: asStringForCategoryDerivation(event.input_text) ??
    event: ActivityEventRow
  ): string | null {
    return (
>     asStringForCategoryDerivation(event.input_text) ??
      asStringForCategoryDerivation(event.title) ??
      asStringForCategoryDerivation(event.description)
    );
  }
  
  function isCategoryDerivationSourceAllowedForCompleteRoute(
    source: string | null
  ): boolean {

============================================================
C) DECISION
============================================================
Use the route that can create status started/planned/pending event.
Do not reuse /api/activity/debug/free-text-value-object-test as final setup if it always creates completed events.
Next: build C2-B-FIX2 browser script using the selected non-completed setup route, then call production /api/activity/complete.